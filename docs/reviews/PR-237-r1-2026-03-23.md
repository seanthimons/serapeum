# PR #237 Review: HOTFIX: Fix download button for abstract-imported documents

**Branch:** hotfix/doc-notebook-download-broken -> integration
**Files changed:** 1 (0 new, 1 modified) | ~29 lines added
**Mergeability:** CLEAN
**Review round:** 1
**Date:** 2026-03-23

---

## HIGH -- Must fix before merge

1. **Unsafe filenames silently prevent text file creation on Windows** (`R/mod_document_notebook.R:663`) `[claude]`
   - Academic paper titles routinely contain characters invalid in Windows filenames: colons (`:`), slashes (`/`), question marks (`?`), angle brackets, etc. Example: `"Methods: A Systematic Review"` becomes filename `"Methods: A Systematic Review.txt"`.
   - On Windows, `writeLines(content, txt_path)` silently fails when the path contains a colon — no error is thrown, but no file is created. The download button then still 404s, making the fix ineffective for any paper with special characters in its title.
   - The root cause is at import time (`mod_search_notebook.R:3113` — `paste0(abs$title, ".txt")`), which is pre-existing. But this PR must sanitize at write time to avoid introducing a fix that silently fails.
   - Fix: Sanitize the filename before constructing the file path:
     ```r
     safe_filename <- gsub('[/:*?"<>|\\\\]', "_", docs$filename[j])
     txt_path <- file.path(pdf_dir, safe_filename)
     ```
     Also update the download URL construction at line 712-713 to use the same sanitized name, and update the delete handler at line 720 to match.

## MEDIUM -- Should fix

2. **`nchar(docs$filepath)` fragile on NA values** (`R/mod_document_notebook.R:657`) `[claude]` -- `nchar(NA)` returns `NA`, not `0`. The `filepath` column has a `NOT NULL` constraint so current data stores `""`, but a defensive check would be safer: `(is.na(docs$filepath) | nchar(docs$filepath) == 0)`.

## LOW -- Nice to have

3. **`!file.exists()` guard prevents refreshing stale text files** (`R/mod_document_notebook.R:664`) `[claude]` -- If a document's abstract were ever edited in the DB, the cached `.txt` file would never be rewritten. Acceptable if abstract editing is not a current workflow.

## Verified correct

- **`file.path()` uses forward slashes on Windows R**: Confirmed — `file.path("resource", "file.txt")` returns `"resource/file.txt"`, not backslash. URL construction is safe.
- **Schema columns exist**: `title`, `authors`, `year`, `doi`, `abstract_id` columns confirmed present via migration 008.
- **NA guards on metadata fields**: Lines 666-670 correctly check `!is.na()` before accessing `title`, `authors`, `year`, `doi`.
- **Delete handler cleans up txt files**: Line 720-721 uses the same path construction, so `.txt` files are deleted when documents are removed.
- **`.temp/` in `.gitignore`**: Covered by the `.*/` glob rule — runtime text files won't be committed.
- **`addResourcePath` registration**: Correctly conditional on `dir.exists(pdf_dir)`, which is TRUE after `dir.create` runs for text docs.
- **Tooltip conditional**: `if (is_pdf) "Download PDF" else "Download text"` is correct and clear.

## Pre-existing issues

- **Filename not sanitized at import time** (`R/mod_search_notebook.R:3113`) -- `paste0(abs$title, ".txt")` stores raw paper titles as filenames. This is the root cause of the unsafe-filename issue but predates this PR. Should be fixed separately with a sanitization function applied at import time.

---

**Recommendation:** Fix HIGH item #1 (filename sanitization at write time) before merge — without it, the fix silently fails for any paper whose title contains colons, slashes, or other special characters, which is common in academic papers. MEDIUM #2 is a one-line defensive improvement worth including. LOW #3 is acceptable as-is.

## Issues filed

| # | Issue | Severity | Finding |
|---|-------|----------|---------|
| 2 | #238 | MEDIUM | Defensive NA check for filepath |
| 3 | #239 | LOW | Cached .txt files not refreshed |
| — | #240 | PRE-EXISTING | Sanitize paper titles at import time |

**Convergence:** Round 1/3 | 1 open HIGH | 3 issues filed | Verdict: Fix 1 item
