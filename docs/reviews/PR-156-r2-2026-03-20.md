# PR #156 Review: feat: v12 UX Polish & Onboarding (#150, #87, #60, #9)

**Branch:** v12-ux-polish-onboarding -> main
**Files changed:** 17 (2 new, 15 modified) | ~1077 lines added
**Review round:** 2
**Date:** 2026-03-20

---

## Round 1 HIGH Items -- Resolution Status

1. **Missing `session` parameter in `bslib::update_switch()` for verbose_mode** -- **FIXED** (`R/mod_settings.R:327`)
   - Now reads: `bslib::update_switch("verbose_mode", value = isTRUE(verbose_mode), session = session)`
   - Confirmed correct and consistent with all other `update_switch` calls in the codebase.

## New Changes Since Round 1

The fix commits (bd20f54, 1063a13, 5e89a5a) made the following changes:

- **Prompt editor removed entirely** from both `mod_document_notebook.R` and `mod_search_notebook.R` (UI, JS handlers, server logic). Clean removal -- no orphaned references found.
- **API key redaction fixed** in `perform_openalex()` (`R/api_openalex.R:10`) -- was logging raw URL, now uses `gsub("api_key=[^&]+", "api_key=<REDACTED>", req$url)`.
- **`processing_doc_count` reactiveVal** added to `mod_document_notebook.R:188` -- avoids DB query during UI render. Correctly initialized before `is_processing(TRUE)` in all 7 preset handlers.
- **`updateSynthesisStatus` JS handler** moved from `mod_document_notebook_ui` to global `app.R:159-162` so both notebook modules can use it. Old location cleaned up.
- **Empty notebook guards** added to 3 search notebook presets (overview, conclusions, research questions) that previously lacked them.
- **Wizard step 4** text corrected from "New Document Notebook" to "Query Builder" with matching description.
- **`req(!is.null(input$has_seen_wizard))`** guard added (`app.R:671`) to prevent race condition on startup.
- **Verbose mode description** corrected from "browser console" to "R console" (`R/mod_settings.R:57`).
- **Roxygen docs** reordered in `rag.R` to fix misplaced docblock.
- **README** updated to remove "Editable prompts" bullet.

## Regression Check

No new HIGH issues found in the fix commits. All changes are clean deletions or straightforward additions. Specifically verified:

- All `prompt_edit`, `prompt_editor_open`, `prompt_details`, `populatePromptEditor`, and `populate_if_editor_open` references fully removed
- `processing_doc_count()` set before `is_processing(TRUE)` in all 7 handlers
- `docChatReady` JS handler is NOT orphaned -- still actively used for chat button state restoration
- `custom_prompt` parameter retained in `generate_preset()` signature for API flexibility (no callers currently use it)

## Round 1 MEDIUM/LOW Items -- Tracking

All MEDIUM items from round 1 were properly filed as GitHub issues:
- #200: Error toast behind modal
- #201: Duplicate synthesis helpers
- #202: Inconsistent error handling

These do not block merge per convergence protocol.

## Verified correct

- **API key redaction**: Fixed and now applies `gsub` before logging in `perform_openalex()` -- no credential exposure
- **Prompt editor removal**: Complete and clean across both modules, README, and JS handlers
- **Processing state**: New `processing_doc_count` reactiveVal eliminates DB query in render path
- **Wizard race condition**: `req(!is.null(input$has_seen_wizard))` prevents premature wizard evaluation
- **Empty notebook guards**: All search notebook presets now check for empty notebooks before processing

---

**Recommendation:** All round 1 HIGH items are resolved. No new HIGH issues introduced by the fix commits. The prompt editor removal was clean and thorough. Ship it.

**Convergence:** Round 2/3 | 0 open HIGHs | 3 issues filed (#200, #201, #202) | Verdict: Ship now
