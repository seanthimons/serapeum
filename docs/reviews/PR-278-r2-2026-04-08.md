# PR #278 Review: fix: standalone bugfix batch (6 issues)

**Branch:** feature/standalone-fixes -> integration
**Files changed:** 7 (1 new, 6 modified) | ~112 lines added
**Mergeability:** CLEAN
**Review round:** 2
**Date:** 2026-04-08

---

## HIGH — Must fix before merge

1. **`config.R` not sourced in import mirai worker — `OA_CONTENT_DOWNLOAD_COST_USD` undefined at runtime** (`R/mod_search_notebook.R:553-562`) `[claude]`
   - **Status: UNRESOLVED from round 1 (finding #1).** No changes have been made to the branch since round 1.
   - The batch import ExtendedTask worker sources `db_migrations.R`, `db.R`, `interrupt.R`, `bulk_import.R`, `_ragnar.R`, `pdf.R`, `api_openalex.R`, and `import_paper.R` — but NOT `config.R`. Since `import_paper.R:64` now references `OA_CONTENT_DOWNLOAD_COST_USD` (defined in `config.R`), the worker will throw `Error: object 'OA_CONTENT_DOWNLOAD_COST_USD' not found`. The outer `tryCatch` swallows it, silently breaking cost logging.
   - Fix: Add `source("R/config.R")` before `source("R/import_paper.R")` at line 555, matching the pattern at line 525 where the reindex worker already sources `config.R`.

## Verified correct (unchanged from round 1)

- **`extract_message_content` vector fix**: `all(nchar(content) == 0)` correctly handles multi-element vectors.
- **`build_latency_sparkline` Inf guard**: `!is.finite(max_ms) || max_ms <= 0` correctly handles all-NA, all-zero inputs.
- **API key NA guard in query builder**: `is.na() || !nzchar()` properly prevents `nchar(NA)` warning.
- **Config constant extraction**: Good pattern — just needs sourcing in the worker.
- **Test coverage**: 13 new tests with good boundary testing.

---

**Convergence:** Round 2/3 | 1 open HIGH | 0 new issues filed | Verdict: Fix 1 item (config.R sourcing in worker)

**Recommendation:** The single HIGH from round 1 remains unresolved — no commits have been pushed since the initial review. Add `source("R/config.R")` to the mirai worker's source list at `mod_search_notebook.R:555`. This is a one-line fix. Once applied, the PR is ready to merge.
