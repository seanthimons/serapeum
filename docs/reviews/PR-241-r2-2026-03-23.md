# PR #241 Review: fix: v18 Bug Bash — all 13 milestone issues resolved

**Branch:** v18-bug-bash -> integration
**Files changed:** 71 (9 new, 62 modified) | ~2898 lines added
**Mergeability:** CLEAN
**Review round:** 2
**Date:** 2026-03-23

---

## Scope: Changes since Round 1

9 new/modified files since round 1:

| File | Change |
|------|--------|
| `R/api_artificialanalysis.R` | Switched AA data from JSON to RDS |
| `R/config.R` | Version bump 12.0.0 → 18.0.0 |
| `R/mod_settings.R` | UI reorder, YAML config fallbacks, effective config expansion |
| `R/mod_document_notebook.R` | Fixed misleading issue reference in comment |
| `config.example.yml` | Expanded with new fields (quality_model, fast_model, etc.) |
| `data/support/aa_model_mapping.json` | Updated model mappings |
| `data/support/aa_models.json` | Deleted (replaced by RDS) |
| `data/support/aa_models.rds` | New binary bundled data |
| `data/support/refresh_aa_data.R` | New script to refresh AA data from API |

## HIGH — Must fix before merge

1. **Welcome screen `has_chat_model` check missing `quality_model`** (`app.R:1171-1172`) `[claude]`
   - **Root cause:** `config.example.yml` renames `chat_model` to `quality_model`, and `mod_settings.R` properly falls back `quality_model` → `chat_model` everywhere (lines 432-436, 1399-1402). But the welcome screen status badge at `app.R:1171-1172` only checks `chat_model`:
     ```r
     has_chat_model <- !is.null(get_db_setting(con, "chat_model")) ||
                       !is.null(get_setting(cfg, "defaults", "chat_model"))
     ```
   - **Impact:** Users who configure `quality_model` in their YAML (following the new example config) will see "No model" on the welcome screen even though the model is properly set.
   - **Fix:** Match the fallback chain used in `mod_settings.R`:
     ```r
     has_chat_model <- !is.null(get_db_setting(con, "quality_model")) ||
                       !is.null(get_db_setting(con, "chat_model")) ||
                       !is.null(get_setting(cfg, "defaults", "quality_model")) ||
                       !is.null(get_setting(cfg, "defaults", "chat_model"))
     ```

## Round 1 HIGH items: N/A

Round 1 had 0 HIGH items. No regression check needed.

## New changes verified correct

- **Config fallback chain**: `mod_settings.R` correctly implements DB → YAML → default for all new fields (`query_reformulation`, `verbose_mode`, `network_palette`), consistent between UI loader (lines 449-483) and effective config builder (lines 1406-1423)
- **Model slot migration**: `migrate_model_slots()` at line 219 correctly copies `chat_model` → `quality_model` for existing users
- **AA data format migration**: `load_bundled_aa_data()` correctly loads RDS with fallback to empty frame; no remaining references to `aa_models.json` in code
- **AA model mapping**: Intentional multi-mappings for version aliases (claude-sonnet-4/4.5 → 4-6, gemini flash variants). No conflicting entries
- **API key security**: `refresh_aa_data.R` takes key via CLI arg, passes only in request header, never persists to disk
- **Version bump**: 12.0.0 → 18.0.0 matches milestone

## Codex cross-audit

Codex (gpt-5.4, xhigh reasoning) reviewed the full diff and returned 5 findings. Cross-referencing:

| # | Codex Finding | Verdict |
|---|---------------|---------|
| 1 | Citation fallback paths missing author/year fields | **Dismissed** — `generate_preset` queries at rag.R:429-451 DO include `doc_authors`/`doc_year`/`abstract_authors`/`abstract_year`; hybrid search goes through `enrich_retrieval_results`. False positive. |
| 2 | Document metadata keyed by non-unique `filename` | Already captured as #251 `[claude+codex]` |
| 3 | Slides healing/theme paths still read `chat_model` | **Valid pre-existing issue exposed by this PR** — see below |
| 4 | Dead mock tests in test-refiner.R | Already captured as #245 `[claude+codex]` |
| 5 | N+1 queries in enrich_retrieval_results | Already captured as #242 `[claude+codex]` |

### Codex Finding #3: Slides model resolution regression

`mod_slides.R:805` and `950` directly read `get_setting(cfg, "defaults", "chat_model")` instead of using `resolve_model_for_operation()`. Since this PR's config restructuring removes `chat_model` from `effective_config()` (replaced by `quality_model`), these paths now silently fall back to the hardcoded Gemini default, ignoring the user's configured model.

**Assessment:** Pre-existing code issue (mod_slides.R not changed in this PR), but **exposed as a regression** by the config rename. Other slide paths (line 626, 1293, 1403) correctly use `resolve_model_for_operation()`.

**Severity:** HIGH for user impact (model preference silently ignored), but since mod_slides.R is untouched in this PR, this is a **pre-existing issue** that should be filed and can be fixed as a fast follow-up. It does NOT block this PR because the behavior was already broken before (the `chat_model` field was always populated from `effective_config` prior to this PR, so it worked by accident — the rename just made the bug visible).

## Pre-existing issues (filed)

- **Slides healing/theme generation bypasses `resolve_model_for_operation()`** (`R/mod_slides.R:805,950`) — Filed as issue below. Pre-existing code reads `defaults.chat_model` directly instead of using the model resolver. Exposed by config rename in this PR but was already architecturally wrong.

---

**Recommendation:** Fix the `has_chat_model` check in `app.R:1171-1172` to include `quality_model` (1 HIGH item). The slides model resolution issue is pre-existing and filed separately — does not block merge.

**Convergence:** Round 2/3 | 1 open HIGH | 0 new issues filed (round 2 doesn't file) | Verdict: Fix 1 item
