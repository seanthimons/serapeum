# PR #261 Review: HOTFIX: Fix rapi_bind length mismatch when saving citation networks

**Branch:** hotfix/citation-network-bind-length-v2 -> main
**Files changed:** 2 (1 modified, 1 new) | ~128 lines added
**Mergeability:** MERGEABLE (CI: UNSTABLE — pre-existing)
**Review round:** 1
**Date:** 2026-03-24

---

## Verified correct

- **NULL coercion for source_notebook_id** (`R/db.R:1507`): `%||% NA_character_` correctly converts NULL to a length-1 NA string, which DuckDB handles as a SQL NULL. This is the standard R idiom for this pattern.
- **Length-safe fallbacks for is_overlap/community** (`R/db.R:1523-1524`): `rep(FALSE, nrow(nodes_df))` and `rep(NA_character_, nrow(nodes_df))` guarantee the fallback vector matches the length of all other columns. The `if/else` is explicit and avoids the `%||%` scalar trap.
- **Parameterized queries** (`R/db.R:1502-1507`): All SQL uses parameterized binding — no SQL injection risk.
- **Test coverage** (`tests/testthat/test-save-network.R`): Both the NULL case (the bug) and the fully-populated case are tested. Assertions verify node count, column values, and metadata round-trip.
- **Test isolation**: Each test creates its own in-memory DuckDB with `on.exit(dbDisconnect)` — no cross-test leakage.

---

**Recommendation:** Ship. No HIGH, MEDIUM, or LOW findings. The fix is minimal, targeted, and well-tested. The CI instability is pre-existing and unrelated to this change.

**Convergence:** Round 1/3 | 0 open HIGHs | 0 issues filed | Verdict: Ship
