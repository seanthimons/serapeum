# PR #272 Review: v20.0 Shiny Reactivity Cleanup

**Branch:** gsd/phase-67-infrastructure -> integration
**Files changed:** 63 (+1 since r1) | ~6921 lines added
**Mergeability:** CLEAN
**Review round:** 2
**Date:** 2026-03-29

---

## Round 1 findings — resolution

| # | Finding | Severity | Status |
|---|---------|----------|--------|
| 1 | Unparameterized table_name in test helper (`test-db.R:6`) | MEDIUM | **Fixed** — `sprintf('%s')` replaced with `?` parameterization |
| 2 | Missing NULL assignment in session cleanup (`mod_document_notebook.R:1889,1894,1899`) | MEDIUM | **Fixed** — 3 NULL assignments added matching LIFE-02 pattern |
| 3 | Bootstrap INSERT uses literal values (`db_migrations.R:125`) | LOW | **Fixed** — parameterized with `VALUES (?, ?)` |
| 4 | Missing section_filter regression test (`test-db-leak.R:199-252`) | LOW | **Fixed** — 3-case test covers NA, empty string, and mixed inputs |

## New findings from fix commit

_No new HIGH findings._ The fix commit (`e75dd1f`) contains only the 4 targeted fixes plus documentation (TODO.md, review report). All changes are minimal and mechanical.

## Verified correct (fix commit)

- **Parameterized query in test helper** (`test-db.R:6-10`): `WHERE table_name = ?` with `list(table_name)` — DBI handles escaping. All 59 test-db.R assertions pass.
- **NULL assignment in session cleanup** (`mod_document_notebook.R:1889,1894,1899`): Now consistent with LIFE-02 pattern at line 946. Each NULL assignment follows immediately after the destroy call within the same loop iteration.
- **Parameterized bootstrap INSERT** (`db_migrations.R:125`): `VALUES (?, ?)` with `list(1L, "...")` — matches `apply_migration()` pattern. All 41 migration tests pass.
- **Section filter regression test** (`test-db-leak.R:199-252`): Proper test setup (temp DB, ragnar store, dummy chunk, on.exit cleanup with gc). All 3 expect_no_error assertions pass.

## Pre-existing issues (filed)

- **Stale api_key parameter in test-db-leak.R** (`test-db-leak.R:69,130`) — Filed as #277. Tests 1 and 2 pass `api_key = "test"` to `search_chunks_hybrid()` but this parameter was removed from the function signature. Both tests fail with "unused argument" error.

---

**Convergence:** Round 2/3 | 0 open HIGHs | 0 issues filed | Verdict: Ship

**Recommendation:** All 4 round 1 findings are resolved. No new HIGHs introduced. Tests pass. Ship it.
