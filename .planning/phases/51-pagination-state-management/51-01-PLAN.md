---
phase: 51-pagination-state-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_search_notebook.R
  - tests/testthat/test-pagination-state.R
autonomous: true
requirements: [PAGE-01, PAGE-05]

must_haves:
  truths:
    - "Refresh button resets cursor to NULL and fetches page 1"
    - "Cursor state resets when Edit Search parameters change (query, year, type, OA, min citations, retracted, search field)"
    - "Year slider on main view does NOT reset cursor"
    - "Sort dropdown reorders displayed papers client-side without resetting cursor"
    - "pagination_state tracks cursor, has_more, total_fetched, api_total without reactive loops"
    - "Result count displays 'X of Y results' in toolbar area"
  artifacts:
    - path: "R/mod_search_notebook.R"
      provides: "pagination_state reactiveValues, cursor reset logic, sort client-side, result count output"
      contains: "pagination_state <- reactiveValues"
    - path: "tests/testthat/test-pagination-state.R"
      provides: "Unit tests for format_result_count helper and pagination reset behavior"
  key_links:
    - from: "R/mod_search_notebook.R (do_search_refresh)"
      to: "pagination_state"
      via: "Updates cursor, has_more, api_total, total_fetched after API call"
      pattern: "pagination_state\\$cursor <- result\\$next_cursor"
    - from: "R/mod_search_notebook.R (save_search observer)"
      to: "pagination_state"
      via: "Resets cursor and has_more before triggering refresh"
      pattern: "pagination_state\\$cursor <- NULL"
    - from: "R/mod_search_notebook.R (papers_data reactive)"
      to: "list_abstracts"
      via: "Sort applied in R after DB fetch (client-side reorder)"
      pattern: "switch.*sort_by"
---

<objective>
Add server-side pagination state management to the search notebook module, enabling distinct Refresh vs Load More behaviors (Load More UI comes in Phase 52).

Purpose: Establish the reactive state layer that tracks the OpenAlex cursor, total counts, and has_more flag. Make sort client-side only. Display "X of Y results" count. This prepares for Phase 52's Load More button without any UI button changes in this phase.

Output: Updated mod_search_notebook.R with pagination_state reactiveValues, cursor reset on filter changes, client-side sort, result count display. Test file for helper logic.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-pagination-state-management/51-CONTEXT.md
@.planning/phases/51-pagination-state-management/51-RESEARCH.md
@.planning/phases/50-api-pagination-foundation/50-01-SUMMARY.md

<interfaces>
<!-- From R/api_openalex.R (Phase 50 output) -->
search_papers() returns: list(papers = [...], next_cursor = "string"|NULL, count = integer)
- cursor param: NULL = page 1 (sends "*" to API), string = continuation
- sort param: always "relevance_score" (locked decision)

<!-- From R/mod_search_notebook.R (existing state) -->
- search_refresh_trigger <- reactiveVal(0)  # line 2108 — triggers do_search_refresh
- paper_refresh <- reactiveVal(0)  # line 356 — triggers paper list re-read
- do_search_refresh() # line 2189 — calls search_papers(), saves to DB
- save_search observer # line 2127 — collects Edit Search params, updates notebook, triggers refresh
- papers_data reactive # line 931 — reads from DB with sort_by from input$sort_by
- list_abstracts(con, nb_id, sort_by) # R/db.R line 664 — SQL query with ORDER BY

<!-- From R/mod_search_notebook.R (existing reactiveValues pattern) -->
- delete_observers <- reactiveValues()  # line 361
- block_journal_observers <- reactiveValues()  # line 363
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pagination_state, update do_search_refresh, cursor reset, and client-side sort</name>
  <files>R/mod_search_notebook.R</files>
  <action>
  **1. Initialize pagination_state reactiveValues** (after line 376, near existing reactiveValues):

  ```r
  # Pagination state for cursor-based pagination (Phase 51)
  pagination_state <- reactiveValues(
    cursor = NULL,          # NULL = page 1, string = continuation token
    has_more = FALSE,       # TRUE if next_cursor was non-NULL in last response
    total_fetched = 0L,     # Total papers in notebook (from DB count)
    api_total = 0L          # Total matching papers from OpenAlex meta.count
  )
  ```

  **2. Update do_search_refresh() to update pagination state** (around line 2189):

  After the `search_papers()` call succeeds (line ~2240), add pagination state updates:
  ```r
  # Update pagination state after successful fetch (Phase 51)
  pagination_state$cursor <- result$next_cursor
  pagination_state$has_more <- !is.null(result$next_cursor)
  pagination_state$api_total <- result$count
  ```

  After papers are saved to DB and `paper_refresh()` is called (line ~2312), update total_fetched:
  ```r
  # Update total_fetched from actual DB count (Phase 51)
  pagination_state$total_fetched <- nrow(list_abstracts(con(), nb_id))
  ```

  Note: The existing `total_in_nb` variable on line 2315 already computes this. Reuse it:
  ```r
  pagination_state$total_fetched <- total_in_nb
  ```

  IMPORTANT: `do_search_refresh()` currently hardcodes `cursor = NULL` (line 2229). Keep this — Refresh always fetches page 1 per user decision. Phase 52 will add a separate Load More path.

  **3. Add cursor reset in save_search observer** (around line 2127):

  Before `search_refresh_trigger()` increment (line 2185), add:
  ```r
  # Reset pagination cursor — API query is changing (Phase 51)
  pagination_state$cursor <- NULL
  pagination_state$has_more <- FALSE
  ```

  This ensures that when Edit Search params change, the cursor resets to page 1. The `do_search_refresh()` called via `search_refresh_trigger` will then fetch page 1 and set fresh cursor/has_more/api_total.

  **4. Make sort client-side only** (papers_data reactive, line 931):

  Currently `papers_data()` calls `list_abstracts(con(), nb_id, sort_by = sort_by)` which does SQL ORDER BY. Change to:
  - Always fetch unsorted (or default sort): `list_abstracts(con(), nb_id)`
  - Apply sort in R after fetch:

  ```r
  papers_data <- reactive({
    paper_refresh()
    nb_id <- notebook_id()
    req(nb_id)
    sort_by <- input$sort_by %||% "year"
    papers <- list_abstracts(con(), nb_id)

    # Client-side sort — sort dropdown is display-only, does not affect API cursor (Phase 51)
    if (nrow(papers) > 0) {
      papers <- switch(sort_by,
        cited_by_count = papers[order(papers$cited_by_count, decreasing = TRUE, na.last = TRUE), ],
        fwci = papers[order(papers$fwci, decreasing = TRUE, na.last = TRUE), ],
        referenced_works_count = papers[order(papers$referenced_works_count, decreasing = TRUE, na.last = TRUE), ],
        papers[order(papers$year, papers$created_at, decreasing = TRUE, na.last = TRUE), ]  # default: year
      )
      rownames(papers) <- NULL
    }

    # BUGF-01 Part B: Pin seed paper to row 1 (existing logic preserved)
    # ... existing seed pinning code unchanged ...

    papers
  })
  ```

  Also update `list_abstracts()` in `R/db.R` to use a default sort when no `sort_by` given — OR simply pass "year" as default. Since `list_abstracts()` is called elsewhere, keep its interface unchanged. The key change is that `papers_data()` no longer passes `input$sort_by` to the DB — it always gets the full set and sorts locally.

  Actually, simpler approach: keep passing `sort_by = "year"` to `list_abstracts()` always (consistent DB ordering), then re-sort in R only when `sort_by != "year"`. This minimizes the change:

  ```r
  papers <- list_abstracts(con(), nb_id, sort_by = "year")  # Always default DB sort

  # Client-side re-sort if user selected different sort (Phase 51)
  if (nrow(papers) > 0 && sort_by != "year") {
    papers <- switch(sort_by,
      cited_by_count = papers[order(papers$cited_by_count, decreasing = TRUE, na.last = TRUE), ],
      fwci = papers[order(papers$fwci, decreasing = TRUE, na.last = TRUE), ],
      referenced_works_count = papers[order(papers$referenced_works_count, decreasing = TRUE, na.last = TRUE), ],
      papers
    )
    rownames(papers) <- NULL
  }
  ```

  **5. Add result count text output** in the server section:

  Create a helper function (at module level, outside server, for testability):
  ```r
  #' Format result count for display
  #' @param fetched Total papers in notebook
  #' @param total Total matching results from API
  #' @return Character string like "25 of 100 results" or ""
  format_result_count <- function(fetched, total) {
    if (is.null(total) || total == 0) return("")
    if (fetched >= total) return(paste(total, "results"))
    paste(fetched, "of", total, "results")
  }
  ```

  Add renderText output in server:
  ```r
  # Result count display (Phase 51)
  output$result_count <- renderText({
    format_result_count(pagination_state$total_fetched, pagination_state$api_total)
  })
  ```

  Add textOutput in UI — place it in the toolbar area, after the button group div (around line 101), as a small muted text span:
  ```r
  span(class = "text-muted small ms-2 align-self-center", textOutput(ns("result_count"), inline = TRUE))
  ```

  Place this inside the `d-flex gap-2` div (line 72) after the last button, so it appears at the end of the toolbar row.

  **6. Update total_fetched when papers change outside of search** (paper deletion, import):

  Add an observer that syncs total_fetched when paper_refresh changes:
  ```r
  # Sync total_fetched with DB when papers change (delete, import, etc.) (Phase 51)
  observeEvent(paper_refresh(), {
    nb_id <- notebook_id()
    if (!is.null(nb_id)) {
      pagination_state$total_fetched <- nrow(list_abstracts(con(), nb_id))
    }
  }, ignoreInit = TRUE)
  ```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3841, launch.browser=FALSE)" &amp; sleep 15 &amp;&amp; kill %1</automated>
  </verify>
  <done>
  - pagination_state reactiveValues initialized with cursor, has_more, total_fetched, api_total
  - do_search_refresh() updates all four pagination_state fields after API call
  - save_search observer resets cursor and has_more before triggering refresh
  - Sort dropdown reorders papers client-side only (does not affect cursor)
  - Result count "X of Y results" renders in toolbar area
  - total_fetched syncs when papers change (delete, import)
  - App starts without errors (smoke test)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for pagination state helpers</name>
  <files>tests/testthat/test-pagination-state.R</files>
  <behavior>
    - format_result_count(0, 0) returns ""
    - format_result_count(0, NULL) returns ""
    - format_result_count(25, 100) returns "25 of 100 results"
    - format_result_count(100, 100) returns "100 results"
    - format_result_count(150, 100) returns "100 results" (fetched >= total)
  </behavior>
  <action>
  Create `tests/testthat/test-pagination-state.R` with tests for the `format_result_count()` helper function.

  Source the module file to access the helper: `source("R/mod_search_notebook.R", local = TRUE)` — but since format_result_count is defined at module level inside the file, it may need to be extracted. If it's not accessible via sourcing, define it in a testable location.

  Better approach: Define `format_result_count()` as a standalone function near the top of `mod_search_notebook.R` (before the module UI/server functions) so it's accessible when sourced. Verify it's exported or at least sourceable.

  Test file:
  ```r
  # Test format_result_count helper (Phase 51)
  source("R/mod_search_notebook.R", local = TRUE)

  test_that("format_result_count returns empty string when no results", {
    expect_equal(format_result_count(0, 0), "")
    expect_equal(format_result_count(0, NULL), "")
  })

  test_that("format_result_count shows X of Y when more results available", {
    expect_equal(format_result_count(25, 100), "25 of 100 results")
    expect_equal(format_result_count(1, 500), "1 of 500 results")
  })

  test_that("format_result_count shows total when all fetched", {
    expect_equal(format_result_count(100, 100), "100 results")
    expect_equal(format_result_count(150, 100), "100 results")
  })
  ```

  Note: If sourcing the full module file fails due to Shiny dependencies, wrap `format_result_count` in an environment or extract to a small utility. The executor should handle this pragmatically — the function is pure (no Shiny deps) so it just needs to be reachable.

  Run TDD cycle:
  1. RED: Write tests first, run, confirm they fail (function not yet defined or not accessible)
  2. GREEN: Ensure format_result_count is defined and accessible, run tests, confirm pass
  3. Verify: Run full test suite to confirm no regressions
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-pagination-state.R')"</automated>
  </verify>
  <done>
  - test-pagination-state.R exists with 3+ test cases covering format_result_count
  - All tests pass
  - Full test suite shows no regressions
  </done>
</task>

</tasks>

<verification>
1. Shiny smoke test: App starts on port 3841 without errors
2. Unit tests: `testthat::test_file("tests/testthat/test-pagination-state.R")` passes
3. Full suite: `testthat::test_dir("tests/testthat")` shows no regressions
4. Code review: pagination_state reactiveValues exists, cursor reset in save_search, do_search_refresh updates state, sort is client-side, result count output exists
</verification>

<success_criteria>
- pagination_state reactiveValues tracks cursor, has_more, total_fetched, api_total
- Refresh always passes cursor=NULL (page 1 behavior preserved)
- save_search observer resets cursor before triggering refresh
- Sort dropdown does not affect cursor (client-side reorder only)
- "X of Y results" text appears in toolbar area
- All tests pass, app starts without errors
- Phase 52 can read pagination_state$cursor and pagination_state$has_more for Load More button
</success_criteria>

<output>
After completion, create `.planning/phases/51-pagination-state-management/51-01-SUMMARY.md`
</output>
