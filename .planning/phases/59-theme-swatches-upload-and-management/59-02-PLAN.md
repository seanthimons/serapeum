---
phase: 59-theme-swatches-upload-and-management
plan: "02"
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - R/mod_slides.R
autonomous: false
requirements: [THME-01, THME-02, THME-03, THME-04, THME-09]

must_haves:
  truths:
    - "Theme dropdown shows color swatch dots next to each built-in theme name"
    - "Custom themes uploaded via .scss file input appear in the dropdown with swatch dots and delete button"
    - "Clicking the delete button removes a custom theme from disk and dropdown without selecting it"
    - "Selecting a built-in theme sets options$theme to the theme name and options$custom_scss to NULL"
    - "Selecting a custom theme sets options$theme to 'default' and options$custom_scss to the full file path"
    - "Uploaded .scss files are validated for section markers and rejected with inline error if invalid"
    - "Uploaded themes persist in data/themes/ and reappear after app restart"
  artifacts:
    - path: "R/mod_slides.R"
      provides: "Theme UI with swatches, upload, delete, and base theme wiring"
      contains: "selectizeInput"
  key_links:
    - from: "R/mod_slides.R"
      to: "R/themes.R"
      via: "source and function calls"
      pattern: "build_theme_choices_df|list_custom_themes|validate_scss_file|BUILTIN_THEME_SWATCHES"
    - from: "R/mod_slides.R"
      to: "R/slides.R"
      via: "options$custom_scss passed to generate_slides"
      pattern: "custom_scss"
    - from: "R/mod_slides.R"
      to: "data/themes/"
      via: "file.copy for upload, file.remove for delete"
      pattern: "data/themes"
---

<objective>
Wire the theme helper functions from R/themes.R into the slide generation modal UI and server logic in R/mod_slides.R. Replace the plain selectInput with a selectizeInput showing color swatches, add upload/delete functionality, and wire the selected theme into the existing custom_scss pipeline.

Purpose: Complete the user-facing theme management experience — swatches, upload, delete, and base theme selection.
Output: Modified R/mod_slides.R with full theme UI
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-theme-swatches-upload-and-management/59-CONTEXT.md
@.planning/phases/59-theme-swatches-upload-and-management/59-RESEARCH.md
@.planning/phases/59-theme-swatches-upload-and-management/59-UI-SPEC.md
@.planning/phases/59-theme-swatches-upload-and-management/59-01-SUMMARY.md

<interfaces>
<!-- From R/themes.R (Plan 01 output) -->
```r
# Named list of 11 built-in themes, each list(bg, fg, accent)
BUILTIN_THEME_SWATCHES

# Extracts hex colors from scss:defaults block, resolving variable refs
# Returns list(bg, fg, accent) with defaults for missing values
parse_scss_swatches(scss_text)

# Returns TRUE if scss_text contains both section markers
validate_scss_file(scss_text)

# Scans data/themes/, parses each .scss, returns list of list(filename, label, bg, fg, accent)
list_custom_themes(themes_dir = "data/themes")

# Returns data.frame with columns: value, label, bg, fg, accent, group
# Built-in: value=theme_name, group="builtin"
# Custom: value=filename, group="custom"
build_theme_choices_df(custom_themes = list())
```

<!-- From R/mod_slides.R current structure -->
```r
# Line 88-93: current selectInput to replace
selectInput(ns("theme"), "Theme", choices = themes, selected = "default")

# Line 393-401: last_options assembly — wire custom_scss here
generation_state$last_options <- list(
  model = input$model,
  length = input$length,
  audience = input$audience,
  citation_style = input$citation_style,
  include_notes = input$include_notes,
  theme = input$theme,
  custom_scss = NULL,  # <-- Phase 59 replaces this hardcoded NULL
  custom_instructions = input$custom_instructions
)

# Line 622-628: healing path — already reads custom_scss from last_options (no change needed)
```

<!-- From 59-UI-SPEC.md -->
- Swatch dots: 10px diameter, border-radius 50%, 2px border in surface color
- Delete icon: danger red, stopPropagation, Shiny.setInputValue with namespaced ID
- Upload link: 14px, text-muted, below theme dropdown
- Validation error: inline alert-danger div below upload link
- Success notification: showNotification type="message", 3 second duration
- optgroup labels: "Built-in" and "Custom"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace selectInput with selectizeInput swatch dropdown and add upload/delete UI</name>
  <files>R/mod_slides.R</files>
  <read_first>
    - R/mod_slides.R (full file — understand current modal structure, server logic, generation_state)
    - R/themes.R (Plan 01 output — function signatures and return types)
    - .planning/phases/59-theme-swatches-upload-and-management/59-UI-SPEC.md (visual contract)
    - .planning/phases/59-theme-swatches-upload-and-management/59-RESEARCH.md (patterns 1-5, pitfalls 1-6)
  </read_first>
  <action>
    **A. Add source("R/themes.R") at the top of mod_slides.R** (after existing library/source calls, or inside the server function if the file uses that pattern). Verify how other R/ files are sourced in the project first — follow the existing pattern (likely sourced via app.R or Shiny's auto-source).

    **B. Replace the UI selectInput (lines 88-93) with a selectizeInput:**

    Remove:
    ```r
    selectInput(ns("theme"), "Theme", choices = themes, selected = "default")
    ```

    Replace with:
    ```r
    selectizeInput(
      ns("theme"),
      "Theme",
      choices = NULL,  # populated server-side via updateSelectizeInput
      options = list(
        render = I(paste0('{
          option: function(item, escape) {
            var dots = \'<span style="display:inline-flex;gap:3px;margin-right:6px;">\' +
              \'<span style="width:10px;height:10px;border-radius:50%;display:inline-block;background:\' + escape(item.bg) + \';border:2px solid rgba(128,128,128,0.3)"></span>\' +
              \'<span style="width:10px;height:10px;border-radius:50%;display:inline-block;background:\' + escape(item.fg) + \';border:2px solid rgba(128,128,128,0.3)"></span>\' +
              \'<span style="width:10px;height:10px;border-radius:50%;display:inline-block;background:\' + escape(item.accent) + \';border:2px solid rgba(128,128,128,0.3)"></span>\' +
              \'</span>\';
            var del = item.group === "custom"
              ? \'<span class="theme-delete-btn" data-value="\' + escape(item.value) + \'" onclick="event.stopPropagation();event.preventDefault();Shiny.setInputValue(\\\'', session$ns("theme_delete"), '\\\', this.getAttribute(\\\'data-value\\\'), {priority:\\\'event\\\'});return false;" style="margin-left:auto;cursor:pointer;color:#dc3545;padding:2px 6px;font-size:16px;line-height:1;">\\u00d7</span>\'
              : \'\';
            return \'<div style="display:flex;align-items:center;">\' + dots + escape(item.label) + del + \'</div>\';
          },
          item: function(item, escape) {
            var dots = \'<span style="display:inline-flex;gap:3px;margin-right:6px;">\' +
              \'<span style="width:10px;height:10px;border-radius:50%;display:inline-block;background:\' + escape(item.bg) + \';border:2px solid rgba(128,128,128,0.3)"></span>\' +
              \'<span style="width:10px;height:10px;border-radius:50%;display:inline-block;background:\' + escape(item.fg) + \';border:2px solid rgba(128,128,128,0.3)"></span>\' +
              \'<span style="width:10px;height:10px;border-radius:50%;display:inline-block;background:\' + escape(item.accent) + \';border:2px solid rgba(128,128,128,0.3)"></span>\' +
              \'</span>\';
            return \'<div style="display:flex;align-items:center;">\' + dots + escape(item.label) + \'</div>\';
          }
        }')),
        optgroupField = "group",
        optgroups = I('[{value: "builtin", label: "Built-in"}, {value: "custom", label: "Custom"}]'),
        searchField = list("label"),
        labelField = "label",
        valueField = "value"
      )
    )
    ```

    IMPORTANT: The `session$ns("theme_delete")` in the JS string is an R expression — it must be injected at render time. Since the UI function receives `ns` not `session`, this JS string must be constructed in the SERVER function when calling `updateSelectizeInput`, NOT in the static UI. In the UI, use a placeholder `selectizeInput(ns("theme"), "Theme", choices = NULL)` with NO render options. Then in the server, after the modal is shown, call `updateSelectizeInput` with the full options including the render callback that injects `session$ns("theme_delete")`.

    ALTERNATIVELY, if the `selectizeInput` with render must be in the UI function: pass the namespace prefix as a parameter to `mod_slides_modal_ui` and bake it into the JS string. The namespace prefix is `ns("")` which returns e.g. `"slides-"`. Then the JS uses `Shiny.setInputValue('slides-theme_delete', ...)`.

    **C. Add upload link and hidden fileInput below the theme selectizeInput:**

    After the theme selectizeInput, still inside the same `layout_columns` column (or as a new row below it), add:
    ```r
    # Upload link
    actionLink(ns("upload_theme_link"), "Upload custom theme (.scss)",
               icon = icon("upload"), class = "small text-muted"),
    # Hidden file input (triggered by the link)
    div(style = "display:none;",
        fileInput(ns("theme_file"), NULL, accept = ".scss")),
    # Validation error output
    uiOutput(ns("upload_error"))
    ```

    **D. Add JavaScript to trigger fileInput click from actionLink:**

    Add a `tags$script` in the modal UI (or use `shinyjs::onclick` if available) that wires the actionLink to trigger the hidden fileInput:
    ```r
    tags$script(HTML(sprintf(
      "$(document).on('click', '#%s', function(e) {
        e.preventDefault();
        $('#%s').click();
      });",
      ns("upload_theme_link"), ns("theme_file")
    )))
    ```
    Note: fileInput in Shiny renders as a div with an `<input type="file">` inside. The click target should be `$('#%s input[type=file]')` or `$('#%s').find('input[type=file]').click()`.

    **E. Add server observers for upload, delete, and theme selection:**

    Inside the slides module server function, add these observers:

    1. **Upload observer** — `observeEvent(input$theme_file, { ... })`:
       - Read uploaded file: `scss_text <- paste(readLines(input$theme_file$datapath, warn = FALSE), collapse = "\n")`
       - Validate: `if (!validate_scss_file(scss_text))` → render error via `output$upload_error <- renderUI(div(class="alert alert-danger py-1 px-2 small mt-1", "Invalid .scss file. Must contain /*-- scss:defaults --*/ and /*-- scss:rules --*/ sections."))`; return
       - Save: `dir.create("data/themes", recursive = TRUE, showWarnings = FALSE)` then `file.copy(input$theme_file$datapath, file.path("data/themes", input$theme_file$name), overwrite = TRUE)`
       - Clear error: `output$upload_error <- renderUI(NULL)`
       - Show notification: `showNotification(paste0("Theme '", input$theme_file$name, "' uploaded."), type = "message", duration = 3)`
       - Refresh dropdown: call the dropdown refresh helper (see F below)

    2. **Delete observer** — `observeEvent(input$theme_delete, { ... })`:
       - `theme_filename <- input$theme_delete`
       - `file.remove(file.path("data/themes", theme_filename))`
       - Refresh dropdown: call the dropdown refresh helper
       - If deleted theme was selected, reset selection to "default"

    3. **Theme selection wiring** — modify the last_options assembly (line 393-401) to replace `custom_scss = NULL` with dynamic logic:
       ```r
       selected_theme <- input$theme
       if (selected_theme %in% names(BUILTIN_THEME_SWATCHES)) {
         theme_val <- selected_theme
         custom_scss_val <- NULL
       } else {
         theme_val <- "default"
         custom_scss_val <- file.path("data/themes", selected_theme)
       }
       generation_state$last_options <- list(
         model = input$model,
         length = input$length,
         audience = input$audience,
         citation_style = input$citation_style,
         include_notes = input$include_notes,
         theme = theme_val,
         custom_scss = custom_scss_val,
         custom_instructions = input$custom_instructions
       )
       ```

    **F. Dropdown refresh helper:**

    Create a local function or reactive that rebuilds and pushes the theme dropdown:
    ```r
    refresh_theme_dropdown <- function(selected = NULL) {
      custom <- list_custom_themes()
      df <- build_theme_choices_df(custom)
      updateSelectizeInput(
        session, "theme",
        choices = df,
        selected = selected %||% input$theme %||% "default",
        server = TRUE
      )
    }
    ```

    Call `refresh_theme_dropdown()` on modal open (in the observer that calls `showModal`), after upload, and after delete.

    **G. Remove the static themes vector from mod_slides_modal_ui (lines 8-10):**

    Delete:
    ```r
    themes <- c("default", "beige", "blood", "dark", "league",
                "moon", "night", "serif", "simple", "sky", "solarized")
    ```

    This list is now provided by BUILTIN_THEME_SWATCHES in R/themes.R.

    **H. Source R/themes.R:**

    Check how R/mod_slides.R gets its dependencies. If app.R sources all R/*.R files (e.g., via `source()` calls or Shiny's auto-source from R/ directory), then R/themes.R will be auto-sourced. If not, add `source("R/themes.R")` at the top of mod_slides.R. Verify by checking app.R's sourcing pattern.
  </action>
  <verify>
    <automated>
      "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests/testthat')"
    </automated>
    Manual: Start the app, open slide generation modal, verify swatch dots appear next to theme names.
  </verify>
  <acceptance_criteria>
    - R/mod_slides.R contains `selectizeInput(ns("theme")` (not `selectInput(ns("theme")`)
    - R/mod_slides.R contains `optgroupField` in selectize options
    - R/mod_slides.R contains `observeEvent(input$theme_file` (upload handler)
    - R/mod_slides.R contains `observeEvent(input$theme_delete` (delete handler)
    - R/mod_slides.R contains `file.copy(input$theme_file$datapath, file.path("data/themes"` (save to data/themes)
    - R/mod_slides.R contains `file.remove(file.path("data/themes"` (delete from data/themes)
    - R/mod_slides.R contains `validate_scss_file` call in upload handler
    - R/mod_slides.R contains `build_theme_choices_df` call in dropdown refresh
    - R/mod_slides.R contains `list_custom_themes` call
    - R/mod_slides.R does NOT contain the old static `themes <- c("default", "beige",` vector
    - R/mod_slides.R last_options assembly contains logic checking `names(BUILTIN_THEME_SWATCHES)` to determine built-in vs custom
    - R/mod_slides.R contains `custom_scss_val <- file.path("data/themes", selected_theme)` for custom theme path
    - R/mod_slides.R contains `stopPropagation` in JS render callback
    - R/mod_slides.R contains `Shiny.setInputValue` with namespaced `theme_delete` in JS render callback
    - All existing tests in tests/testthat/ pass (no regressions)
  </acceptance_criteria>
  <done>
    Theme dropdown shows color swatch dots for all built-in themes. Upload link below dropdown opens file picker, validates .scss, saves to data/themes/, shows success notification, and refreshes dropdown. Delete button on custom themes removes file and updates dropdown without selecting the theme. Selecting a built-in theme sets theme=name, custom_scss=NULL. Selecting a custom theme sets theme="default", custom_scss=path. All existing tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify theme UI end-to-end</name>
  <files>R/mod_slides.R</files>
  <what-built>Complete theme management UI: swatch dropdown with color dots, upload flow, delete flow, and base theme wiring to slide pipeline.</what-built>
  <how-to-verify>
    1. Start the app: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)"`
    2. Open a notebook with documents, click "Generate Slides"
    3. Verify the Theme dropdown shows color dots (3 circles: bg/fg/accent) next to each built-in theme name
    4. Verify "Built-in" and "Custom" group headers appear in the dropdown
    5. Click "Upload custom theme (.scss)" link below the dropdown
    6. Upload www/themes/epa-owm.scss — verify success notification appears
    7. Reopen the dropdown — verify "epa-owm" appears in the "Custom" group with swatch dots
    8. Hover over the x button on epa-owm — verify it turns red
    9. Click the x button — verify epa-owm disappears from dropdown, selected theme does NOT change
    10. Upload epa-owm.scss again, select it, generate slides — verify slides render with EPA theme styling
    11. Try uploading a plain .txt file renamed to .scss (without section markers) — verify inline error message appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>Human visual verification of the theme management UI. No code changes needed.</action>
  <verify>User confirms all 11 verification steps pass.</verify>
  <done>User approves the theme UI end-to-end.</done>
</task>

</tasks>

<verification>
1. All tests pass: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests/testthat')"`
2. App starts without errors (smoke test)
3. Human verification of visual UI and interactive flows
</verification>

<success_criteria>
- Theme dropdown renders with 3 color swatch dots per theme
- Built-in and custom themes are visually grouped
- Upload flow: file picker -> validation -> save to data/themes/ -> dropdown refresh
- Delete flow: x click -> file removed -> dropdown refresh (no theme selection change)
- Built-in theme selection: theme=name, custom_scss=NULL
- Custom theme selection: theme="default", custom_scss=data/themes/filename.scss
- Uploaded themes survive app restart (persist on disk)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/59-theme-swatches-upload-and-management/59-02-SUMMARY.md`
</output>
