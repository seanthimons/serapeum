---
phase: 01-multi-seeded-citation-network
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - R/mod_citation_network.R
autonomous: false
requirements:
  - MSEED-07

must_haves:
  truths:
    - "After building a multi-seed network, side panel has two tabs: Paper Details and Missing Papers"
    - "Missing Papers tab lists network nodes NOT in the originating notebook"
    - "User can import individual missing papers into the originating notebook"
    - "Papers already in the notebook do not appear in Missing Papers list"
  artifacts:
    - path: "R/mod_citation_network.R"
      provides: "Tabbed side panel with Paper Details and Missing Papers tabs, import handler"
  key_links:
    - from: "R/mod_citation_network.R:missing_papers_content"
      to: "DuckDB abstracts table"
      via: "Set-difference query: network paper_ids NOT IN notebook abstracts"
      pattern: "NOT IN.*abstracts"
    - from: "R/mod_citation_network.R:import_paper handler"
      to: "R/api_openalex.R:get_paper + R/db.R:add_abstract"
      via: "Fetch paper from OpenAlex then add to notebook"
      pattern: "get_paper.*add_abstract"
---

<objective>
Add the discovery + import workflow: a tabbed side panel with "Paper Details" (existing) and "Missing Papers" (new) tabs, where users can see and import network papers not yet in their notebook.

Purpose: This is the key discovery value of multi-seed networks — users can find important papers in the citation graph that they haven't collected yet and import them directly. The Missing Papers tab uses a lightweight DuckDB set-difference query (explicitly NOT the full citation audit module).

Output: Tabbed side panel, missing papers list with import buttons, working import flow.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-seeded-citation-network/01-CONTEXT.md
@.planning/phases/01-multi-seeded-citation-network/01-RESEARCH.md
@.planning/phases/01-multi-seeded-citation-network/01-01-SUMMARY.md
@.planning/phases/01-multi-seeded-citation-network/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From R/mod_citation_network.R (module state after Plan 02):
```r
current_seed_ids <- reactiveVal(character())
source_notebook_id <- reactiveVal(NULL)
current_network_data <- reactiveVal(NULL)
# current_network_data() returns list(nodes, edges, metadata)
# metadata includes: source_notebook_id, seed_paper_ids

selected_node_id <- reactiveVal(NULL)
# Side panel currently renders when selected_node_id() is not NULL
# output$side_panel <- renderUI({ ... })
```

From R/db.R (existing functions):
```r
add_abstract <- function(con, notebook_id, paper)
# paper is a list with: paper_id, title, authors, abstract, keywords, year, venue, pdf_url, work_type, work_type_crossref, oa_status, is_oa, cited_by_count, referenced_works_count, fwci, doi
```

From R/api_openalex.R:
```r
get_paper <- function(paper_id, email, api_key = NULL)
# Returns paper list or NULL on error
```

DuckDB tables:
```sql
-- abstracts table has: id, notebook_id, paper_id, title, authors, ...
SELECT paper_id FROM abstracts WHERE notebook_id = ?
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tabbed side panel with Missing Papers tab and import workflow</name>
  <files>R/mod_citation_network.R</files>
  <action>
  1. **Restructure side panel output (around line 754):**
     Replace the current `output$side_panel <- renderUI({...})` with a tabbed layout:

     - The side panel should always show when a network is built (not only when a node is selected)
     - Use `bslib::navset_card_tab()` for tabs
     - Two tabs:
       a. **"Paper Details"** — shows existing paper details content (only when a node is selected, otherwise "Click a node to see details")
       b. **"Missing Papers"** — shows papers in the network but not in the originating notebook

     Implementation:
     ```r
     output$side_panel <- renderUI({
       net_data <- current_network_data()
       if (is.null(net_data)) return(NULL)

       ns <- session$ns
       notebook_id <- source_notebook_id()

       navset_card_tab(
         id = ns("side_panel_tabs"),

         nav_panel(
           title = "Paper Details",
           value = "details",
           uiOutput(ns("paper_details_content"))
         ),

         # Only show Missing Papers tab when there's a source notebook
         if (!is.null(notebook_id)) {
           nav_panel(
             title = tagList("Missing Papers ", uiOutput(ns("missing_count_badge"), inline = TRUE)),
             value = "missing",
             uiOutput(ns("missing_papers_content"))
           )
         }
       )
     })
     ```

  2. **Move existing paper details into sub-output:**
     Create `output$paper_details_content <- renderUI({...})` containing the current side panel body (paper title, authors, year, venue, citations, DOI, abstract, Explore button). When `selected_node_id()` is NULL, show placeholder text: "Click a node to see paper details."

  3. **Create missing papers output:**
     ```r
     # Compute missing papers reactively
     missing_papers_data <- reactive({
       net_data <- current_network_data()
       req(net_data)
       notebook_id <- source_notebook_id()
       if (is.null(notebook_id)) return(NULL)

       # Get all paper_ids in the network (exclude seed papers — they're already in notebook)
       network_paper_ids <- net_data$nodes$paper_id[!net_data$nodes$is_seed]

       if (length(network_paper_ids) == 0) return(data.frame())

       # Set-difference query: network papers NOT in notebook
       # Use parameterized query with DuckDB list
       con <- con_r()
       notebook_paper_ids <- dbGetQuery(con,
         "SELECT paper_id FROM abstracts WHERE notebook_id = ?",
         list(notebook_id)
       )$paper_id

       missing_ids <- setdiff(network_paper_ids, notebook_paper_ids)
       if (length(missing_ids) == 0) return(data.frame())

       # Get details from network nodes data (already have title, authors, year, citations)
       net_data$nodes[net_data$nodes$paper_id %in% missing_ids,
                      c("paper_id", "paper_title", "authors", "year", "cited_by_count", "doi", "is_overlap"),
                      drop = FALSE]
     })

     # Badge showing count
     output$missing_count_badge <- renderUI({
       mp <- missing_papers_data()
       if (is.null(mp) || nrow(mp) == 0) return(NULL)
       span(class = "badge bg-info", nrow(mp))
     })

     output$missing_papers_content <- renderUI({
       ns <- session$ns
       notebook_id <- source_notebook_id()

       if (is.null(notebook_id)) {
         return(div(class = "text-muted p-3",
           "No source notebook — network was built from sidebar seed search.",
           br(), br(),
           "To see missing papers, seed the network from a search notebook or BibTeX import."
         ))
       }

       mp <- missing_papers_data()
       if (is.null(mp) || nrow(mp) == 0) {
         return(div(class = "text-muted p-3",
           icon("circle-check", class = "text-success"),
           " All network papers are already in your notebook."
         ))
       }

       # Sort: overlap papers first (more interesting), then by citation count
       mp <- mp[order(-as.integer(mp$is_overlap), -mp$cited_by_count), ]

       tagList(
         div(class = "p-2 small text-muted",
           paste(nrow(mp), "papers found in the network but not in your notebook.")
         ),
         div(
           style = "max-height: 550px; overflow-y: auto;",
           lapply(seq_len(nrow(mp)), function(i) {
             paper <- mp[i, ]
             div(
               class = "border-bottom p-2",
               div(
                 class = "d-flex justify-content-between align-items-start",
                 div(
                   strong(class = "small", paper$paper_title),
                   if (isTRUE(paper$is_overlap)) {
                     span(class = "badge bg-info ms-1", "overlap")
                   }
                 ),
                 tags$button(
                   class = "btn btn-sm btn-outline-primary ms-2 flex-shrink-0",
                   onclick = sprintf(
                     "Shiny.setInputValue('%s', '%s', {priority: 'event'});",
                     ns("import_missing_paper"), paper$paper_id
                   ),
                   icon("plus"), " Import"
                 )
               ),
               div(class = "small text-muted", paper$authors),
               div(class = "small text-muted",
                 paste0(
                   if (!is.na(paper$year)) paste("Year:", paper$year) else "",
                   if (!is.na(paper$cited_by_count)) paste(" | Citations:", paper$cited_by_count) else ""
                 )
               )
             )
           })
         )
       )
     })
     ```

  4. **Import handler:**
     ```r
     observeEvent(input$import_missing_paper, {
       paper_id <- input$import_missing_paper
       notebook_id <- source_notebook_id()
       req(paper_id, notebook_id)

       config <- config_r()
       email <- config$openalex$email

       tryCatch({
         # Fetch full paper details from OpenAlex
         paper <- get_paper(paper_id, email, api_key = NULL)
         if (is.null(paper)) {
           showNotification("Could not fetch paper details", type = "error")
           return()
         }

         # Add to notebook
         add_abstract(con_r(), notebook_id, paper)

         showNotification(
           paste("Imported:", substr(paper$title, 1, 60), "..."),
           type = "message"
         )

         # Refresh missing papers list by invalidating reactive
         # The missing_papers_data reactive will re-query and the imported paper will be excluded
       }, error = function(e) {
         showNotification(paste("Import failed:", e$message), type = "error")
       })
     })
     ```

  5. **Clean up the old side panel close/explore handlers:**
     - The close_panel handler (line 872) should still work — it clears `selected_node_id()` which hides paper details in the tab
     - The explore_from_node handler (line 877) should still work
     - Both now operate within the "Paper Details" tab only

  AVOID: Don't use the citation audit module for missing papers detection — just simple set-difference. Don't add "Import All" bulk button (deferred per research recommendation). Don't auto-switch to Missing Papers tab — let users discover it naturally.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "tryCatch({ source('R/mod_citation_network.R'); cat('Module loaded OK\n') }, error = function(e) { cat('ERROR:', e$message, '\n'); quit(status=1) })"</automated>
  </verify>
  <done>
  - Side panel has two tabs: Paper Details and Missing Papers
  - Paper Details tab shows existing behavior (node details when clicked, placeholder when not)
  - Missing Papers tab shows network papers not in originating notebook
  - Missing papers sorted by overlap first, then citation count
  - Each missing paper has Import button that fetches from OpenAlex and adds to notebook
  - Badge shows missing paper count on the tab
  - When no source notebook (single-seed from sidebar), Missing Papers tab not shown or shows explanatory message
  - Import removes paper from missing list on next render
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete multi-seed citation network feature</name>
  <action>
  Human verifies complete multi-seed citation network feature:
  1. Search notebook "Seed Network" button sends all papers to citation network
  2. BibTeX import "Seed Citation Network" button sends imported papers
  3. Multi-seed BFS builds combined network with deduplication
  4. Star shapes for seeds, diamond shapes for papers reachable from 2+ seeds
  5. Tabbed side panel with Paper Details and Missing Papers tabs
  6. Import buttons for missing papers
  </action>
  <verify>
  1. Open the app and navigate to a search notebook that has 3+ papers
  2. Click the "Seed Citation Network" button (share-nodes icon) in the papers toolbar
  3. Verify: switches to Citation Network tab, does NOT auto-build
  4. Set depth to 1, node cap to 50, click "Build Network"
  5. Verify: progress shows "Processing seed X of Y..."
  6. Verify: seed papers have star shape, shared papers have diamond shape
  7. Verify: legend shows star/diamond/dot entries
  8. Check the "Missing Papers" tab in the side panel
  9. Verify: lists papers not in the notebook with Import buttons
  10. Click Import on one paper, verify it's added and disappears from list
  11. (Optional) Test BibTeX import: import a .bib file, click "Seed Citation Network" in the modal
  12. (Optional) Test single-seed: use sidebar seed search — verify it still works
  </verify>
  <done>User types "approved" or describes issues to fix</done>
</task>

</tasks>

<verification>
- Tabbed side panel renders with Paper Details and Missing Papers tabs
- Missing Papers shows correct set-difference (network minus notebook)
- Import buttons work (fetch from OpenAlex, add to notebook)
- Full end-to-end flow works: seed from notebook -> build network -> discover missing papers -> import
</verification>

<success_criteria>
- Discovery + import workflow functional end-to-end
- Missing papers correctly identified via DuckDB query
- Individual paper import works
- User has verified the complete feature via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-seeded-citation-network/01-03-SUMMARY.md`
</output>
