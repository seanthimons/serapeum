---
phase: 65-observer-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_document_notebook.R
  - R/mod_slides.R
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03

must_haves:
  truths:
    - "Slide chip handlers are registered exactly once at module init, not re-registered on modal open"
    - "Figure action observers are destroyed before re-creation when figures are re-extracted"
    - "list_documents() DB query runs once per invalidation cycle, not once per renderUI block"
  artifacts:
    - path: "R/mod_slides.R"
      provides: "LIFE-01 verification — chip handlers confirmed at module-server top level"
    - path: "R/mod_document_notebook.R"
      provides: "LIFE-02 ordering verified, LIFE-03 reactive caching added"
      contains: "docs_reactive"
  key_links:
    - from: "R/mod_document_notebook.R docs_reactive"
      to: "output$document_list and output$index_action_ui"
      via: "docs_reactive() call replaces direct list_documents()"
      pattern: "docs_reactive\\(\\)"
    - from: "R/mod_document_notebook.R destroy loop (line ~931)"
      to: "fig_refresh() increment (line ~940)"
      via: "Sequential execution: destroy completes before fig_refresh triggers renderUI"
      pattern: "fig_action_observers\\[\\[old_id\\]\\] <- NULL"
---

<objective>
Verify and fix observer registration patterns in mod_slides.R and mod_document_notebook.R: confirm chip handlers are not accumulated (LIFE-01), verify figure action observer destroy-before-recreate ordering (LIFE-02), and cache list_documents() in a reactive() to eliminate redundant DB queries during async processing (LIFE-03).

Purpose: Eliminate observer accumulation and redundant DB queries that degrade performance during repeated modal opens and document processing.
Output: Two hardened module files with verified observer lifecycle and cached document queries.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-observer-lifecycle/65-CONTEXT.md
@.planning/phases/65-observer-lifecycle/65-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs -->

From R/mod_research_refiner.R (canonical destroy-before-recreate):
```r
seed_observers <- reactiveValues()
observe({
  seeds <- seed_papers()
  for (nm in names(seed_observers)) {
    seed_observers[[nm]]$destroy()
    seed_observers[[nm]] <- NULL
  }
  lapply(seq_along(seeds), function(i) {
    obs <- observeEvent(input[[paste0("remove_seed_", i)]], { ... }, ignoreInit = TRUE, once = TRUE)
    seed_observers[[paste0("obs_", i)]] <- obs
  })
})
```

From R/mod_document_notebook.R (current state):
- Line 197: `doc_refresh <- reactiveVal(0)`
- Line 205: `extract_observers <- reactiveValues()`
- Line 206: `fig_action_observers <- reactiveValues()`
- Lines 281-285: `output$index_action_ui <- renderUI({ ... docs <- list_documents(con(), nb_id) ... })`
- Lines 639-644: `output$document_list <- renderUI({ doc_refresh(); ... docs <- list_documents(con(), nb_id) ... })`
- Lines 931-938: destroy loop for fig_action_observers
- Line 940: `fig_refresh(fig_refresh() + 1)` after destroy
- Lines 1022-1098: figure action observer registration with `is.null()` guard

From R/mod_slides.R (current state):
- Lines 1217-1225: `lapply(seq_len(10), function(i) { observeEvent(input[[paste0("chip_", i)]], {...}) })` — at module server top level
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit LIFE-01 chip handlers and verify LIFE-02 figure observer ordering</name>
  <files>R/mod_slides.R, R/mod_document_notebook.R</files>
  <read_first>
    - R/mod_slides.R (full file — confirm lapply at lines 1217-1225 is at module server body level, NOT inside any observe/renderUI/observeEvent wrapper)
    - R/mod_document_notebook.R lines 920-945 (destroy loop and fig_refresh increment)
    - R/mod_document_notebook.R lines 1015-1100 (figure action observer registration inside renderUI)
    - R/mod_document_notebook.R lines 980-1010 (renderUI that triggers observer registration — confirm it depends on fig_refresh())
  </read_first>
  <action>
**LIFE-01 Audit (mod_slides.R):**

1. Read the full mod_slides_server function body. Confirm the `lapply(seq_len(10), function(i) { observeEvent(...) })` at lines 1217-1225 is directly in the moduleServer callback body — not nested inside any `observe()`, `observeEvent()`, `renderUI()`, or `reactive()`.

2. If confirmed at top level: LIFE-01 is already correct. Add a comment above the lapply block:
```r
# LIFE-01: Pre-allocated chip handlers — registered once at module init.
# current_chips() reactiveVal gates which indices are active.
```

3. If NOT at top level (inside a reactive scope): apply destroy-before-recreate using the mod_research_refiner.R pattern. Create a `chip_observers <- reactiveValues()` store, iterate `names()` calling `$destroy()` + NULL assignment, then re-create via `lapply`. This is decision D-02 from CONTEXT.md.

**LIFE-02 Verification (mod_document_notebook.R):**

4. Verify the execution order: the destroy loop at lines 931-938 runs inside the extraction success handler. It iterates `names(fig_action_observers)`, calls `$destroy()` on each observer in the list, assigns NULL. Then at line 940, `fig_refresh(fig_refresh() + 1)` increments the counter that invalidates the gallery renderUI (which contains the observer registration at lines 1022-1098). Since Shiny processes reactive invalidations sequentially, the destroy is guaranteed to complete before renderUI re-executes.

5. Verify the `is.null(fig_action_observers[[fig$id]])` guard at line 1023 works correctly after destroy: since the destroy loop assigns `fig_action_observers[[old_id]] <- NULL`, the `is.null()` check will return TRUE for re-extracted figures with the same ID, allowing re-registration. This is correct.

6. If ordering is correct, add a comment at the destroy loop:
```r
# LIFE-02: Destroy all figure action observers before re-extraction.
# fig_refresh() increment (below) triggers gallery renderUI to re-register fresh observers.
# Sequential invalidation guarantees destroy completes before re-registration.
```

7. Add defensive tryCatch around the `$destroy()` call to handle already-destroyed observers gracefully:
```r
for (old_id in names(fig_action_observers)) {
  obs_list <- fig_action_observers[[old_id]]
  if (is.list(obs_list)) {
    for (obs in obs_list) {
      if (!is.null(obs)) tryCatch(obs$destroy(), error = function(e) NULL)
    }
  }
  fig_action_observers[[old_id]] <- NULL
}
```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests/testthat')"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_slides.R contains comment "LIFE-01: Pre-allocated chip handlers" above the lapply(seq_len(10)) block
    - R/mod_document_notebook.R contains comment "LIFE-02: Destroy all figure action observers" above the destroy loop
    - R/mod_document_notebook.R destroy loop uses tryCatch around obs$destroy() calls
    - The lapply(seq_len(10)) in mod_slides.R is NOT inside any observe(), renderUI(), or observeEvent() — confirmed by code structure
    - All existing tests pass
  </acceptance_criteria>
  <done>LIFE-01 verified as accumulation-free (or fixed if needed), LIFE-02 ordering verified and destroy loop hardened with tryCatch</done>
</task>

<task type="auto">
  <name>Task 2: Cache list_documents() in reactive() for LIFE-03</name>
  <files>R/mod_document_notebook.R</files>
  <read_first>
    - R/mod_document_notebook.R lines 195-215 (reactive state declarations — where to add docs_reactive)
    - R/mod_document_notebook.R lines 280-300 (output$index_action_ui renderUI with list_documents call)
    - R/mod_document_notebook.R lines 635-700 (output$document_list renderUI with list_documents call and doc_refresh dependency)
    - R/mod_document_notebook.R lines 189-195 (function signature — confirm notebook_id and con are available)
  </read_first>
  <action>
**LIFE-03: Extract list_documents() into a cached reactive() expression.**

1. After the `fig_action_observers <- reactiveValues()` declaration (around line 206), add a new reactive expression:
```r
# LIFE-03: Cache list_documents() result — runs once per invalidation cycle
# regardless of how many renderUI blocks depend on it.
docs_reactive <- reactive({
  doc_refresh()        # Invalidate on refresh counter
  nb_id <- notebook_id()
  req(nb_id)
  list_documents(con(), nb_id)
})
```

2. Update `output$document_list` renderUI (around line 639):
   - REMOVE the direct `doc_refresh()` call (line 640) — the dependency is inherited through `docs_reactive()`
   - REMOVE the direct `list_documents(con(), nb_id)` call (line 644)
   - REMOVE the direct `nb_id <- notebook_id()` and `req(nb_id)` calls (lines 641-642) — these move into the reactive
   - REPLACE with: `docs <- docs_reactive()`
   - The renderUI body should start with `docs <- docs_reactive()` then proceed to the `if (nrow(docs) == 0)` check

   Before:
   ```r
   output$document_list <- renderUI({
     doc_refresh()
     nb_id <- notebook_id()
     req(nb_id)
     docs <- list_documents(con(), nb_id)
     ...
   })
   ```

   After:
   ```r
   output$document_list <- renderUI({
     docs <- docs_reactive()
     nb_id <- notebook_id()
     ...
   })
   ```

   NOTE: `nb_id` is still needed later in the renderUI body (for PDF path construction, resource path registration, etc.), so keep `nb_id <- notebook_id()` but it no longer needs `req()` since `docs_reactive()` already handles that. If `nb_id` is used after the `docs_reactive()` call, read it with `notebook_id()` (Shiny caches within the same flush cycle).

3. Update `output$index_action_ui` renderUI (around line 281):
   - REMOVE the direct `list_documents(con(), nb_id)` call (line 285)
   - REMOVE `nb_id <- notebook_id()` and `req(nb_id)` (lines 282-283) — handled by reactive
   - REPLACE with: `docs <- docs_reactive()`

   Before:
   ```r
   output$index_action_ui <- renderUI({
     nb_id <- notebook_id()
     req(nb_id)
     docs <- list_documents(con(), nb_id)
     ...
   })
   ```

   After:
   ```r
   output$index_action_ui <- renderUI({
     docs <- docs_reactive()
     nb_id <- notebook_id()
     ...
   })
   ```

   NOTE: `nb_id` may be needed later in this renderUI for `get_notebook_ragnar_path(nb_id)`. Keep it but get it from `notebook_id()` without `req()`.

4. Verify that NO other renderUI or output block in mod_document_notebook.R calls `list_documents()` directly. Search for all occurrences and ensure they either go through `docs_reactive()` or are in a different context (e.g., a one-off call in an observeEvent handler is fine — those don't benefit from caching).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests/testthat')"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_document_notebook.R contains `docs_reactive <- reactive({` with `doc_refresh()`, `notebook_id()`, `req(nb_id)`, and `list_documents(con(), nb_id)` inside
    - R/mod_document_notebook.R output$document_list renderUI calls `docs_reactive()` instead of `list_documents(con(), nb_id)` directly
    - R/mod_document_notebook.R output$index_action_ui renderUI calls `docs_reactive()` instead of `list_documents(con(), nb_id)` directly
    - R/mod_document_notebook.R output$document_list renderUI does NOT contain a direct `doc_refresh()` call (dependency inherited through reactive chain)
    - grep for "list_documents" in output$document_list and output$index_action_ui renderUI blocks returns zero matches (only docs_reactive)
    - Comment "LIFE-03: Cache list_documents" is present above the reactive definition
    - All existing tests pass
  </acceptance_criteria>
  <done>list_documents() cached in a single reactive() expression, both renderUI blocks consume the cached result, DB queries reduced from 2 to 1 per invalidation cycle</done>
</task>

</tasks>

<verification>
1. All existing tests pass: `testthat::test_dir("tests/testthat")`
2. Shiny smoke test: app starts without errors on port 3838
3. Code audit confirms:
   - mod_slides.R chip lapply is at module body level (LIFE-01)
   - mod_document_notebook.R destroy loop runs before fig_refresh increment (LIFE-02)
   - mod_document_notebook.R has docs_reactive() replacing direct list_documents() calls (LIFE-03)
</verification>

<success_criteria>
- LIFE-01: Chip handler registration verified as accumulation-free (single registration at module init)
- LIFE-02: Figure action observer destroy loop hardened with tryCatch, ordering verified with comments
- LIFE-03: list_documents() DB query runs exactly once per doc_refresh() cycle via reactive() caching
- All existing tests green
- App starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/65-observer-lifecycle/65-01-SUMMARY.md`
</output>
