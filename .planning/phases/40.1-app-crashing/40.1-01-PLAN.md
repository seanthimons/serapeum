---
phase: 40.1-app-crashing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_bulk_import.R
  - R/bulk_import.R
  - R/api_openalex.R
  - R/mod_seed_discovery.R
autonomous: true
requirements: [HOTFIX]
must_haves:
  truths:
    - "Clicking 'Seed Network' from bulk import does not crash the app"
    - "Bulk import progress polling does not crash when progress file is empty"
    - "DOI lookup falls back to Work ID when DOI lookup returns no result"
  artifacts:
    - path: "R/mod_bulk_import.R"
      provides: "Corrected SQL query for import_run_items table"
      contains: "import_run_items"
    - path: "R/bulk_import.R"
      provides: "Defensive guard for empty progress file"
      contains: "length(line) == 0"
    - path: "R/api_openalex.R"
      provides: "Paper fetch with Work ID fallback"
      contains: "fetch_paper_with_fallback"
  key_links:
    - from: "R/mod_bulk_import.R"
      to: "R/db.R"
      via: "SQL query using correct table/column names"
      pattern: "import_run_items.*run_id"
    - from: "R/mod_seed_discovery.R"
      to: "R/api_openalex.R"
      via: "fetch_paper_with_fallback call"
      pattern: "fetch_paper_with_fallback"
---

<objective>
Fix three crash/failure bugs: (1) seed citation network crash from wrong table/column names, (2) bulk import progress crash from empty file race condition, (3) DOI lookup failures for papers with mismatched DOIs.

Purpose: Restore core workflows that are currently broken — users cannot seed networks from imports or reliably look up papers.
Output: Three bug fixes in existing files, no new files.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40.1-app-crashing/40.1-CONTEXT.md
@.planning/phases/40.1-app-crashing/40.1-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs -->

From R/db.R:224 (import_run_items schema):
```sql
CREATE TABLE IF NOT EXISTS import_run_items (
  id VARCHAR PRIMARY KEY,
  run_id VARCHAR NOT NULL,        -- FK to import_runs.id (NOT import_run_id)
  doi VARCHAR NOT NULL,           -- bare DOI (no work_id column exists)
  status VARCHAR NOT NULL,
  error_reason VARCHAR,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

From R/api_openalex.R:491 (get_paper function):
```r
get_paper <- function(paper_id, email, api_key = NULL)
# Accepts DOI URL (https://doi.org/...) or Work ID (W2741809807)
# Returns parsed work or NULL
```

From R/bulk_import.R:122 (read_import_progress):
```r
read_import_progress <- function(progress_file)
# Returns list(batch, total_batches, found, failed, message, pct)
# Currently crashes when readLines returns character(0) on empty file
```

From R/mod_seed_discovery.R:74 (seed paper lookup):
```r
paper <- tryCatch({
  get_paper(doi, email, api_key)  # Uses DOI only — no fallback
}, error = function(e) { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix seed network crash and bulk import progress race condition</name>
  <files>R/mod_bulk_import.R, R/bulk_import.R</files>
  <action>
**Fix 1: Seed network table/column names (mod_bulk_import.R:818)**

Replace the broken SQL query at line 818:
```r
# WRONG:
imported <- dbGetQuery(con_val, "SELECT work_id FROM import_items WHERE import_run_id = ? AND status = 'imported'", list(run_id))
```

With corrected query using proper table name `import_run_items`, FK column `run_id`, and joining to `abstracts` to get `paper_id` (Work ID):
```r
imported <- dbGetQuery(con_val, "
  SELECT a.paper_id
  FROM import_run_items i
  JOIN abstracts a ON i.doi = a.doi
  WHERE i.run_id = ? AND i.status = 'imported'
", list(run_id))
```

Also update line 824 to use `imported$paper_id` instead of `imported$work_id`:
```r
network_seed_request(list(
  seed_ids = imported$paper_id,
  ...
))
```

**Fix 2: Progress file race condition (bulk_import.R:127-129)**

After the `readLines` tryCatch at line 127-128, add a guard before the `strsplit` at line 129:
```r
# After readLines tryCatch, before strsplit:
if (length(line) == 0 || nchar(line[1]) == 0) {
  return(default)
}
```

This prevents the crash when `readLines` returns `character(0)` on an empty file written by a mirai worker.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/bulk_import.R'); cat('read_import_progress loaded OK\n'); source('R/mod_bulk_import.R'); cat('mod_bulk_import loaded OK\n')"</automated>
  </verify>
  <done>
- `import_run_items` table name used (not `import_items`)
- `run_id` column used (not `import_run_id`)
- `paper_id` from abstracts join used (not `work_id`)
- `read_import_progress()` returns default when file is empty instead of crashing
  </done>
</task>

<task type="auto">
  <name>Task 2: Add DOI-to-Work-ID fallback for paper lookups</name>
  <files>R/api_openalex.R, R/mod_seed_discovery.R</files>
  <action>
**Step 1: Create fallback function in api_openalex.R**

Add `fetch_paper_with_fallback()` after the existing `get_paper()` function (after line 514):

```r
#' Fetch paper with DOI-to-Work-ID fallback
#'
#' Tries DOI lookup first. If it fails, silently retries with the
#' OpenAlex Work ID. This handles cases where display DOIs differ
#' from canonical DOIs in OpenAlex.
#'
#' @param doi Optional DOI string
#' @param paper_id Optional OpenAlex Work ID (e.g., "W2626778328")
#' @param email User email for OpenAlex API
#' @param api_key Optional API key
#' @return Parsed work list, or NULL if both fail
fetch_paper_with_fallback <- function(doi = NULL, paper_id = NULL, email, api_key = NULL) {
  # Try DOI first if provided
  if (!is.null(doi) && nchar(doi) > 0) {
    result <- tryCatch(
      get_paper(doi, email, api_key),
      error = function(e) NULL
    )
    if (!is.null(result)) return(result)
  }

  # Silently fallback to Work ID
  if (!is.null(paper_id) && nchar(paper_id) > 0) {
    result <- tryCatch(
      get_paper(paper_id, email, api_key),
      error = function(e) NULL
    )
    if (!is.null(result)) return(result)
  }

  NULL
}
```

**Step 2: Update seed discovery to use fallback (mod_seed_discovery.R)**

In the pre-fill DOI handler (line 72-81), update the paper fetch to also query the DB for the paper_id (Work ID) and use the fallback:

Change the lookup at line 73-81 from:
```r
paper <- tryCatch({
  get_paper(doi, email, api_key)
}, error = function(e) { ... })
```

To:
```r
# Try to find Work ID in DB for fallback
db_paper <- tryCatch(
  DBI::dbGetQuery(con(), "SELECT paper_id FROM abstracts WHERE doi = ? LIMIT 1", list(doi)),
  error = function(e) data.frame()
)
work_id <- if (nrow(db_paper) > 0) db_paper$paper_id[1] else NULL

paper <- tryCatch({
  fetch_paper_with_fallback(doi = doi, paper_id = work_id, email = email, api_key = api_key)
}, error = function(e) {
  showNotification(paste("Error fetching paper:", e$message), type = "error", duration = 5)
  NULL
})
```

Also update the manual lookup button handler (around line 97+) with the same fallback pattern.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/api_openalex.R'); stopifnot(is.function(fetch_paper_with_fallback)); cat('fetch_paper_with_fallback exists\n')"</automated>
  </verify>
  <done>
- `fetch_paper_with_fallback()` function exists in api_openalex.R
- Seed discovery pre-fill handler uses fallback with DB-sourced Work ID
- Manual lookup button handler also uses fallback
- User never sees "paper not found" for papers that exist in OpenAlex with mismatched DOIs
  </done>
</task>

</tasks>

<verification>
1. Source R/mod_bulk_import.R — no errors, SQL references `import_run_items` and `run_id`
2. Source R/bulk_import.R — `read_import_progress()` handles empty file gracefully
3. Source R/api_openalex.R — `fetch_paper_with_fallback()` is exported and callable
4. Source R/mod_seed_discovery.R — pre-fill DOI handler uses fallback function
</verification>

<success_criteria>
- All three crash bugs fixed: seed network SQL, progress file race condition, DOI lookup failure
- No new dependencies introduced
- Existing tests still pass (if any touch these functions)
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-app-crashing/40.1-01-SUMMARY.md`
</output>
