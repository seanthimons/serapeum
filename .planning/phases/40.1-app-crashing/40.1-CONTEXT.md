# Phase 40.1: App Crashing - Context

**Gathered:** 2026-03-01
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix crashes and broken functionality that prevent core workflows from completing. Covers: seed citation network crash, bulk import crash, DOI lookup failures, physics toggle (non-functional + bad layout), and imported papers UI duplication.

</domain>

<decisions>
## Implementation Decisions

### Seed Citation Network Crash
- Table name typo: `import_items` → `import_run_items` in `mod_bulk_import.R:818`
- Column name fixes: `import_run_id` → `run_id`, `work_id` → needs correct column from schema (table has `doi`, not `work_id`)

### Bulk Import Progress Crash
- `read_import_progress()` in `bulk_import.R:129` crashes when progress file exists but is empty (race condition)
- `readLines` returns `character(0)` → `strsplit(character(0))[[1]]` → subscript out of bounds
- Fix: guard against empty/zero-length readLines result, return default progress

### DOI Lookup Failures
- Some OpenAlex papers have mismatched DOIs (display DOI vs canonical DOI in `ids.doi`)
- Example: "Attention Is All You Need" — display DOI `10.65215/2q58a426` can't be looked up via OpenAlex API, but Work ID `W2626778328` works
- Fix: use OpenAlex Work ID for re-lookups instead of DOI
- When DOI lookup fails, silently retry with stored Work ID — seamless to user

### Physics Toggle — Layout
- Move physics toggle OUT of the build button row
- Place it in the legend panel (collapsible overlay on the graph), at the bottom of the legend body
- Change from checkbox to bslib toggle switch
- Row 1 of controls becomes: Direction, Depth, Node Cap, Build Network, Save Network (no physics)
- Year filter row stays as-is (no physics there either)

### Physics Toggle — Functionality
- Currently `observeEvent(input$physics_enabled, ...)` fires but may not be wired correctly to visNetwork
- Ensure the toggle actually enables/disables the physics simulation on the rendered graph

### Imported Papers UI Duplication
- Imported papers section appears at both top and bottom of abstract notebook
- Fix: show at top only, remove duplicate rendering at bottom

### Claude's Discretion
- Exact implementation of Work ID fallback strategy (store during network build, use during re-lookup)
- How to wire physics toggle in legend panel to visNetwork proxy
- Whether to add defensive guards elsewhere for similar table name issues

</decisions>

<specifics>
## Specific Ideas

- Physics toggle in legend should use bslib toggle switch, not checkbox
- DOI fallback should be seamless — user should never see "paper not found" for papers that are in OpenAlex

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 40.1-app-crashing*
*Context gathered: 2026-03-01*
