---
status: resolved
phase: 40.1-app-crashing
source: [40.1-01-SUMMARY.md, 40.1-02-SUMMARY.md]
started: 2026-03-02T04:15:00Z
updated: 2026-03-02T11:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Seed Network from Bulk Import
expected: From the Bulk Import section, select a completed import run and click "Seed Network". The app should NOT crash. It should switch to the Citation Network view with the imported papers loaded as seeds.
result: issue
reported: "fail. Modal says: No papers imported yet"
severity: major

### 2. Bulk Import Progress Polling
expected: Start a new bulk import. While it's running, the progress indicator should update smoothly without crashing — even if you start it immediately (when the progress file may be empty).
result: pass

### 3. DOI Lookup with Mismatched DOI
expected: Find a paper whose display DOI differs from its canonical OpenAlex DOI (e.g., "Attention Is All You Need"). Try to look it up via the seed discovery panel. The lookup should succeed silently — no "paper not found" error.
result: issue
reported: "fail. Paper not found, same error."
severity: major

### 4. Physics Toggle Location
expected: Navigate to the Citation Network view. The build button row should contain ONLY Build Network and Save Network buttons (no physics checkbox). Open the Legend panel — a "Physics Simulation" toggle switch should appear at the bottom.
result: pass

### 5. Physics Toggle Functionality
expected: Build a citation network. Toggle the "Physics Simulation" switch OFF in the legend panel — nodes should freeze in place. Toggle it back ON — nodes should start moving/rebalancing.
result: pass

### 6. Button Layout Alignment
expected: In the Citation Network view, the Build Network and Save Network buttons should be horizontally balanced and vertically aligned — no visual misalignment.
result: issue
reported: "fail. Still same error."
severity: cosmetic

### 7. No Duplicate Paper Found Notification
expected: From a search notebook, click "Use as Seed" on a paper. The seed discovery panel should load the paper silently (no notification). Then manually click "Look Up" — you should see exactly ONE "Paper found" notification, not two.
result: pass
note: "tentative pass"

### 8. No Duplicate Seed Network Notification
expected: After loading seeds, switch to the Citation Network view. You should NOT see a "Seeding network with X papers" notification — the view switch itself is the feedback. After clicking "Build Network", you should see ONE "Network built" notification.
result: pass

## Summary

total: 8
passed: 5
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "Seed Network from Bulk Import switches to Citation Network with imported papers as seeds"
  status: resolved
  reason: "User reported: fail. modal says no papers uploaded yet when clicking seed citation network."
  severity: major
  test: 1
  root_cause: "SQL query uses status = 'imported' but actual status value is 'success' — zero rows returned, triggering 'no papers' modal"
  artifacts:
    - path: "R/mod_bulk_import.R"
      issue: "Line 822: WHERE i.status = 'imported' should be i.status = 'success'"
    - path: "R/db.R"
      issue: "Line 1740: documents valid statuses as success/not_found/api_error/malformed/duplicate"
  missing:
    - "Change i.status = 'imported' to i.status = 'success' at R/mod_bulk_import.R:822"

- truth: "DOI lookup falls back to Work ID when DOI lookup returns no result"
  status: resolved
  reason: "User reported: fail. 10.65215/2q58a426 still does not resolve."
  severity: major
  test: 3
  root_cause: "DB query uses URL-format DOI (https://doi.org/...) but abstracts table stores bare DOI (10.xxx/yyy) — Work ID fallback lookup always returns 0 rows"
  artifacts:
    - path: "R/mod_seed_discovery.R"
      issue: "Lines 75 and 138: DB query uses url-format doi variable but table stores bare format"
    - path: "R/utils_doi.R"
      issue: "normalize_doi_bare() exists but is not called before DB query"
  missing:
    - "Use normalize_doi_bare(doi) in DB query at R/mod_seed_discovery.R lines 75 and 138"

- truth: "Build Network and Save Network buttons are horizontally balanced and vertically aligned"
  status: resolved
  reason: "User reported: buttons not vertically centered with controls row, sit too high, large gap between them"
  severity: cosmetic
  test: 6
  root_cause: "d-flex align-items-center applied inside each column, but layout_columns grid cells are not vertically aligned — buttons align to top of their grid cell"
  artifacts:
    - path: "R/mod_citation_network.R"
      issue: "Lines 62-81: flexbox on inner divs doesn't align columns in the grid row"
  missing:
    - "Apply d-flex align-items-end h-100 to button column containers, or add CSS for .citation-network-controls column vertical alignment"

- truth: "Use as Seed loads paper silently without duplicate notification"
  status: resolved
  reason: "Tentative pass on re-test"
  severity: major
  test: 7
