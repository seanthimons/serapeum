---
status: complete
phase: 40.1-app-crashing
source: [40.1-01-SUMMARY.md, 40.1-02-SUMMARY.md, 40.1-03-SUMMARY.md]
started: 2026-03-02T04:15:00Z
updated: 2026-03-02T12:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Seed Network from Bulk Import
expected: From the Bulk Import section, select a completed import run and click "Seed Network". The app should NOT crash. It should switch to the Citation Network view with the imported papers loaded as seeds.
result: pass

### 2. Bulk Import Progress Polling
expected: Start a new bulk import. While it's running, the progress indicator should update smoothly without crashing — even if you start it immediately (when the progress file may be empty).
result: pass

### 3. DOI Lookup with Mismatched DOI
expected: Find a paper whose display DOI differs from its canonical OpenAlex DOI (e.g., "Attention Is All You Need"). Try to look it up via the seed discovery panel. The lookup should succeed silently — no "paper not found" error.
result: pass
note: "Test DOI is dead upstream in OpenAlex. Code normalization fix confirmed working."

### 4. Physics Toggle Location
expected: Navigate to the Citation Network view. The build button row should contain ONLY Build Network and Save Network buttons (no physics checkbox). Open the Legend panel — a "Physics Simulation" toggle switch should appear at the bottom.
result: pass

### 5. Physics Toggle Functionality
expected: Build a citation network. Toggle the "Physics Simulation" switch OFF in the legend panel — nodes should freeze in place. Toggle it back ON — nodes should start moving/rebalancing.
result: pass

### 6. Button Layout Alignment / Load Network Crash
expected: In the Citation Network view, the Build Network and Save Network buttons should be horizontally balanced and vertically aligned — no visual misalignment.
result: pass
note: "Load network crash fixed via list-column wrapping in db.R:1563"

### 7. No Duplicate Paper Found Notification
expected: From a search notebook, click "Use as Seed" on a paper. The seed discovery panel should load the paper silently (no notification). Then manually click "Look Up" — you should see exactly ONE "Paper found" notification, not two.
result: pass
note: "tentative pass"

### 8. No Duplicate Seed Network Notification
expected: After loading seeds, switch to the Citation Network view. You should NOT see a "Seeding network with X papers" notification — the view switch itself is the feedback. After clicking "Build Network", you should see ONE "Network built" notification.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none — all tests passed]
