---
phase: 40.1-app-crashing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_citation_network.R
  - R/mod_bulk_import.R
autonomous: false
requirements: [HOTFIX]
must_haves:
  truths:
    - "Physics toggle appears in the legend panel as a bslib switch, not in the build button row"
    - "Physics toggle actually freezes/unfreezes nodes on the rendered graph"
    - "Imported papers section appears only once in the abstract notebook (not duplicated)"
  artifacts:
    - path: "R/mod_citation_network.R"
      provides: "Physics toggle in legend panel using input_switch"
      contains: "input_switch"
  key_links:
    - from: "R/mod_citation_network.R (legend UI)"
      to: "R/mod_citation_network.R (physics observer)"
      via: "input$physics_enabled wired to visNetworkProxy"
      pattern: "visPhysics.*physics_enabled"
---

<objective>
Fix UI issues: (1) relocate physics toggle from build button row to legend panel, change to bslib switch, verify it works; (2) find and remove duplicate imported papers section in abstract notebook.

Purpose: Clean up layout (physics clutters build row) and eliminate confusing UI duplication.
Output: Modified citation network module and potentially search notebook module.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40.1-app-crashing/40.1-CONTEXT.md
@.planning/phases/40.1-app-crashing/40.1-RESEARCH.md

<interfaces>
<!-- Current citation network UI structure (mod_citation_network.R) -->

Lines 62-75: Build button row (physics toggle currently here):
```r
div(
  actionButton(ns("build_network"), "Build Network", ...),
  checkboxInput(ns("physics_enabled"), tags$span("Physics", ...), value = TRUE)
),
actionButton(ns("save_network"), "Save Network", ...)
```

Lines 99-159: Legend panel structure:
```r
div(
  class = "citation-network-legend",
  id = ns("legend_panel"),
  div(class = "legend-header", ...),    # Toggle collapse
  div(
    class = "legend-body",
    selectInput(ns("palette"), ...),     # Palette selector
    div(... "Publication Year" ...),     # Color gradient
    div(... "Citation Count" ...),       # Size legend
    div(                                 # Shape legend
      icon("star") = Seed,
      icon("diamond") = Overlap,
      icon("circle") = Regular
    )
  )
)
```

Lines 753-758: Physics observer (already correct, just needs the input to exist):
```r
observeEvent(input$physics_enabled, {
  req(current_network_data())
  visNetwork::visNetworkProxy("network_graph") |>
    visNetwork::visPhysics(enabled = input$physics_enabled)
}, ignoreInit = TRUE)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate physics toggle to legend panel and fix imported papers duplication</name>
  <files>R/mod_citation_network.R, R/mod_bulk_import.R</files>
  <action>
**Fix 1: Move physics toggle from build row to legend panel**

In the UI function `citation_network_ui()`:

1. **Remove** the `checkboxInput` for physics from the build button row (lines 70-74). The build row div should only contain the Build Network button. Keep the Save Network button where it is.

The result should be:
```r
div(
  actionButton(ns("build_network"), "Build Network", class = "btn-primary", icon = icon("diagram-project"))
),
actionButton(ns("save_network"), "Save Network", class = "btn-outline-success", icon = icon("save"))
```

2. **Add** a bslib `input_switch()` at the bottom of the legend body div (after the shape legend, before the closing `)`of `div(class = "legend-body", ...)`). Insert after the shape legend div (after line 158, before the closing `)` on line 159):

```r
tags$hr(),
bslib::input_switch(
  ns("physics_enabled"),
  "Physics Simulation",
  value = TRUE
)
```

Server observer already watches `input$physics_enabled` (line 754) and wires to `visNetworkProxy("network_graph") |> visPhysics(enabled = ...)` — no changes needed to wiring after relocating the UI element. The `input_switch` produces the same TRUE/FALSE values as `checkboxInput`, so the observer works identically.

**Fix 2: Investigate and fix imported papers duplication**

The imported papers section is rendered via `R/mod_bulk_import.R`. The UI function (`mod_bulk_import_ui`) is called once at `R/mod_search_notebook.R:335` and contains `uiOutput(ns("import_history"))` at line 9. The server-side `output$import_history <- renderUI({...})` is at `R/mod_bulk_import.R:833`.

Steps:
1. Check `R/mod_bulk_import.R` for any duplicate `uiOutput` or `renderUI` calls related to import history
2. Check `R/mod_search_notebook.R` for any separate import history rendering outside the bulk_import module call
3. If `renderUI` for import_history produces content that appears in an unexpected location due to CSS/layout, fix the layout
4. If a separate rendering path exists, remove the duplicate instance (keep the one inside `mod_bulk_import`)

The duplication may be caused by the `renderUI` at line 833 producing output that appears at both top and bottom due to layout, or by a separate rendering path. The executor should investigate the actual DOM to find the true source.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/mod_citation_network.R'); cat('citation network module loads OK\n')"</automated>
  </verify>
  <done>
- Physics toggle removed from build button row
- Physics toggle appears as bslib input_switch in legend panel body
- Physics observer still wired correctly (unchanged server code)
- Build row contains only: Direction, Depth, Node Cap, Build Network, Save Network
- Imported papers section appears only once in the abstract notebook
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify physics toggle and UI fixes visually</name>
  <what-built>Physics toggle relocated to legend panel as bslib switch. Imported papers duplication investigated and fixed. All crash fixes from Plan 01 also in effect.</what-built>
  <how-to-verify>
    1. Start the app: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R')"`
    2. Navigate to Citation Network view
    3. Verify the build button row has NO physics checkbox — just Build Network and Save Network
    4. Open the legend panel (click Legend header)
    5. Verify "Physics Simulation" toggle switch appears at the bottom of the legend
    6. Build a network (seed from any notebook)
    7. Toggle the physics switch OFF — nodes should freeze in place
    8. Toggle the physics switch ON — nodes should start moving/rebalancing
    9. Navigate to a search notebook with imported papers
    10. Verify the imported papers section appears only once (at the top, not duplicated at bottom)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Source R/mod_citation_network.R — no errors
2. Visual: physics toggle in legend panel, not build row
3. Functional: toggle freezes/unfreezes network nodes
4. Visual: imported papers section not duplicated
</verification>

<success_criteria>
- Physics toggle in legend panel as bslib switch
- Toggle actually controls physics simulation on rendered graph
- No UI duplication of imported papers section
- Build button row is clean (no physics checkbox)
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-app-crashing/40.1-02-SUMMARY.md`
</output>
