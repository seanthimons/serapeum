---
phase: 67-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 67-01
files_modified:
  - tests/testthat/test-db-migrations.R
  - tests/testthat/test-db.R
  - .planning/phases/67-infrastructure/67-VALIDATION.md
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "A brand-new DuckDB database can initialize through get_db_connection() with the real migration set and no SQL errors"
    - "Running the same startup path a second time against the initialized database does not fail on duplicate tables, indexes, or columns"
    - "The migration audit finding is captured in executable regression coverage rather than only in comments"
  artifacts:
    - path: "tests/testthat/test-db.R"
      provides: "Fresh-install startup-path regression using get_db_connection()"
      contains: "get_db_connection"
    - path: "tests/testthat/test-db-migrations.R"
      provides: "Migration audit and rerun safety assertions"
      contains: "run_pending_migrations"
    - path: ".planning/phases/67-infrastructure/67-VALIDATION.md"
      provides: "Updated validation strategy after execution evidence"
      contains: "nyquist_compliant"
---

<objective>
Prove the Phase 67 migration audit actually protects fresh installs by adding regression coverage around the real startup path and rerun behavior.

Purpose: Turn migration hygiene into an executable contract that prevents the same class of startup failures from returning.
Output: Focused DB tests that exercise clean startup plus rerun and document the audit outcome in the validation artifact.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-infrastructure/67-CONTEXT.md
@.planning/phases/67-infrastructure/67-RESEARCH.md
@.planning/phases/67-infrastructure/67-VALIDATION.md
@.planning/phases/67-infrastructure/67-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add real fresh-install and rerun regression coverage</name>
  <files>tests/testthat/test-db.R, tests/testthat/test-db-migrations.R</files>
  <read_first>
    tests/testthat/helper-source.R
    tests/testthat/test-db.R
    tests/testthat/test-db-migrations.R
    R/db.R
    R/db_migrations.R
    migrations/005_add_doi_column.sql
    migrations/006_create_citation_networks.sql
    migrations/008_add_document_metadata.sql
    migrations/012_add_duration_ms_to_cost_log.sql
    migrations/018_create_prompt_versions.sql
  </read_first>
  <action>
Add or extend tests so the repo proves the real startup path is safe on a blank database and on rerun. Implement these exact checks:

1. In `tests/testthat/test-db.R`, add a test that creates a temp DuckDB file path, calls `get_db_connection(db_path)`, asserts success, and verifies representative audited objects exist:
   - `abstracts.doi`
   - `documents.title`, `documents.authors`, `documents.year`, `documents.doi`, `documents.abstract_id`
   - `cost_log.duration_ms`
   - `citation_networks`, `network_nodes`, `network_edges`
   - `prompt_versions`
2. In the same file or paired test, reopen the same `db_path` with `get_db_connection(db_path)` and assert the second call completes without error and the representative objects still exist.
3. In `tests/testthat/test-db-migrations.R`, add focused assertions that the startup/rerun path does not create duplicate rows in `schema_migrations`.
4. Prefer concrete `information_schema` checks or `dbListTables()` over vague "did not error" assertions.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-db-migrations.R'); testthat::test_file('tests/testthat/test-db.R')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-db.R contains a test that calls `get_db_connection(` on a temp DB path
    - that test checks for `schema_migrations` entries after startup
    - that test checks that `citation_networks`, `network_nodes`, `network_edges`, and `prompt_versions` exist
    - that test checks that `documents` has `title`, `authors`, `year`, `doi`, and `abstract_id`
    - that test checks that `cost_log` has `duration_ms`
    - the same DB path is opened a second time and startup succeeds again
    - tests/testthat/test-db-migrations.R asserts rerun does not duplicate applied migration records
    - the targeted DB test command exits successfully
  </acceptance_criteria>
  <done>The test suite proves clean startup plus rerun behavior for the real migration set.</done>
</task>

<task type="auto">
  <name>Task 2: Record the audit outcome in validation artifacts</name>
  <files>.planning/phases/67-infrastructure/67-VALIDATION.md</files>
  <read_first>
    .planning/phases/67-infrastructure/67-VALIDATION.md
    .planning/phases/67-infrastructure/67-RESEARCH.md
    tests/testthat/test-db.R
    tests/testthat/test-db-migrations.R
  </read_first>
  <action>
Update `67-VALIDATION.md` after the regression tests are in place so it reflects the real evidence collected during execution. Apply these exact target states once the tests pass:

1. Mark the Wave 1 and Wave 2 verification rows green or completed with the final command(s) used.
2. Set `wave_0_complete: true`.
3. Set `nyquist_compliant: true` if the targeted DB tests cover both clean startup and rerun behavior with executable checks.
4. Keep the validation artifact focused on concrete commands and outcomes.
  </action>
  <verify>
    <automated>Get-Content .planning/phases/67-infrastructure/67-VALIDATION.md | Select-String -Pattern "nyquist_compliant: true|wave_0_complete: true"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/67-infrastructure/67-VALIDATION.md contains `wave_0_complete: true`
    - .planning/phases/67-infrastructure/67-VALIDATION.md contains `nyquist_compliant: true`
    - validation rows reference the DB test commands actually used during execution
  </acceptance_criteria>
  <done>The validation artifact reflects executable proof of the migration audit outcome.</done>
</task>
</tasks>
