---
phase: 44-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-db-leak.R
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
must_haves:
  truths:
    - "search_chunks_hybrid closes self-opened ragnar stores via on.exit()"
    - "Dead code (with_ragnar_store, register_ragnar_cleanup) does not exist in codebase"
    - "Connection leak detection test exists and passes in CI"
  artifacts:
    - path: "tests/testthat/test-db-leak.R"
      provides: "Connection leak detection tests for search_chunks_hybrid"
      min_lines: 30
    - path: "R/db.R"
      provides: "search_chunks_hybrid with own_store cleanup pattern"
      contains: "own_store"
  key_links:
    - from: "tests/testthat/test-db-leak.R"
      to: "R/db.R"
      via: "tests call search_chunks_hybrid and verify cleanup"
      pattern: "search_chunks_hybrid"
---

<objective>
Validate existing DEBT-01/DEBT-02 fixes and add missing connection leak detection test.

Purpose: DEBT-01 (connection leak in search_chunks_hybrid) and DEBT-02 (dead code removal) were already implemented in Phase 25 (commit 9c16371, 2026-02-18). This plan adds the missing automated test coverage for connection lifecycle validation and confirms dead code stays removed — satisfying the Phase 44 success criteria before increasing rendering load in later phases.

Output: `tests/testthat/test-db-leak.R` with connection lifecycle tests
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tech-debt-cleanup/44-RESEARCH.md

@R/db.R
@R/_ragnar.R
@tests/testthat/test-db.R
@tests/testthat/test-ragnar.R

<interfaces>
<!-- Key functions and patterns the executor needs -->

From R/db.R (lines 790-821):
```r
search_chunks_hybrid <- function(con, query, notebook_id = NULL, limit = 5,
                                  ragnar_store = NULL,
                                  ragnar_store_path = NULL,
                                  api_key = NULL, embed_model = NULL) {
  # ...
  own_store <- is.null(ragnar_store)
  store <- ragnar_store %||% connect_ragnar_store(ragnar_store_path)

  if (!is.null(store) && own_store) {
    on.exit(
      tryCatch(DBI::dbDisconnect(store@con, shutdown = TRUE), error = function(e) NULL),
      add = TRUE
    )
  }
  # ...
}
```

From R/_ragnar.R:
```r
connect_ragnar_store <- function(path)
# Returns a ragnar store object with @con slot (DuckDB connection)

get_notebook_ragnar_path <- function(notebook_id)
# Returns path string for per-notebook ragnar store
```

From R/db.R:
```r
get_db_connection <- function(db_path)
close_db_connection <- function(con)
init_schema <- function(con)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create connection leak detection test</name>
  <files>tests/testthat/test-db-leak.R</files>
  <action>
Create `tests/testthat/test-db-leak.R` with the following tests:

1. **Test: search_chunks_hybrid closes self-opened stores (DEBT-01)**
   - Create a temp DuckDB database with `get_db_connection()` and `init_schema()`
   - Create a temp ragnar store path using `tempfile(fileext = ".ragnar.duckdb")`
   - If ragnar is available (`ragnar_available()`), create a minimal ragnar store at that path using `get_ragnar_store()` or `ragnar::ragnar_store()` so the file exists
   - Call `search_chunks_hybrid(con, "test query", ragnar_store_path = store_path)` — this should internally open AND close the store
   - After the call returns, verify the store connection was closed. Strategy: attempt to connect to the same ragnar store file again — if the previous connection was properly closed with `shutdown = TRUE`, this should succeed. If leaked, DuckDB will error with "database is locked" or similar.
   - Use `skip_if_not(ragnar_available(), "ragnar not installed")` at top
   - Clean up with `on.exit()` for both temp db and temp store path

2. **Test: search_chunks_hybrid does NOT close caller-provided stores (DEBT-01 ownership)**
   - Create a ragnar store explicitly
   - Pass it as `ragnar_store` parameter to `search_chunks_hybrid()`
   - After call returns, verify the store connection is STILL open (caller retains ownership)
   - The caller then closes it explicitly
   - Use `skip_if_not(ragnar_available(), "ragnar not installed")`

3. **Test: dead code is absent from codebase (DEBT-02)**
   - Use `list.files("R/", pattern = "\\.R$", full.names = TRUE)` to get all R source files
   - Read each file with `readLines()` and search for `with_ragnar_store` and `register_ragnar_cleanup`
   - `expect_equal(length(matches), 0)` for each dead function name
   - This test does NOT need ragnar — no skip_if_not needed

Follow project test conventions:
- Source required files using the project_root pattern from test-db.R
- Use `on.exit()` for cleanup
- Descriptive test names
- One logical assertion per test
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-db-leak.R')"</automated>
  </verify>
  <done>
    - test-db-leak.R exists with 3 tests covering DEBT-01 (self-close, ownership) and DEBT-02 (dead code absent)
    - All tests pass (ragnar tests may skip if ragnar not available, but dead code test always runs)
    - Dead code test confirms zero matches for with_ragnar_store and register_ragnar_cleanup in R/ directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify fixes and run full test suite</name>
  <files>tests/testthat/test-db-leak.R</files>
  <action>
Run the full test suite to confirm the new test file doesn't break anything:

1. Run `testthat::test_dir('tests/testthat')` and verify no NEW failures (13 pre-existing fixture failures are known/expected per STATE.md)
2. Run `git grep "with_ragnar_store\|register_ragnar_cleanup" R/` and confirm empty output (DEBT-02 final verification)
3. If any new test in test-db-leak.R fails, debug and fix the test (not the source code — the fixes are already correct per Phase 25)

Note: Do NOT modify R/db.R or R/_ragnar.R — the implementation is already correct. Only fix the test file if tests fail due to test setup issues.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests/testthat', filter = 'db-leak')"</automated>
  </verify>
  <done>
    - Full test suite runs without new failures beyond the 13 known fixture issues
    - git grep confirms zero matches for dead code patterns in R/
    - test-db-leak.R passes all non-skipped tests
  </done>
</task>

</tasks>

<verification>
1. `tests/testthat/test-db-leak.R` exists and contains 3 test_that blocks
2. Running `testthat::test_file('tests/testthat/test-db-leak.R')` passes (ragnar-dependent tests may skip)
3. `git grep "with_ragnar_store\|register_ragnar_cleanup" R/` returns empty (DEBT-02)
4. `grep -c "own_store" R/db.R` returns at least 2 (DEBT-01 pattern present)
5. No new test failures in full suite
</verification>

<success_criteria>
- Connection leak detection test file exists and passes
- Dead code absence is verified both by test and by git grep
- DEBT-01 and DEBT-02 requirements are satisfied with automated coverage
- App remains stable (no regressions from test additions)
</success_criteria>

<output>
After completion, create `.planning/phases/44-tech-debt-cleanup/44-01-SUMMARY.md`
</output>
