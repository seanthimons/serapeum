---
phase: 53.1-import-run-dialog-appearing-at-bottom-of-abstract-notebook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_bulk_import.R
  - R/mod_search_notebook.R
  - app.R
  - tests/testthat/test-bulk_import.R
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - HIST-05
  - HIST-06
must_haves:
  truths:
    - "Import history no longer appears as unstyled content at the bottom of the abstract notebook"
    - "Import history appears as a collapsible section inside the import modal (search notebook only)"
    - "History toggle shows 'Past Imports (N)' with count badge, collapsed by default"
    - "Empty state shows 'No imports yet' message instead of hiding the section"
    - "Sidebar import shows no history UI (only JS script tag)"
    - "Delete buttons are removed from history cards"
  artifacts:
    - path: "R/mod_bulk_import.R"
      provides: "Parameterized module with show_history flag controlling history in modal footer"
      contains: "show_history = FALSE"
    - path: "R/mod_search_notebook.R"
      provides: "Caller passes show_history = TRUE to bulk import module"
      contains: "show_history = TRUE"
    - path: "app.R"
      provides: "Sidebar caller uses default show_history = FALSE"
  key_links:
    - from: "R/mod_bulk_import.R"
      to: "showModal(modalDialog(...))"
      via: "Collapsible history section in modal footer when show_history = TRUE"
      pattern: "import_history_collapse"
    - from: "R/mod_search_notebook.R"
      to: "R/mod_bulk_import.R"
      via: "show_history = TRUE parameter in both UI and server calls"
      pattern: "show_history.*TRUE"
---

<objective>
Fix the import run history bug where history cards render as visible, unstyled content at the bottom of the abstract notebook. Move history into the import modal as a collapsible section, parameterize the module to control history visibility, and remove delete functionality.

Purpose: Eliminate a UI bug present since Phase 35 where `uiOutput(ns("import_history"))` in `mod_bulk_import_ui()` renders directly into the page flow instead of being scoped to the modal.
Output: Clean import modal with optional collapsible history footer, no page-level history rendering.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53.1-import-run-dialog-appearing-at-bottom-of-abstract-notebook/53.1-CONTEXT.md
@.planning/phases/53.1-import-run-dialog-appearing-at-bottom-of-abstract-notebook/53.1-RESEARCH.md

<interfaces>
<!-- Key code locations the executor needs -->

From R/mod_bulk_import.R (lines 3-11) — current UI function to modify:
```r
mod_bulk_import_ui <- function(id) {
  ns <- NS(id)
  tagList(
    tags$script(src = "js/import-progress.js"),
    uiOutput(ns("import_history"))  # BUG: renders in page flow
  )
}
```

From R/mod_bulk_import.R (lines 26-27) — current server signature:
```r
mod_bulk_import_server <- function(id, con, notebook_id, config, paper_refresh, db_path_r,
                                    standalone = FALSE, navigate_to_notebook = NULL) {
```

From R/mod_bulk_import.R (lines 103-132) — modal dialog with footer:
```r
showModal(modalDialog(
  title = tagList(icon_file_import(), "Bulk DOI Import"),
  notebook_selector_ui,
  tabsetPanel(...),
  textInput(ns("run_name"), ...),
  uiOutput(ns("preview_panel")),
  footer = tagList(
    modalButton("Cancel"),
    actionButton(ns("preview_btn"), "Validate", ...),
    actionButton(ns("start_import"), "Import", ...)
  ),
  size = "l",
  easyClose = FALSE
))
```

From R/mod_bulk_import.R (lines 838-896) — history render + delete handlers to modify/remove:
```r
output$import_history <- renderUI({ ... })  # Lines 838-873
observe({ ... delete handlers ... })         # Lines 877-896
```

From R/mod_bulk_import.R (line 48) — history refresh reactive:
```r
history_refresh <- reactiveVal(0)
```

Caller sites:
- R/mod_search_notebook.R:360 — `mod_bulk_import_ui(ns("bulk_import"))`
- R/mod_search_notebook.R:414 — `bulk_import_api <- mod_bulk_import_server("bulk_import", con, notebook_id, config, ...)`
- app.R:229 — `mod_bulk_import_ui("sidebar_import")`
- app.R:1069 — `sidebar_import_api <- mod_bulk_import_server(...)`

Existing collapse pattern from R/mod_search_notebook.R (lines 207-219):
```r
card_header(
  class = "d-flex justify-content-between align-items-center",
  style = "cursor: pointer;",
  `data-bs-toggle` = "collapse",
  `data-bs-target` = paste0("#", ns("keyword_filter_body")),
  span(icon_key(), " Keywords"),
  icon_chevron_down(class = "text-muted")
),
card_body(
  id = ns("keyword_filter_body"),
  class = "collapse show",
  ...
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor mod_bulk_import module with show_history parameterization</name>
  <files>R/mod_bulk_import.R</files>
  <action>
Modify `R/mod_bulk_import.R` to add `show_history` parameter and relocate history into the modal:

**1. Update `mod_bulk_import_ui()` (lines 3-11):**
- Add `show_history = FALSE` parameter to function signature
- Remove the `uiOutput(ns("import_history"))` entirely from the UI function
- Both `show_history = TRUE` and `show_history = FALSE` output only the JS script tag. History goes inside the modal, not in page-level UI.

Result:
```r
mod_bulk_import_ui <- function(id, show_history = FALSE) {
  ns <- NS(id)
  tagList(
    tags$script(src = "js/import-progress.js")
  )
}
```

**2. Update `mod_bulk_import_server()` (line 26-27):**
- Add `show_history = FALSE` parameter after `navigate_to_notebook`
- Store it for use in `show_import_modal` closure

**3. Modify `show_import_modal` function (around line 103):**
- The modal footer currently has Cancel, Validate, Import buttons
- When `show_history` is TRUE, append a collapsible history section BELOW the footer buttons. Use a `tags$div` with `style = "width: 100%; margin-top: 1rem;"` wrapping:
  - An `hr()` separator
  - A toggle div with `data-bs-toggle = "collapse"` and `data-bs-target = paste0("#", ns("import_history_collapse"))`, `style = "cursor: pointer;"`, `class = "d-flex justify-content-between align-items-center"`
  - Toggle text: render the count inline. Call `get_import_runs(con(), notebook_id())` wrapped in tryCatch. Show `tags$span("Past Imports ", tags$span(class = "badge bg-secondary", nrow(runs)))`. Use total count for the badge (not capped at 5).
  - A collapse target div with `id = ns("import_history_collapse")`, `class = "collapse"` (collapsed by default, no "show" class)
  - Inside the collapse div: render last 5 history cards (`runs <- head(runs, 5)`) most recent first, using the existing card layout pattern but WITHOUT delete buttons. Remove the `actionButton(ns(paste0("delete_run_", run$id)), ...)` from each card.
  - If no runs exist (`nrow(runs) == 0`), show: `tags$div(class = "text-muted small mt-2", "No imports yet")`

NOTE: Since `show_import_modal` is a closure defined inside `moduleServer`, it already has access to `con()`, `notebook_id()`, etc. The history rendering happens INSIDE `show_import_modal` directly (not via `renderUI`/`uiOutput`), built as part of the modal dialog content each time the modal opens.

**4. Remove the old `output$import_history` renderUI (lines 838-873):**
- Delete this entire block. History is now rendered inline in the modal.
- Keep the `history_refresh` reactiveVal (line 48) and its increment at line 598 — used for triggering refreshes after import completion.

**5. Remove delete run handlers entirely (lines 877-896):**
- Delete the entire `observe({ ... delete handlers ... })` block
- Per user decision: history is read-only, runs cleaned up on notebook delete

**Important details:**
- Use `ns()` for ALL element IDs in the collapse section to avoid namespace conflicts
- Limit displayed runs to 5: `runs <- head(runs, 5)` after fetching
- History card layout: keep `border rounded p-2 mb-2` pattern, remove delete button column
- The toggle should use `cursor: pointer` style
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/mod_bulk_import.R'); cat('Module sources OK\n')" 2>&1 | head -5</automated>
  </verify>
  <done>
- `mod_bulk_import_ui()` accepts `show_history` param; outputs only JS script tag (no uiOutput for history)
- `mod_bulk_import_server()` accepts `show_history` param
- When `show_history = TRUE`, import modal footer includes collapsible "Past Imports (N)" section with last 5 runs
- When `show_history = FALSE`, no history UI rendered anywhere
- Delete buttons and delete observers completely removed
- Empty state shows "No imports yet" message
- All element IDs properly namespaced with `ns()`
  </done>
</task>

<task type="auto">
  <name>Task 2: Update callers, add tests, and smoke test</name>
  <files>R/mod_search_notebook.R, app.R, tests/testthat/test-bulk_import.R</files>
  <action>
**1. Update R/mod_search_notebook.R:**
- Line 360: Change `mod_bulk_import_ui(ns("bulk_import"))` to `mod_bulk_import_ui(ns("bulk_import"), show_history = TRUE)`
- Line 414: Change `mod_bulk_import_server("bulk_import", ...)` to add `show_history = TRUE` parameter at the end of the call

**2. Verify app.R needs no changes:**
- Line 229: `mod_bulk_import_ui("sidebar_import")` — leave as-is (default `show_history = FALSE`)
- Line 1069: `mod_bulk_import_server(...)` — leave as-is (default `show_history = FALSE`)

**3. Add unit tests to tests/testthat/test-bulk_import.R:**
First, add a conditional source block for the Shiny module (the file currently only sources `R/bulk_import.R` for utility functions). Before the new tests, add:
```r
if (requireNamespace("shiny", quietly = TRUE)) {
  library(shiny)
  source(file.path(project_root, "R", "mod_bulk_import.R"))
}
```

Then append test cases at the end of the file:

```r
# --- show_history parameter tests ---

test_that("mod_bulk_import_ui with show_history=FALSE outputs only script tag", {
  skip_if_not_installed("shiny")
  ui <- mod_bulk_import_ui("test_import", show_history = FALSE)
  ui_html <- as.character(ui)
  expect_true(grepl("import-progress.js", ui_html))
  expect_false(grepl("import_history", ui_html))
})

test_that("mod_bulk_import_ui with show_history=TRUE outputs only script tag (history is in modal)", {
  skip_if_not_installed("shiny")
  ui <- mod_bulk_import_ui("test_import", show_history = TRUE)
  ui_html <- as.character(ui)
  expect_true(grepl("import-progress.js", ui_html))
  expect_false(grepl("import_history", ui_html))
})

test_that("mod_bulk_import_ui default show_history is FALSE", {
  skip_if_not_installed("shiny")
  ui <- mod_bulk_import_ui("test_default")
  ui_html <- as.character(ui)
  expect_true(grepl("import-progress.js", ui_html))
  expect_false(grepl("import_history", ui_html))
})
```

**4. Run Shiny smoke test:**
Start the app in background, wait for "Listening on" or error, kill process. Fix any crash immediately.

**5. Run existing tests to verify no regressions:**
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-bulk_import.R')"
```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-bulk_import.R')"</automated>
  </verify>
  <done>
- `mod_search_notebook.R` passes `show_history = TRUE` to both UI and server calls
- `app.R` sidebar import uses default `show_history = FALSE` (no change needed)
- Unit tests pass for `show_history` parameter behavior
- Shiny smoke test confirms app starts without crash
- No regressions in existing bulk import tests
  </done>
</task>

</tasks>

<verification>
1. Shiny smoke test passes (app starts without crash)
2. Unit tests in test-bulk_import.R all pass
3. Visual confirmation: import history no longer appears at bottom of abstract notebook page
4. Visual confirmation: opening import modal in search notebook shows "Past Imports (N)" collapsible section in footer
</verification>

<success_criteria>
- Import history cards no longer render as unstyled content at the bottom of the abstract notebook
- Import modal in search notebook shows collapsible "Past Imports (N)" section in footer
- Sidebar import shows no history (clean UI)
- Delete buttons removed from history cards
- Empty state shows "No imports yet" message
- App starts without errors (smoke test)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/53.1-import-run-dialog-appearing-at-bottom-of-abstract-notebook/53.1-01-SUMMARY.md`
</output>
