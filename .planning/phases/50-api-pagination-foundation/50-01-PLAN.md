---
phase: 50-api-pagination-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/api_openalex.R
  - R/mod_search_notebook.R
  - tests/testthat/test-api-openalex.R
autonomous: true
requirements:
  - PAGE-06

must_haves:
  truths:
    - "search_papers() accepts cursor and sort parameters"
    - "search_papers() returns list(papers, next_cursor, count) instead of flat list"
    - "Cursor is treated as opaque string (never parsed or decoded)"
    - "build_openalex_request() adds retry logic benefiting all API functions"
    - "Malformed API responses throw descriptive errors"
    - "Empty results return list(papers = list(), next_cursor = NULL, count = 0)"
    - "Existing caller in mod_search_notebook.R works with new return format"
  artifacts:
    - path: "R/api_openalex.R"
      provides: "Cursor-based pagination in search_papers(), global retry in build_openalex_request()"
      contains: "req_retry"
    - path: "R/mod_search_notebook.R"
      provides: "Updated caller using result$papers"
      contains: "result\\$papers"
    - path: "tests/testthat/test-api-openalex.R"
      provides: "Unit tests for pagination, cursor handling, error cases"
      contains: "next_cursor"
  key_links:
    - from: "R/api_openalex.R::search_papers"
      to: "R/api_openalex.R::build_openalex_request"
      via: "Shared request builder provides retry to search_papers"
      pattern: "build_openalex_request.*works"
    - from: "R/mod_search_notebook.R"
      to: "R/api_openalex.R::search_papers"
      via: "Caller accesses result$papers from new return format"
      pattern: "result\\$papers"
---

<objective>
Add cursor-based pagination support to the OpenAlex API client and update the single caller.

Purpose: Enable Phase 51-52 (pagination state + Load More button) by providing cursor-aware API infrastructure.
Output: Modified `search_papers()` with cursor/sort params, structured return type, global retry, and updated caller.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-api-pagination-foundation/CONTEXT.md
@.planning/phases/50-api-pagination-foundation/50-RESEARCH.md

<interfaces>
<!-- Key functions and patterns the executor needs. Extracted from codebase. -->

From R/api_openalex.R (lines 98-110) - current build_openalex_request:
```r
build_openalex_request <- function(endpoint, email = NULL, api_key = NULL) {
  req <- request(paste0(OPENALEX_BASE_URL, "/", endpoint))
  if (!is.null(email)) {
    req <- req |> req_url_query(mailto = email)
  }
  if (!is.null(api_key) && nchar(api_key) > 0 && !grepl("^your-", api_key)) {
    req <- req |> req_url_query(api_key = api_key)
  }
  req |> req_timeout(30)
}
```

From R/api_openalex.R (lines 294-376) - current search_papers:
```r
search_papers <- function(query, email, api_key = NULL,
                          from_year = NULL, to_year = NULL, per_page = 25,
                          search_field = "default", is_oa = FALSE,
                          min_citations = NULL, exclude_retracted = TRUE,
                          work_types = NULL) {
  # ... builds filters, constructs request ...
  body <- resp_body_json(resp)
  if (is.null(body$results)) {
    return(list())
  }
  lapply(body$results, parse_openalex_work)
}
```

From R/api_openalex.R - error handling:
```r
stop_api_error(e, "OpenAlex")  # Existing error wrapper pattern
```

From R/mod_search_notebook.R (lines 2216-2243) - current caller:
```r
papers <- tryCatch({
  search_papers(
    nb$search_query, email, api_key,
    from_year = filters$from_year, to_year = filters$to_year,
    per_page = abstracts_count, search_field = filters$search_field %||% "default",
    is_oa = filters$is_oa %||% FALSE, min_citations = filters$min_citations,
    exclude_retracted = if (!is.null(filters$exclude_retracted)) filters$exclude_retracted else TRUE,
    work_types = filters$work_types
  )
}, error = function(e) { ... return(list()) })
if (length(papers) == 0) { ... }
incProgress(0.4, detail = paste("Found", length(papers), "papers"))
```

From tests/testthat/test-api-openalex.R - test helper:
```r
make_mock_work <- function(overrides = list()) {
  base <- list(id = "https://openalex.org/W99999", title = "Mock Paper",
               authorships = list(), abstract_inverted_index = NULL,
               publication_year = 2024, primary_location = NULL,
               open_access = NULL, keywords = list())
  modifyList(base, overrides)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cursor pagination and retry to OpenAlex API client</name>
  <files>R/api_openalex.R, tests/testthat/test-api-openalex.R</files>
  <behavior>
    - search_papers() accepts cursor param (default NULL) and sort param (default "relevance_score")
    - search_papers() returns list(papers, next_cursor, count) on success
    - cursor=NULL sends cursor="*" to API (initiates pagination)
    - cursor=<string> passes cursor value through unchanged (opaque)
    - sort param is passed as query parameter to API
    - Empty results return list(papers = list(), next_cursor = NULL, count = 0)
    - Missing body$meta throws "Unexpected OpenAlex response format" error
    - Missing body$results throws "Unexpected OpenAlex response format" error
    - build_openalex_request() includes req_retry(max_tries=3) with is_transient for 429/503
  </behavior>
  <action>
1. Write tests FIRST in tests/testthat/test-api-openalex.R (RED phase). Add a new section "# --- Phase 50: Cursor pagination ---" after existing tests. Since search_papers() makes HTTP calls, tests should mock at the response level or test the return structure contract. Write these test cases:
   - "search_papers returns list with papers, next_cursor, count" — mock httptest or use with_mock to verify return structure
   - "search_papers accepts cursor parameter" — verify function signature accepts cursor
   - "search_papers accepts sort parameter" — verify function signature accepts sort
   - "search_papers throws on missing meta field" — test response validation
   - "search_papers throws on missing results field" — test response validation
   - "search_papers returns empty structure when no results" — verify empty case

   NOTE: Since search_papers makes real HTTP calls, extract the response-validation and return-structure logic into a testable helper function `parse_search_response(body)` that takes the parsed JSON body and returns the structured list. This keeps tests fast and deterministic without HTTP mocking. The helper should:
   - Validate body$meta and body$results exist (throw if not)
   - Extract next_cursor from body$meta$next_cursor
   - Extract count from body$meta$count (default 0)
   - Parse papers via lapply(body$results, parse_openalex_work)
   - Return list(papers, next_cursor, count)

2. Run tests to confirm RED (they fail because parse_search_response doesn't exist yet).

3. Implement in R/api_openalex.R (GREEN phase):
   a. In build_openalex_request() (line ~109, before the final `req |> req_timeout(30)`): Add req_retry after req_timeout. Replace the final line with:
      ```r
      req |>
        req_timeout(30) |>
        req_retry(
          max_tries = 3,
          is_transient = \(resp) httr2::resp_status(resp) %in% c(429, 503),
          backoff = \(i) 2^(i - 1)
        )
      ```

   b. Create parse_search_response() helper (before search_papers):
      ```r
      parse_search_response <- function(body) {
        if (is.null(body$meta) || is.null(body$results)) {
          stop("Unexpected OpenAlex response format: missing 'meta' or 'results' field")
        }
        next_cursor <- body$meta$next_cursor
        count <- body$meta$count %||% 0L
        papers <- if (length(body$results) > 0) {
          lapply(body$results, parse_openalex_work)
        } else {
          list()
        }
        list(papers = papers, next_cursor = next_cursor, count = count)
      }
      ```

   c. Modify search_papers() signature (line 294-298): Add `cursor = NULL` and `sort = "relevance_score"` parameters after `work_types`.

   d. In search_papers() request building (around line 358-361): Add sort and cursor query params:
      ```r
      req <- req |> req_url_query(
        filter = filter_str,
        per_page = per_page,
        sort = sort,
        cursor = if (is.null(cursor)) "*" else cursor
      )
      ```

   e. Replace the return block (lines 369-376) with:
      ```r
      body <- resp_body_json(resp)
      parse_search_response(body)
      ```

4. Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-api-openalex.R')"</automated>
  </verify>
  <done>search_papers() accepts cursor/sort params, returns structured list(papers, next_cursor, count). build_openalex_request() includes retry. Response validation throws on malformed responses. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update caller in mod_search_notebook.R and smoke test</name>
  <files>R/mod_search_notebook.R</files>
  <action>
1. In R/mod_search_notebook.R around line 2216-2243, update the search_papers() caller:

   Change from:
   ```r
   papers <- tryCatch({
     search_papers(
       nb$search_query, email, api_key,
       ...
     )
   }, error = function(e) { ... return(list()) })
   ```

   To:
   ```r
   result <- tryCatch({
     search_papers(
       nb$search_query, email, api_key,
       from_year = filters$from_year,
       to_year = filters$to_year,
       per_page = abstracts_count,
       search_field = filters$search_field %||% "default",
       is_oa = filters$is_oa %||% FALSE,
       min_citations = filters$min_citations,
       exclude_retracted = if (!is.null(filters$exclude_retracted)) filters$exclude_retracted else TRUE,
       work_types = filters$work_types,
       cursor = NULL
     )
   }, error = function(e) {
     if (inherits(e, "api_error")) {
       show_error_toast(e$message, e$details, e$severity)
     } else {
       err <- classify_api_error(e, "OpenAlex")
       show_error_toast(err$message, err$details, err$severity)
     }
     return(list(papers = list(), next_cursor = NULL, count = 0))
   })
   papers <- result$papers
   ```

   IMPORTANT: The error handler must now return the structured format `list(papers = list(), next_cursor = NULL, count = 0)` instead of `list()`, so that `result$papers` works in the error path too.

2. Verify the downstream code still works: `length(papers) == 0` check on line 2240 still works because `papers` is now `result$papers` which is a list.

3. Also update the `incProgress` line (~2245) — it uses `length(papers)` which still works since `papers` is the extracted list.

4. Run Shiny smoke test per project CLAUDE.md protocol:
   ```
   "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)"
   ```
   Wait for "Listening on" or error. Kill process. Fix if it crashes.

5. Run full test suite to confirm nothing is broken.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests/testthat')"</automated>
  </verify>
  <done>Caller updated to use result$papers from new return format. Error path returns structured format. Shiny app starts without crash. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. All tests in test-api-openalex.R pass (pagination, cursor, error cases)
2. Full test suite passes with no regressions
3. Shiny app starts successfully (smoke test)
4. search_papers() signature includes cursor and sort params
5. build_openalex_request() includes req_retry
6. Caller in mod_search_notebook.R uses result$papers
</verification>

<success_criteria>
- PAGE-06 satisfied: OpenAlex cursor-based pagination in API client
- search_papers(cursor=NULL) initiates pagination with cursor="*"
- search_papers(cursor=<string>) continues pagination with provided cursor
- Return format is list(papers, next_cursor, count) for all code paths
- Retry logic in shared builder benefits all OpenAlex API functions
- Single caller updated and working
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/50-api-pagination-foundation/50-01-SUMMARY.md`
</output>
