---
phase: 54-tooltip-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_search_notebook.R
  - app.R
  - R/mod_keyword_filter.R
autonomous: false
requirements:
  - TOOL-05
must_haves:
  truths:
    - "Every toolbar button shows a descriptive tooltip on hover"
    - "Every sidebar discovery button shows a descriptive tooltip on hover"
    - "Tooltips appear on keyboard focus (Tab key navigation)"
    - "Tooltips are readable in both light and dark modes"
    - "Keyword filter badges show native browser tooltip on hover"
  artifacts:
    - path: "R/mod_search_notebook.R"
      provides: "Toolbar button tooltips (6 buttons)"
      contains: "bslib::tooltip"
    - path: "app.R"
      provides: "Sidebar discovery button tooltips (6 buttons)"
      contains: "bslib::tooltip"
    - path: "R/mod_keyword_filter.R"
      provides: "Dynamic keyword badge title attributes"
      contains: "title ="
  key_links:
    - from: "R/mod_search_notebook.R"
      to: "bslib::tooltip()"
      via: "Wraps each actionButton in card_header grid"
      pattern: "tooltip\\(.*actionButton"
    - from: "app.R"
      to: "bslib::tooltip()"
      via: "Wraps each sidebar discovery actionButton"
      pattern: "tooltip\\(.*actionButton"
---

<objective>
Add accessible, keyboard-navigable tooltips to all 12 static buttons (6 toolbar + 6 sidebar) using bslib::tooltip(), and title attributes to dynamic keyword filter badges.

Purpose: Satisfy TOOL-05 requirement — every toolbar button has a descriptive tooltip for discoverability and WCAG 2.2 accessibility compliance.
Output: Updated mod_search_notebook.R, app.R, mod_keyword_filter.R with tooltip wrapping.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-tooltip-layer/54-CONTEXT.md
@.planning/phases/54-tooltip-layer/54-RESEARCH.md

<interfaces>
<!-- Key code locations the executor needs -->

From R/mod_search_notebook.R lines 88-126 (toolbar card_header):
```r
# 3x2 button grid — single CSS grid for perfect column alignment
div(
  style = "display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 0.5rem;",
  actionButton(ns("open_bulk_import"), "Import", ...),
  actionButton(ns("edit_search"), "Edit Search", ...),
  actionButton(ns("seed_citation_network"), "Cit Network", ...),
  div(class = "btn-group btn-group-sm w-100", ...),  # Export dropdown
  actionButton(ns("refresh_search"), "Refresh", ...),
  actionButton(ns("load_more"), "Load More", ...)
)
```

From app.R lines 176-193 (sidebar discovery buttons):
```r
actionButton("import_papers", "Import Papers", class = "btn-outline-peach", icon = icon_file_import()),
actionButton("discover_paper", "Discover from Paper", class = "btn-outline-success", icon = icon_seedling()),
actionButton("explore_topics", "Explore Topics", class = "btn-outline-warning", icon = icon_compass()),
actionButton("build_query", "Build a Query", class = "btn-outline-info", icon = icon_wand()),
actionButton("new_network", "Citation Network", class = "btn-outline-primary", icon = icon_diagram()),
actionButton("citation_audit", "Citation Audit", class = "btn-outline-sky", icon = icon_audit())
```

From R/mod_keyword_filter.R lines 126-134 (dynamic keyword badges):
```r
actionLink(
  ns(input_id),
  span(
    class = badge_class,
    style = "cursor: pointer;",
    badge_icon,
    paste0(kw$keyword, " (", kw$count, ")")
  )
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap all 12 static buttons with bslib::tooltip()</name>
  <files>R/mod_search_notebook.R, app.R</files>
  <action>
In R/mod_search_notebook.R, wrap each of the 6 toolbar buttons (lines ~92-125) with bslib::tooltip(). Use the approved copy from CONTEXT.md and the pattern from RESEARCH.md:

For the 5 regular actionButtons (Import, Edit Search, Cit Network, Refresh, Load More), wrap each with:
```r
bslib::tooltip(
  actionButton(...),
  "tooltip text here",
  placement = "bottom",
  options = list(delay = list(show = 300, hide = 100))
)
```

Approved toolbar tooltip copy:
- Import: "Add papers by pasting DOIs or uploading a BibTeX file"
- Edit Search: "Change your search query, filters, or discovery method"
- Cit Network: "Build a citation network from a seed paper in your results"
- Export: "Download your current papers as BibTeX or CSV"
- Refresh: "Re-run your current search to check for new results"
- Load More: "Fetch the next batch of results from OpenAlex"

For the Export dropdown (btn-group div), wrap the entire div with bslib::tooltip() and add container = "body" to the options list to prevent clipping:
```r
bslib::tooltip(
  div(class = "btn-group btn-group-sm w-100", ...),
  "Download your current papers as BibTeX or CSV",
  placement = "bottom",
  options = list(delay = list(show = 300, hide = 100), container = "body")
)
```

In app.R, wrap each of the 6 sidebar discovery buttons (lines ~176-193) with bslib::tooltip(). Same pattern but WITHOUT the container = "body" option (not in a btn-group):

Approved sidebar tooltip copy:
- Import Papers: "Add papers by pasting DOIs or uploading a BibTeX file"
- Discover from Paper: "Find related work by using a known paper as a seed"
- Explore Topics: "Browse OpenAlex topic hierarchies to find research areas"
- Build a Query: "Use AI to help construct an effective search query"
- Citation Network: "Visualize citation relationships between papers"
- Citation Audit: "Check your collection for missing references and gaps"

Do NOT wrap the "New Search Notebook" or "New Document Notebook" buttons (per user decision: labels are self-explanatory, excluded from tooltip coverage).
  </action>
  <verify>
    Smoke test only (tooltip behavior is visual — validated manually in Task 3 checkpoint):
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 8 && curl -s http://localhost:3838 > /dev/null && echo "APP_STARTED_OK" || echo "APP_FAILED"; kill %1 2>/dev/null</automated>
    Tooltip hover behavior, placement, and copy correctness are verified in Task 3 UAT checklist.
  </verify>
  <done>All 12 static buttons (6 toolbar + 6 sidebar) wrapped with bslib::tooltip() using approved copy, 300ms delay, bottom placement. App starts without error.</done>
</task>

<task type="auto">
  <name>Task 2: Add title attributes to dynamic keyword filter badges</name>
  <files>R/mod_keyword_filter.R</files>
  <action>
In R/mod_keyword_filter.R, add a `title` attribute to the actionLink elements that render keyword badges (around line 126-134). The title should describe the current state and action:

For each keyword badge actionLink, add a title attribute that reflects the filter state:
- neutral state: "Click to include this keyword in filter"
- include state: "Click to exclude this keyword"
- exclude state: "Click to clear this keyword filter"

Create the title text based on the `state` variable that's already computed at line 109:
```r
badge_title <- switch(state,
  "neutral" = paste0("Click to include '", kw$keyword, "' in filter"),
  "include" = paste0("Click to exclude '", kw$keyword, "'"),
  "exclude" = paste0("Click to clear '", kw$keyword, "' filter"),
  ""
)
```

Then add `title = badge_title` to the actionLink call. Since actionLink passes extra args to the underlying tag, add it as a named argument.

Do NOT use bslib::tooltip() for these dynamic elements — per user decision, dynamic buttons use title attributes only.
  </action>
  <verify>
    Smoke test only (title attribute behavior is visual — validated manually in Task 3 checkpoint):
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 8 && curl -s http://localhost:3838 > /dev/null && echo "APP_STARTED_OK" || echo "APP_FAILED"; kill %1 2>/dev/null</automated>
    Keyword badge title attributes are verified in Task 3 UAT checklist (step 10).
  </verify>
  <done>Keyword badge actionLinks have contextual title attributes that change based on filter state (neutral/include/exclude). App starts without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Accessible tooltips on all 12 static buttons (6 toolbar + 6 sidebar) using bslib::tooltip(), plus title attributes on dynamic keyword filter badges. All tooltips use approved copy with 300ms hover delay and bottom placement.</what-built>
  <how-to-verify>
    1. Start the app: open browser to http://localhost:3838
    2. Create or open a search notebook with results
    3. **Toolbar tooltips (6):** Hover each toolbar button (Import, Edit Search, Cit Network, Export, Refresh, Load More) — verify tooltip appears below after ~300ms with correct descriptive text
    4. **Sidebar tooltips (6):** Hover each sidebar discovery button (Import Papers, Discover from Paper, Explore Topics, Build a Query, Citation Network, Citation Audit) — verify tooltip appears below with correct text
    5. **Excluded buttons:** Verify "New Search Notebook" and "New Document Notebook" do NOT have tooltips
    6. **Keyboard accessibility:** Tab through toolbar buttons — verify tooltip appears on focus for each button
    7. **Escape dismissal:** Focus a button (Tab), see tooltip, press Escape — verify tooltip closes
    8. **Dark mode:** Toggle dark mode, repeat hover checks — verify tooltip background/text has readable contrast
    9. **Flicker test:** Quickly drag cursor across toolbar grid — verify no rapid tooltip flickering (300ms delay should prevent it)
    10. **Keyword badges:** If search has keyword results, hover a keyword badge — verify browser native tooltip shows state-appropriate text
    11. **Export dropdown:** Click Export dropdown — verify it still opens correctly and tooltip doesn't interfere with dropdown menu
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Shiny smoke test passes (app starts without crash)
- All 12 static buttons show bslib::tooltip() on hover with approved copy
- Keyboard Tab navigation triggers tooltip display
- Dark mode does not break tooltip readability
- Export dropdown still functions with tooltip wrapping
- Keyword badges show native title attribute tooltips
</verification>

<success_criteria>
- TOOL-05 satisfied: every toolbar button has a bslib tooltip (max 15 words, keyboard-accessible)
- 12 static buttons wrapped with bslib::tooltip() (6 toolbar + 6 sidebar)
- Dynamic keyword badges have title attributes
- 300ms delay prevents flicker in dense button grid
- WCAG 2.2 compliance via Bootstrap 5 native tooltip behavior (dismissible, hoverable, persistent)
</success_criteria>

<output>
After completion, create `.planning/phases/54-tooltip-layer/54-01-SUMMARY.md`
</output>
