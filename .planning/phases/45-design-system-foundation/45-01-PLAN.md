---
phase: 45-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/theme_catppuccin.R
  - www/swatch.html
autonomous: false
requirements:
  - DSGN-01
  - DSGN-02
must_haves:
  truths:
    - "Semantic color policy is documented in R/theme_catppuccin.R with action-to-color mappings for primary, danger, success, warning, info, secondary"
    - "Icon wrapper functions exist (icon_save, icon_delete, icon_search, etc.) returning standardized Font Awesome icons"
    - "Swatch sheet at www/swatch.html renders side-by-side Latte and Mocha with all button variants, icons, badges, sidebar colors, alerts, and form inputs"
    - "Peach and yellow are shown side-by-side in swatch for visual ambiguity evaluation"
    - "User validates swatch sheet before Phase 47 UI changes proceed"
  artifacts:
    - path: "R/theme_catppuccin.R"
      provides: "Semantic color policy documentation and icon wrapper functions"
      contains: "icon_save"
    - path: "www/swatch.html"
      provides: "Visual swatch sheet for both Catppuccin flavors"
      min_lines: 100
  key_links:
    - from: "R/theme_catppuccin.R"
      to: "www/swatch.html"
      via: "generate_swatch_html() function"
      pattern: "generate_swatch_html"
    - from: "R/theme_catppuccin.R"
      to: "MOCHA/LATTE constants"
      via: "Icon wrappers use shiny::icon(), color policy references MOCHA/LATTE hex values"
      pattern: "icon_save|icon_delete|icon_search"
---

<objective>
Define the semantic color/icon design system policy and generate a visual swatch sheet for validation.

Purpose: Establish documented rules for button colors, icon mappings, and sidebar theming that Phase 47 will apply across the entire app. The swatch sheet serves as a visual gate — user validates it before any UI code changes happen.

Output: Updated R/theme_catppuccin.R with semantic color policy comments, icon wrapper functions, and generate_swatch_html(). Static www/swatch.html showing all design system components in both Catppuccin Latte and Mocha.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-design-system-foundation/45-CONTEXT.md
@.planning/phases/45-design-system-foundation/45-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/theme_catppuccin.R (existing):
```r
# Color constants already defined:
MOCHA <- list(base, mantle, crust, text, subtext1, subtext0,
              surface0, surface1, surface2, overlay0, overlay1, overlay2,
              lavender, sapphire, sky, blue, green, yellow, red, peach)

LATTE <- list(base, mantle, crust, text, subtext1, subtext0,
              surface0, surface1, surface2, overlay0, overlay1, overlay2,
              lavender, sapphire, sky, blue, green, yellow, red, peach)

hex_to_rgb <- function(hex) # returns "R,G,B" string
catppuccin_dark_css <- function() # returns CSS string for dark mode overrides
```

From www/ directory:
- custom.css exists (app-wide styles)
- swatch.html will be NEW file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic color policy and icon wrappers to theme_catppuccin.R</name>
  <files>R/theme_catppuccin.R</files>
  <action>
Add two new sections to `R/theme_catppuccin.R` AFTER the existing MOCHA/LATTE constants and BEFORE the `catppuccin_dark_css()` function:

**Section 1: Semantic Color Policy (structured comments)**

Add a block comment documenting the action-to-color mapping per user decisions in CONTEXT.md:

```
# =============================================================================
# Semantic Color Policy (DSGN-01)
# =============================================================================
#
# Maps Catppuccin palette colors to Bootstrap semantic roles.
# This is the single source of truth for all button/badge/alert coloring.
# Phase 47 will apply these mappings across the UI.
#
# PRIMARY (blue) — Main actions: Search, Save, Add to Notebook
#   Mocha: #89b4fa | Latte: #1e66f5
#   Button: .btn-primary (solid fill, white text)
#   Sidebar: Active items inherit primary blue
#
# DANGER (red) — Destructive: Delete, Remove, Clear
#   Mocha: #f38ba8 | Latte: #d20f39
#   Button: .btn-danger (solid fill). No confirmation dialog — color IS the warning.
#   Reserve confirmation for irreversible bulk actions only.
#
# SUCCESS (green) — Confirmations: Paper Added, Export Complete
#   Mocha: #a6e3a1 | Latte: #40a02b
#   Button: .btn-success. Also used for completion badges and form validation.
#
# WARNING (yellow) — Cautions: API Key Missing, Rate Limit
#   Mocha: #f9e2af | Latte: #df8e1d
#   Button: .btn-warning. Also used for caution alerts.
#
# INFO (sapphire) — Informational: Tooltips, Help Text
#   Mocha: #74c7ec | Latte: #209fb5
#   Button: .btn-info. Distinct from primary blue.
#
# SECONDARY (surface0/surface1) — Less Important: Cancel, Close
#   Mocha: #313244/#45475a | Latte: #ccd0da/#bcc0cc
#   Button: .btn-outline-secondary (outline style, transparent bg)
#
# PEACH (candidate accent) — Highlights, Badges (PENDING VALIDATION)
#   Mocha: #fab387 | Latte: #fe640b
#   Concern: May look too similar to warning yellow. Evaluate in swatch sheet.
#
# Button Variant Policy:
#   - Primary actions: solid fill (.btn-primary, .btn-danger, etc.)
#   - Secondary actions: outline (.btn-outline-secondary)
#   - Three sizes: btn-sm (inline), default, btn-lg (hero/CTA)
# =============================================================================
```

**Section 2: Icon Wrapper Functions**

Add semantic icon wrapper functions that standardize Font Awesome icon usage. Each function calls `shiny::icon()` with the canonical FA icon name. Icons are color-neutral — color comes from button/context, not the icon.

Required wrappers (verify each FA icon name exists before using):
- `icon_save()` -> `shiny::icon("floppy-disk")`
- `icon_delete()` -> `shiny::icon("trash")`
- `icon_search()` -> `shiny::icon("magnifying-glass")`
- `icon_add()` -> `shiny::icon("plus")`
- `icon_download()` -> `shiny::icon("download")`
- `icon_upload()` -> `shiny::icon("upload")`
- `icon_settings()` -> `shiny::icon("gear")`
- `icon_info()` -> `shiny::icon("circle-info")`
- `icon_warning()` -> `shiny::icon("triangle-exclamation")`
- `icon_close()` -> `shiny::icon("xmark")`
- `icon_edit()` -> `shiny::icon("pen-to-square")`
- `icon_refresh()` -> `shiny::icon("arrows-rotate")`
- `icon_export()` -> `shiny::icon("file-export")`
- `icon_copy()` -> `shiny::icon("copy")`
- `icon_expand()` -> `shiny::icon("expand")`
- `icon_collapse()` -> `shiny::icon("compress")`
- `icon_filter()` -> `shiny::icon("filter")`
- `icon_sort()` -> `shiny::icon("sort")`
- `icon_book()` -> `shiny::icon("book")`
- `icon_paper()` -> `shiny::icon("file-lines")`

Each wrapper should accept `...` and pass through to `shiny::icon()` for additional classes/attributes.

Add roxygen-style doc comments for each wrapper following the pattern in RESEARCH.md.

Do NOT refactor or modify any existing code (MOCHA, LATTE, hex_to_rgb, catppuccin_dark_css). Only ADD new sections.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/theme_catppuccin.R'); cat('icon_save exists:', exists('icon_save'), '\n'); cat('icon_delete exists:', exists('icon_delete'), '\n'); cat('icon_search exists:', exists('icon_search'), '\n'); cat('MOCHA exists:', exists('MOCHA'), '\n'); cat('LATTE exists:', exists('LATTE'), '\n')"</automated>
  </verify>
  <done>
    - Semantic color policy documented as structured comments in R/theme_catppuccin.R
    - 20 icon wrapper functions defined and callable
    - Existing MOCHA/LATTE/catppuccin_dark_css code unchanged
    - All wrappers pass-through ... args to shiny::icon()
  </done>
</task>

<task type="auto">
  <name>Task 2: Create generate_swatch_html() and produce www/swatch.html</name>
  <files>R/theme_catppuccin.R, www/swatch.html</files>
  <action>
Add a `generate_swatch_html()` function to the END of `R/theme_catppuccin.R` (after `catppuccin_dark_css()`). This function uses `htmltools` to build a standalone HTML file and save it to `www/swatch.html`.

**Function signature:**
```r
generate_swatch_html <- function(output_path = "www/swatch.html")
```

**Layout (per user decision in CONTEXT.md):**
Side-by-side: Latte (light) on left column, Mocha (dark) on right column.

**Sections to include (top to bottom):**

1. **Raw Palette** — Show ALL Catppuccin colors from MOCHA/LATTE with:
   - Color swatch (rectangular div with background color)
   - Color name (e.g., "blue", "red", "peach")
   - Hex value
   - Display in a grid, matching colors side-by-side between Latte and Mocha

2. **Semantic Color Mapping** — Show which palette color maps to which Bootstrap semantic role:
   - Primary = blue, Danger = red, Success = green, Warning = yellow, Info = sapphire, Secondary = surface0/surface1
   - Label each with both the semantic name and the action description (e.g., "PRIMARY (blue) — Main actions")

3. **Buttons** — All button variants:
   - `.btn-primary`, `.btn-danger`, `.btn-success`, `.btn-warning`, `.btn-info`, `.btn-secondary`
   - `.btn-outline-primary`, `.btn-outline-secondary`, `.btn-outline-danger`
   - Three sizes: `.btn-sm`, default, `.btn-lg`
   - States: default, hover (via `:hover` CSS), disabled (`opacity: 0.65`)

4. **Peach vs Yellow Comparison** — Side-by-side peach and yellow swatches with:
   - Both as badge backgrounds with sample text
   - Both as highlight backgrounds
   - Clear labels so user can evaluate visual ambiguity

5. **Badges** — Bootstrap badge variants:
   - `.badge.bg-primary`, `.badge.bg-danger`, `.badge.bg-success`, `.badge.bg-warning`, `.badge.bg-info`
   - Peach badge (custom `bg-peach` class) for comparison

6. **Sidebar** — Simulated sidebar items:
   - Active item (primary blue background)
   - Inactive items (surface colors)
   - Hover state (slightly lighter surface)

7. **Alerts** — Bootstrap alert variants:
   - `.alert-primary`, `.alert-danger`, `.alert-success`, `.alert-warning`, `.alert-info`

8. **Form Inputs** — Input fields showing:
   - Default state
   - Focus state (with focus ring color)
   - Disabled state

9. **Cards** — Card component with:
   - Card header, body, footer
   - Proper border colors per theme

10. **Icons** — Display all icon wrappers (call each icon_* function) with labels showing the function name and the FA icon it maps to

**CSS approach:**
- Embed ALL CSS inline in the HTML `<style>` block — no external dependencies
- Use CSS custom properties (`--primary`, `--danger`, etc.) scoped to `.theme-latte` and `.theme-mocha` containers
- Style buttons/badges/alerts to match Bootstrap 5 appearance using the CSS custom properties
- Include hover states via CSS `:hover` rules
- Include a disabled state (opacity: 0.65) for buttons
- Use a 2-column CSS grid or flexbox for side-by-side layout
- Include a header with title "Serapeum Design System Swatch Sheet" and generation timestamp

**Font Awesome:**
- Include Font Awesome 6 via CDN link in the `<head>` so icons render correctly
- For each icon wrapper, render the icon using raw `<i class="fa-solid fa-{name}">` HTML tags (since shiny::icon() returns Shiny tags, use the underlying FA class names directly in the static HTML)

**After building the htmltools tags structure, call:**
```r
htmltools::save_html(swatch_ui, file = output_path)
```

**Then immediately invoke the function to generate the file:**
After defining `generate_swatch_html()`, add a call at the bottom of the task to actually run it and produce `www/swatch.html`. The executor should run:
```r
source("R/theme_catppuccin.R")
generate_swatch_html()
```

The resulting `www/swatch.html` should be openable in any browser without running Shiny.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/theme_catppuccin.R'); generate_swatch_html('www/swatch.html'); cat('File exists:', file.exists('www/swatch.html'), '\n'); cat('File size:', file.size('www/swatch.html'), 'bytes\n'); lines <- readLines('www/swatch.html'); cat('Line count:', length(lines), '\n'); cat('Has Latte:', any(grepl('Latte', lines)), '\n'); cat('Has Mocha:', any(grepl('Mocha', lines)), '\n'); cat('Has btn-primary:', any(grepl('btn-primary', lines)), '\n'); cat('Has peach:', any(grepl('peach', lines)), '\n')"</automated>
  </verify>
  <done>
    - generate_swatch_html() function defined in R/theme_catppuccin.R
    - www/swatch.html exists and is openable in browser
    - Swatch shows side-by-side Latte (left) and Mocha (right)
    - All 10 sections rendered: raw palette, semantic mapping, buttons, peach vs yellow, badges, sidebar, alerts, form inputs, cards, icons
    - Peach and yellow appear side-by-side for visual evaluation
    - Font Awesome icons render correctly via CDN
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User validates swatch sheet</name>
  <files>www/swatch.html</files>
  <action>
    User opens www/swatch.html in browser and validates:
    1. Side-by-side layout: Latte (light) on left, Mocha (dark) on right
    2. Semantic color mappings match decisions: blue=primary, red=danger, green=success, yellow=warning, sapphire=info
    3. Peach vs yellow: distinct enough for badges/highlights vs warnings?
    4. Button variants: solid primary, outline secondary, danger, all three sizes
    5. Sidebar simulation: active item in primary blue, inactive in surface colors
    6. Icons: all 20 icon wrappers displayed with correct Font Awesome icons
    7. Alerts, badges, form inputs, and cards correct in both themes
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has validated the design system swatch sheet and approved it for Phase 47 implementation</done>
</task>

</tasks>

<verification>
1. `source("R/theme_catppuccin.R")` succeeds without errors — all existing code plus new additions load
2. Icon wrapper functions callable: `icon_save()`, `icon_delete()`, `icon_search()` etc.
3. `www/swatch.html` exists and opens in browser showing both themes
4. Swatch contains peach vs yellow comparison section
5. User approves swatch before Phase 47 begins
</verification>

<success_criteria>
- DSGN-01 met: Semantic color policy documented with button semantics, icon-action mappings, and sidebar theming rules in R/theme_catppuccin.R
- DSGN-02 met: Visual swatch sheet rendered in both light and dark mode at www/swatch.html with all button variants, icon mappings, sidebar colors, badge styles
- User has validated the swatch sheet
- No existing code in theme_catppuccin.R broken
</success_criteria>

<output>
After completion, create `.planning/phases/45-design-system-foundation/45-01-SUMMARY.md`
</output>
