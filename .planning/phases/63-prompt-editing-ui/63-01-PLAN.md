---
phase: 63-prompt-editing-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - R/prompt_helpers.R
  - tests/testthat/test-prompt-helpers.R
autonomous: true
requirements: [PRMT-01, PRMT-02, PRMT-03, PRMT-05, PRMT-06]

must_haves:
  truths:
    - "get_effective_prompt() returns hardcoded default when no custom version exists"
    - "save_prompt_version() writes to prompt_versions and get_effective_prompt() returns the custom text"
    - "save_prompt_version() same-day call replaces existing row (UPSERT)"
    - "list_prompt_versions() returns dates in descending order"
    - "get_prompt_version() returns correct text for a specific date"
    - "reset_prompt_to_default() deletes all rows for a slug"
    - "After reset, get_effective_prompt() returns hardcoded default"
    - "PROMPT_DEFAULTS has entries for all 11 preset slugs"
  artifacts:
    - path: "R/prompt_helpers.R"
      provides: "CRUD helpers and PROMPT_DEFAULTS registry"
      exports: ["PROMPT_DEFAULTS", "get_effective_prompt", "get_active_prompt", "save_prompt_version", "list_prompt_versions", "get_prompt_version", "reset_prompt_to_default", "PRESET_GROUPS"]
    - path: "tests/testthat/test-prompt-helpers.R"
      provides: "Unit tests for all CRUD behaviors"
      min_lines: 80
  key_links:
    - from: "R/prompt_helpers.R"
      to: "prompt_versions table"
      via: "DBI::dbGetQuery/dbExecute"
      pattern: "prompt_versions"
---

<objective>
TDD: Create R/prompt_helpers.R with CRUD functions for the prompt_versions table and a PROMPT_DEFAULTS named list containing the exact hardcoded default text for all 11 AI preset slugs.

Purpose: This is the data layer for the prompt editing feature. Every other plan depends on these functions existing and being tested.
Output: R/prompt_helpers.R (production), tests/testthat/test-prompt-helpers.R (tests)
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-prompt-editing-ui/63-CONTEXT.md
@.planning/phases/63-prompt-editing-ui/63-RESEARCH.md
@.planning/phases/62-prompt-storage-schema/62-01-SUMMARY.md

<interfaces>
<!-- prompt_versions table schema (from migrations/011_create_prompt_versions.sql) -->
CREATE TABLE prompt_versions (
  preset_slug  VARCHAR   NOT NULL,
  version_date DATE      NOT NULL,
  prompt_text  TEXT      NOT NULL,
  created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (preset_slug, version_date)
)

<!-- DuckDB UPSERT syntax (confirmed in test-db-migrations.R line 261) -->
INSERT OR REPLACE INTO prompt_versions (preset_slug, version_date, prompt_text) VALUES (?, ?, ?)

<!-- Existing test fixture pattern (from test-db-migrations.R) -->
In-memory DuckDB via DBI::dbConnect(duckdb::duckdb(), ":memory:")
Create table manually in setup, no need to run full migration runner.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD prompt_helpers.R — CRUD functions and PROMPT_DEFAULTS registry</name>
  <files>R/prompt_helpers.R, tests/testthat/test-prompt-helpers.R</files>
  <read_first>
    - R/rag.R (lines 157-162 for quick preset defaults, lines 384-406 for conclusions, lines 550-573 for overview, lines 811-833 for research_questions, lines 1038-1053 for lit_review, lines 1242-1262 for methodology, lines 1471-1493 for gap_analysis)
    - R/slides.R (lines 64-101 for slides prompt)
    - migrations/011_create_prompt_versions.sql (table schema)
    - tests/testthat/test-db-migrations.R (lines 250-273 for DuckDB test fixture pattern and UPSERT syntax)
  </read_first>
  <behavior>
    - PROMPT_DEFAULTS is a named list with exactly 11 entries: summarize, keypoints, studyguide, outline, conclusions, overview, research_questions, lit_review, methodology, gap_analysis, slides
    - Each PROMPT_DEFAULTS entry is a non-empty character string
    - PRESET_GROUPS is a named list with two entries: "Quick" (4 slugs) and "Deep" (7 slugs)
    - get_effective_prompt(con, "summarize") returns PROMPT_DEFAULTS[["summarize"]] when no rows exist in prompt_versions
    - save_prompt_version(con, "summarize", "custom text") followed by get_effective_prompt(con, "summarize") returns "custom text"
    - save_prompt_version(con, "summarize", "v1") then save_prompt_version(con, "summarize", "v2") on same day: get_effective_prompt returns "v2" (UPSERT replaces)
    - list_prompt_versions(con, "summarize") returns character vector of version_date strings, most recent first
    - get_prompt_version(con, "summarize", "2026-03-21") returns correct prompt_text for that date
    - get_prompt_version(con, "summarize", "1999-01-01") returns NULL (no such version)
    - reset_prompt_to_default(con, "summarize") deletes all rows; get_effective_prompt then returns hardcoded default
    - get_active_prompt(con, "summarize") returns NULL when no custom versions exist
    - get_active_prompt(con, "summarize") returns most recent custom text when versions exist
  </behavior>
  <action>
RED phase: Create tests/testthat/test-prompt-helpers.R with an in-memory DuckDB fixture that creates the prompt_versions table manually (same DDL as migration 011). Write test_that blocks covering all behaviors listed above. Run tests — all must FAIL.

GREEN phase: Create R/prompt_helpers.R with:

1. PROMPT_DEFAULTS — named list with exact default text extracted from R/rag.R and R/slides.R:
   - "summarize": exact string from rag.R line 158 ("Provide a comprehensive summary of all the documents. Highlight the main themes, key findings, and important conclusions. Organize your summary with clear sections.")
   - "keypoints": exact string from rag.R line 159 ("Extract the key points from these documents as a bulleted list. Focus on the most important facts, findings, arguments, and conclusions. Group related points together.")
   - "studyguide": exact string from rag.R line 160 (multi-line with \n)
   - "outline": exact string from rag.R line 161 ("Create a structured outline of the main topics covered in these documents. Use hierarchical headings (I, A, 1, a) to organize the content logically. Include brief descriptions under each heading.")
   - "conclusions": the task instruction portion from lines 384-389 only: "You are a research synthesis assistant" role line is HIDDEN. Editable portion: the two numbered items (1. Summarize..., 2. Identify...) plus the IMPORTANT paragraph. Specifically: "1. Summarize the key conclusions across the provided research sources\n2. Identify common themes, agreements, and divergent positions\n\nIMPORTANT: Base your synthesis ONLY on the provided sources. Do not invent findings or cite sources not provided. If sources conflict, note the disagreement explicitly."
   - "overview": the task instruction portion from lines 551-564 (the section format instructions between the role line and CITATION RULES). Specifically: "Generate an Overview of the provided research sources.\nThe Overview must have exactly two sections:\n\n## Summary\n%s\nCover main themes, key findings, and important conclusions.\nBase your summary ONLY on the provided sources.\n\n## Key Points\nOrganize key points under thematic subheadings in this order: Background/Context, Methodology, Findings/Results, Limitations, Future Directions/Gaps.\nEach subheading should contain 3-5 bullet points.\nDo not use a flat bullet list - group all related points under their subheading.\n\nIMPORTANT: Base all content ONLY on the provided sources. Do not invent findings." (Note: keep the %s placeholder for depth_instruction as-is — the generator will sprintf it)
   - "research_questions": lines 811-833 minus the role line. Editable portion starts at "INSTRUCTIONS:" through the end of the IMPORTANT paragraph. Specifically everything from "INSTRUCTIONS:\n1. Analyze the provided research sources..." through "...Every claim in a rationale must trace to a specific source."
   - "lit_review": lines 1039-1053 minus the role line. Editable portion starts at "COLUMNS (exactly these..." through the FOOTNOTES block ending "...Only include footnotes for Key Findings column entries."
   - "methodology": lines 1243-1261 minus the role line. Editable portion starts at "COLUMNS (exactly these..." through the FOOTNOTES block ending "...Include footnotes for Study Design and Statistical Methods columns."
   - "gap_analysis": lines 1472-1492 minus the role line. Editable portion starts at "OUTPUT FORMAT:" through "...Each gap section: identify specific absent elements, underrepresented contexts, or unresolved questions"
   - "slides": lines 92-100 (content rules block only). Editable portion: "- Use ## for individual slide titles (each ## starts a new slide)\n- Use # for section titles (creates section dividers)\n- Keep slides concise - max 5-7 bullet points per slide\n- Always leave a blank line between a heading and the first bullet point\n- Each bullet point must be on its own line starting with - (not inline)\n- Output ONLY valid Quarto markdown slide content, no explanations or code fences\n- Do NOT include any YAML frontmatter, --- delimiters, title:, format:, theme:, or css:"

2. PRESET_GROUPS — named list:
   Quick = c("summarize", "keypoints", "studyguide", "outline")
   Deep = c("overview", "conclusions", "research_questions", "lit_review", "methodology", "gap_analysis", "slides")

3. PRESET_DISPLAY_NAMES — named character vector mapping slugs to human labels:
   summarize="Summarize", keypoints="Key Points", studyguide="Study Guide", outline="Outline",
   overview="Overview", conclusions="Conclusions", research_questions="Research Questions",
   lit_review="Literature Review", methodology="Methodology Extractor", gap_analysis="Gap Analysis",
   slides="Slides"

4. CRUD functions (all take con as first arg, use DBI::dbGetQuery/dbExecute with parameterized queries):
   - list_prompt_versions(con, preset_slug) — SELECT version_date ORDER BY version_date DESC, return as.character vector
   - get_prompt_version(con, preset_slug, version_date) — SELECT prompt_text WHERE both match, return string or NULL
   - get_active_prompt(con, preset_slug) — SELECT prompt_text ORDER BY version_date DESC LIMIT 1, return string or NULL
   - save_prompt_version(con, preset_slug, prompt_text) — INSERT OR REPLACE with as.character(Sys.Date()), return invisible(TRUE)
   - reset_prompt_to_default(con, preset_slug) — DELETE FROM prompt_versions WHERE preset_slug = ?, return invisible(TRUE)
   - get_effective_prompt(con, preset_slug) — calls get_active_prompt, falls back to PROMPT_DEFAULTS[[preset_slug]]

Run tests — all must PASS.

IMPORTANT: When extracting default text from R/rag.R, read the actual lines carefully. The role preamble line (e.g., "You are a research synthesis assistant...") is ALWAYS excluded from the editable portion. CITATION RULES blocks are ALWAYS excluded. OUTPUT FORMAT blocks for conclusions are excluded (they are structural). For research_questions, the OUTPUT FORMAT and SCALING blocks ARE part of the editable portion (they are substantive instructions).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-prompt-helpers.R')"</automated>
  </verify>
  <acceptance_criteria>
    - R/prompt_helpers.R exists and contains `PROMPT_DEFAULTS <- list(`
    - R/prompt_helpers.R contains `PRESET_GROUPS <- list(`
    - R/prompt_helpers.R contains `PRESET_DISPLAY_NAMES <- c(`
    - R/prompt_helpers.R contains all 6 function definitions: `list_prompt_versions <-`, `get_prompt_version <-`, `get_active_prompt <-`, `save_prompt_version <-`, `reset_prompt_to_default <-`, `get_effective_prompt <-`
    - PROMPT_DEFAULTS has exactly 11 named entries (grep for each slug name in the list)
    - tests/testthat/test-prompt-helpers.R exists with at least 8 test_that blocks
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>All CRUD functions implemented with TDD, PROMPT_DEFAULTS contains exact default text for all 11 presets, all tests pass</done>
</task>

</tasks>

<verification>
- All tests in test-prompt-helpers.R pass
- PROMPT_DEFAULTS has entries for all 11 slugs
- CRUD functions handle edge cases (NULL returns, empty results)
</verification>

<success_criteria>
- R/prompt_helpers.R exists with 6 exported functions and 3 data objects
- tests/testthat/test-prompt-helpers.R passes with 0 failures
- PROMPT_DEFAULTS text matches the actual hardcoded defaults in R/rag.R and R/slides.R
</success_criteria>

<output>
After completion, create `.planning/phases/63-prompt-editing-ui/63-01-SUMMARY.md`
</output>
