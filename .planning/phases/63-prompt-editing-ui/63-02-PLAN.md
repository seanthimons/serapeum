---
phase: 63-prompt-editing-ui
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - R/mod_settings.R
autonomous: false
requirements: [PRMT-01, PRMT-02, PRMT-03, PRMT-05, PRMT-06]

must_haves:
  truths:
    - "Settings page shows an AI Prompts section listing all 11 presets in two groups (Quick and Deep)"
    - "Clicking a preset opens a modal with version dropdown, read-only note, textarea editor, Save and Reset buttons"
    - "Editing text and clicking Save stores the new version in prompt_versions via save_prompt_version()"
    - "Version dropdown shows saved dates; selecting one loads that version's text into the textarea"
    - "Reset to Default loads hardcoded text into textarea; saving after reset deletes all custom versions"
    - "RAG plumbing is never visible in the editor — only task instruction text"
  artifacts:
    - path: "R/mod_settings.R"
      provides: "Prompt editor UI section and modal server logic"
      contains: "AI Prompts"
  key_links:
    - from: "R/mod_settings.R"
      to: "R/prompt_helpers.R"
      via: "function calls"
      pattern: "get_effective_prompt|save_prompt_version|list_prompt_versions|reset_prompt_to_default|PRESET_GROUPS|PRESET_DISPLAY_NAMES"
    - from: "R/mod_settings.R"
      to: "prompt_versions table"
      via: "through prompt_helpers.R CRUD functions"
      pattern: "save_prompt_version|get_prompt_version"
---

<objective>
Add an "AI Prompts" section to the Settings page that lists all 11 AI presets in two groups (Quick/Deep). Clicking any preset opens a modal dialog with a version dropdown, read-only explanatory note, textarea editor, Save button, and Reset to Default button.

Purpose: This is the user-facing UI for viewing, editing, versioning, and resetting AI preset prompts (PRMT-01 through PRMT-06).
Output: Modified R/mod_settings.R with new UI section and server logic.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-prompt-editing-ui/63-CONTEXT.md
@.planning/phases/63-prompt-editing-ui/63-RESEARCH.md
@.planning/phases/63-prompt-editing-ui/63-01-PLAN.md

<interfaces>
<!-- From R/prompt_helpers.R (created in Plan 01) -->
PROMPT_DEFAULTS — named list with 11 entries (slug -> default text string)
PRESET_GROUPS — list(Quick = c("summarize", "keypoints", "studyguide", "outline"), Deep = c("overview", "conclusions", "research_questions", "lit_review", "methodology", "gap_analysis", "slides"))
PRESET_DISPLAY_NAMES — named character vector (slug -> human label)

get_effective_prompt(con, preset_slug) -> character string (custom or default)
save_prompt_version(con, preset_slug, prompt_text) -> invisible(TRUE)
list_prompt_versions(con, preset_slug) -> character vector of date strings (desc order)
get_prompt_version(con, preset_slug, version_date) -> character string or NULL
reset_prompt_to_default(con, preset_slug) -> invisible(TRUE)
get_active_prompt(con, preset_slug) -> character string or NULL

<!-- From R/mod_settings.R (current structure) -->
mod_settings_ui(id) — card with card_header + card_body(layout_columns(col_widths=c(6,6), left_div, right_div))
mod_settings_server(id, con, config_rv) — moduleServer with observeEvent handlers

<!-- Semantic icon wrappers (from R/theme_catppuccin.R) -->
icon_edit() — pen-to-square
icon_save() — floppy-disk
icon_refresh() — arrows-rotate
icon_info() — circle-info
icon_settings() — gear
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI Prompts section to Settings UI</name>
  <files>R/mod_settings.R</files>
  <read_first>
    - R/mod_settings.R (full file — understand current structure, line 139 is end of UI function)
    - R/prompt_helpers.R (created by Plan 01 — PRESET_GROUPS, PRESET_DISPLAY_NAMES constants)
    - R/theme_catppuccin.R (lines 130-195 for available icon wrappers)
  </read_first>
  <action>
Modify mod_settings_ui() to add a new full-width section BELOW the existing layout_columns() block but still inside the card_body(). Add:

1. An `hr()` separator after the closing `)` of layout_columns
2. `h5(icon_edit(), " AI Prompts")` heading
3. `p(class = "text-muted small", "Customize the task instructions for each AI preset. Changes affect all future generations.")` description
4. Two sub-sections using the PRESET_GROUPS constant:

For each group ("Quick" and "Deep"), render:
- `h6(group_name)` subheading
- A series of `actionLink(ns(paste0("edit_", slug)), label = PRESET_DISPLAY_NAMES[[slug]], icon = icon_edit())` buttons, one per slug in the group
- Separate each actionLink with a ` | ` text separator (or use a `div(class = "d-flex flex-wrap gap-2 mb-2", ...)` to wrap them as inline buttons)

Use `lapply()` over PRESET_GROUPS to generate the UI dynamically:

```r
hr(),
h5(icon_edit(), " AI Prompts"),
p(class = "text-muted small",
  "Customize the task instructions for each AI preset. Changes affect all future generations."),
lapply(names(PRESET_GROUPS), function(group_name) {
  slugs <- PRESET_GROUPS[[group_name]]
  tagList(
    h6(group_name),
    div(class = "d-flex flex-wrap gap-2 mb-3",
      lapply(slugs, function(slug) {
        actionLink(ns(paste0("edit_", slug)),
                   label = PRESET_DISPLAY_NAMES[[slug]],
                   class = "btn btn-outline-secondary btn-sm")
      })
    )
  )
})
```

This goes after the closing parenthesis of `layout_columns(...)` on line ~137, but before the closing `)` of `card_body()`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/theme_catppuccin.R'); source('R/prompt_helpers.R'); source('R/mod_settings.R'); cat('UI function parsed OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_settings.R contains the string "AI Prompts"
    - R/mod_settings.R contains `PRESET_GROUPS` reference
    - R/mod_settings.R contains `PRESET_DISPLAY_NAMES` reference
    - R/mod_settings.R contains `edit_` pattern for actionLink IDs (e.g., `paste0("edit_", slug)`)
    - R/mod_settings.R contains `icon_edit()` call in the AI Prompts heading
    - The UI function parses without error (verify command exits 0)
  </acceptance_criteria>
  <done>Settings page has an AI Prompts section with grouped preset buttons that parse without error</done>
</task>

<task type="auto">
  <name>Task 2: Add modal editor and server logic for prompt editing</name>
  <files>R/mod_settings.R</files>
  <read_first>
    - R/mod_settings.R (full file — understand server function structure, existing observeEvent patterns)
    - R/prompt_helpers.R (CRUD function signatures)
    - R/mod_document_notebook.R (lines 360-380 for modal dialog pattern reference)
  </read_first>
  <action>
Add server-side logic inside mod_settings_server() (within the moduleServer block) for the prompt editor modal. Add these components:

1. ReactiveVals for tracking state:
```r
current_preset_slug <- reactiveVal(NULL)
reset_pending <- reactiveVal(FALSE)
```

2. A single observer factory that creates one observeEvent per preset slug. Use `local()` inside `lapply()` to capture the slug correctly:
```r
lapply(unlist(PRESET_GROUPS, use.names = FALSE), function(slug) {
  local({
    s <- slug
    observeEvent(input[[paste0("edit_", s)]], {
      current_preset_slug(s)
      reset_pending(FALSE)

      versions <- list_prompt_versions(con(), s)
      initial_text <- get_effective_prompt(con(), s)

      version_choices <- if (length(versions) > 0) {
        c("Current" = "current", setNames(versions, paste("Saved:", versions)))
      } else {
        c("Current (default)" = "current")
      }

      showModal(modalDialog(
        title = paste(icon_edit(), "Edit Prompt:", PRESET_DISPLAY_NAMES[[s]]),
        size = "l",
        easyClose = TRUE,
        selectInput(session$ns("version_select"), "Version History",
                    choices = version_choices, selected = "current"),
        p(class = "text-muted small",
          icon_info(),
          "This prompt is combined with citation rules and source context when generating output.",
          "You're editing the task instructions only."),
        textAreaInput(session$ns("prompt_text"), NULL,
                      value = initial_text, rows = 15, width = "100%"),
        footer = tagList(
          actionButton(session$ns("save_prompt"), "Save",
                       class = "btn-primary", icon = icon_save()),
          actionButton(session$ns("reset_prompt"), "Reset to Default",
                       class = "btn-outline-secondary", icon = icon_refresh()),
          modalButton("Cancel")
        )
      ))
    })
  })
})
```

3. Version dropdown observer — when user selects a different version:
```r
observeEvent(input$version_select, {
  slug <- current_preset_slug()
  req(slug)
  selected <- input$version_select

  if (selected == "current") {
    text <- get_effective_prompt(con(), slug)
  } else {
    text <- get_prompt_version(con(), slug, selected)
    if (is.null(text)) text <- get_effective_prompt(con(), slug)
  }
  updateTextAreaInput(session, "prompt_text", value = text)
}, ignoreInit = TRUE)
```

4. Save button observer:
```r
observeEvent(input$save_prompt, {
  slug <- current_preset_slug()
  req(slug)

  if (reset_pending()) {
    reset_prompt_to_default(con(), slug)
    reset_pending(FALSE)
    removeModal()
    showNotification(
      paste("Reset to default:", PRESET_DISPLAY_NAMES[[slug]]),
      type = "message"
    )
  } else {
    text <- trimws(input$prompt_text)
    if (nchar(text) == 0) {
      showNotification("Prompt cannot be empty.", type = "error")
      return()
    }
    save_prompt_version(con(), slug, text)

    # Refresh version dropdown
    versions <- list_prompt_versions(con(), slug)
    version_choices <- if (length(versions) > 0) {
      c("Current" = "current", setNames(versions, paste("Saved:", versions)))
    } else {
      c("Current (default)" = "current")
    }
    updateSelectInput(session, "version_select",
                      choices = version_choices, selected = "current")

    showNotification(
      paste("Prompt saved:", PRESET_DISPLAY_NAMES[[slug]]),
      type = "message"
    )
  }
})
```

5. Reset button observer:
```r
observeEvent(input$reset_prompt, {
  slug <- current_preset_slug()
  req(slug)
  reset_pending(TRUE)
  default_text <- PROMPT_DEFAULTS[[slug]]
  updateTextAreaInput(session, "prompt_text", value = default_text)
  showNotification("Default text loaded. Click Save to confirm reset.", type = "warning")
})
```

IMPORTANT notes:
- Use `session$ns()` (not `ns()`) inside the server function for dynamic modal input IDs — `ns` is only available in the UI function.
- The `con` parameter is a reactive, so always call it as `con()` to get the connection.
- The `local({ s <- slug; ... })` pattern is critical to avoid the standard R closure-over-loop-variable bug.
- Do NOT add `source("R/prompt_helpers.R")` — the app.R already sources all R/ files.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/theme_catppuccin.R'); source('R/prompt_helpers.R'); source('R/db.R'); source('R/api_openrouter.R'); source('R/api_openalex.R'); source('R/mod_settings.R'); cat('Server function parsed OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_settings.R contains `current_preset_slug <- reactiveVal(NULL)`
    - R/mod_settings.R contains `reset_pending <- reactiveVal(FALSE)`
    - R/mod_settings.R contains `observeEvent(input$save_prompt,` handler
    - R/mod_settings.R contains `observeEvent(input$reset_prompt,` handler
    - R/mod_settings.R contains `observeEvent(input$version_select,` handler
    - R/mod_settings.R contains `showModal(modalDialog(` for the prompt editor
    - R/mod_settings.R contains the read-only note text: "combined with citation rules and source context"
    - R/mod_settings.R contains `save_prompt_version(con()` call
    - R/mod_settings.R contains `reset_prompt_to_default(con()` call
    - R/mod_settings.R contains `get_effective_prompt(con()` call
    - The file parses without error (verify command exits 0)
  </acceptance_criteria>
  <done>Modal editor fully wired with version dropdown, save, reset, and version loading — all server logic parses cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>AI Prompts section in Settings page with modal editor for viewing, editing, versioning, and resetting AI preset prompts</what-built>
  <how-to-verify>
    1. Start the app and navigate to Settings
    2. Scroll down — verify "AI Prompts" section appears with Quick and Deep groups
    3. Click "Summarize" — verify modal opens with:
       - Version dropdown showing "Current (default)"
       - Read-only note about citation rules and source context
       - Textarea with default summarize prompt text
       - Save and Reset to Default buttons
    4. Edit the text, click Save — verify success notification appears
    5. Close and reopen "Summarize" — verify edited text persists and version dropdown shows today's date
    6. Select the saved date version — verify text loads
    7. Click "Reset to Default" — verify default text appears in textarea with warning notification
    8. Click Save after reset — verify reset confirmation notification
    9. Reopen — verify back to default text with no version history
    10. Test a Deep preset (e.g., "Conclusions") — verify its default text appears (different from Summarize)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Settings page renders with AI Prompts section
- Modal opens for each preset with correct default text
- Save/load/reset cycle works end-to-end
- Version dropdown reflects saved versions
</verification>

<success_criteria>
- All 11 presets are accessible from the Settings page
- Modal editor shows only task instruction text (no RAG plumbing visible)
- Save creates a new version in prompt_versions table
- Reset restores hardcoded default and clears custom versions
- Version dropdown allows loading historical versions
</success_criteria>

<output>
After completion, create `.planning/phases/63-prompt-editing-ui/63-02-SUMMARY.md`
</output>
