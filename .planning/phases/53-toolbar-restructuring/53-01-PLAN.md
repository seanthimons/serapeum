---
phase: 53-toolbar-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_search_notebook.R
  - R/mod_keyword_filter.R
autonomous: false
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-06

must_haves:
  truths:
    - "All 6 toolbar buttons display icon+text labels (Import, Edit Search, Citation Network, Export, Refresh, Load More)"
    - "Buttons are arranged in a 3x2 grid: Row 1 (Import | Edit Search | Citation Network), Row 2 (Export | Refresh | Load More)"
    - "Lavender (btn-outline-primary) on Import, Citation Network, Export, Load More; Gray (btn-outline-secondary) on Edit Search, Refresh"
    - "Papers label is gone from card header"
    - "Remaining count appears in keywords panel summary as bold formatted number (e.g., 1.6M remaining)"
    - "Panel split is 5/7 instead of 4/8"
    - "Sort radio buttons are evenly spaced across container width"
  artifacts:
    - path: "R/mod_search_notebook.R"
      provides: "Restructured toolbar with 3x2 grid layout, format_large_number helper"
      contains: "d-flex gap-2 mb-1"
    - path: "R/mod_keyword_filter.R"
      provides: "Keywords panel summary with remaining count"
      contains: "remaining"
  key_links:
    - from: "R/mod_search_notebook.R"
      to: "R/mod_keyword_filter.R"
      via: "remaining_count reactive passed to keyword filter module"
      pattern: "remaining_count|api_total"
---

<objective>
Restructure the search notebook toolbar from a single-row horizontal button strip into a 3x2 grid with icon+text labels, semantic color harmony, and workflow-ordered grouping. Remove the "Papers" header label, relocate the result count to the keywords panel, and widen the paper list panel.

Purpose: Improve toolbar usability by making buttons discoverable (icon+text), logically grouped (input/discovery vs output/data), and visually harmonized (lavender for actions, gray for support).
Output: Updated mod_search_notebook.R with new toolbar layout, updated mod_keyword_filter.R with remaining count display.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-toolbar-restructuring/53-CONTEXT.md
@.planning/phases/53-toolbar-restructuring/53-RESEARCH.md

@R/mod_search_notebook.R
@R/mod_keyword_filter.R
@R/theme_catppuccin.R

<interfaces>
<!-- Current toolbar structure in R/mod_search_notebook.R lines 74-116 -->
<!-- card_header with d-flex justify-content-between, span("Papers"), button row -->
<!-- col_widths = c(4, 8) at line 75 -->
<!-- format_result_count(fetched, total) at line 40 -->
<!-- output$result_count at line 2463 renders format_result_count() -->

<!-- Current keyword filter in R/mod_keyword_filter.R -->
<!-- mod_keyword_filter_server(id, papers_data) — only receives papers_data -->
<!-- output$summary at line 70 renders "X papers | Y keywords" -->

<!-- Icon wrappers from R/theme_catppuccin.R (all exist): -->
<!-- icon_file_import(), icon_edit(), icon_share_nodes(), icon_download(), -->
<!-- icon_rotate(), icon_angles_down(), icon_file_code(), icon_file_csv() -->

<!-- Catppuccin color mappings (already configured in bs_theme()): -->
<!-- btn-outline-primary = lavender, btn-outline-secondary = gray -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure toolbar to 3x2 grid with icon+text labels and semantic colors</name>
  <files>R/mod_search_notebook.R</files>
  <action>
  Modify R/mod_search_notebook.R to restructure the toolbar and related layout:

  1. **Change col_widths** (line 75): Replace `c(4, 8)` with `c(5, 7)` to give paper titles more room.

  2. **Replace card_header** (lines 78-116): Remove the entire `d-flex justify-content-between` container including `span("Papers")` and the single-row button strip. Replace with a 3x2 grid:

  Row 1 (d-flex gap-2 mb-1):
  - `actionButton(ns("open_bulk_import"), "Import", class = "btn-sm btn-outline-primary flex-fill", icon = icon_file_import())`
  - `actionButton(ns("edit_search"), "Edit Search", class = "btn-sm btn-outline-secondary flex-fill", icon = icon_edit())`
  - `actionButton(ns("seed_citation_network"), "Citation Network", class = "btn-sm btn-outline-primary flex-fill", icon = icon_share_nodes())`

  Row 2 (d-flex gap-2):
  - Export dropdown: Wrap existing `btn-group btn-group-sm` in a div with `class = "flex-fill"`. Add `w-100` class to the dropdown-toggle button. Change button class to `btn btn-outline-primary dropdown-toggle w-100`. Add " Export" text label after icon_download().
  - `actionButton(ns("refresh_search"), "Refresh", class = "btn-sm btn-outline-secondary flex-fill", icon = icon_rotate())`
  - `actionButton(ns("load_more"), "Load More", class = "btn-sm btn-outline-primary flex-fill", icon = icon_angles_down())`

  Do NOT include the `textOutput(ns("result_count"))` span in the new header — it moves to the keyword panel in Task 2.

  3. **Balance sort controls** (lines 120-134): Wrap the existing `radioButtons()` div in a flexbox container. Change the wrapping div from `class = "mb-2"` to `class = "d-flex justify-content-around mb-2"` to evenly space the 4 radio options.

  4. **Add format_large_number helper** after `format_result_count` (around line 44): Add a new function:
  ```r
  format_large_number <- function(n) {
    if (is.null(n) || is.na(n) || n <= 0) return("0")
    if (n >= 1e6) return(paste0(round(n / 1e6, 1), "M"))
    if (n >= 1e3) return(paste0(round(n / 1e3, 1), "K"))
    as.character(n)
  }
  ```

  5. **Pass remaining_count to keyword filter module**: Where `mod_keyword_filter_server` is called (search for `mod_keyword_filter_server("keyword_filter"`), add a new reactive argument `remaining_count`:
  ```r
  remaining_count <- reactive({
    total <- pagination_state$api_total
    fetched <- pagination_state$total_fetched
    if (is.null(total) || total == 0) return(NULL)
    remaining <- total - fetched
    if (remaining <= 0) return(NULL)
    remaining
  })
  ```
  Then pass it: `mod_keyword_filter_server("keyword_filter", papers_data = ..., remaining_count = remaining_count)`

  6. **Remove output$result_count render** (lines 2462-2465): Delete or comment out the `output$result_count <- renderText({...})` block since the result count moves to the keyword panel.

  Key constraints:
  - Keep all existing button IDs (ns("open_bulk_import"), ns("edit_search"), etc.) unchanged
  - Keep Export dropdown structure (downloadLink IDs, dropdown-menu items) intact
  - Keep the existing Load More show/hide logic (shinyjs::show/hide) working
  - Remove ALL title= attributes from buttons (tooltips are Phase 54)
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 10 && curl -s http://localhost:3838 > /dev/null && echo "App starts OK" || echo "FAIL"; kill %1 2>/dev/null</automated>
  </verify>
  <done>
  - Card header contains 3x2 button grid with two d-flex rows
  - All 6 buttons have icon + text labels
  - Import, Citation Network, Export, Load More use btn-outline-primary (lavender)
  - Edit Search, Refresh use btn-outline-secondary (gray)
  - "Papers" span removed from card header
  - result_count textOutput removed from card header
  - col_widths changed to c(5, 7)
  - Sort radio buttons wrapped in justify-content-around container
  - format_large_number() helper function exists
  - remaining_count reactive passed to keyword filter module
  - App starts without errors (smoke test)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add remaining count to keywords panel summary</name>
  <files>R/mod_keyword_filter.R</files>
  <action>
  Modify R/mod_keyword_filter.R to accept and display the remaining result count:

  1. **Update function signature** (line 21): Change `mod_keyword_filter_server <- function(id, papers_data)` to `mod_keyword_filter_server <- function(id, papers_data, remaining_count = reactive(NULL))`.

  2. **Update summary output** (lines 70-78): Replace the current summary rendering with:
  ```r
  output$summary <- renderUI({
    papers <- papers_data()
    keywords <- all_keywords()
    remaining <- remaining_count()

    base_text <- paste0(nrow(papers), " papers | ", nrow(keywords), " keywords")

    if (!is.null(remaining) && remaining > 0) {
      div(
        class = "mb-2 text-muted small",
        HTML(paste0(
          base_text, " | ",
          "<strong>", format_large_number(remaining), " remaining</strong>"
        ))
      )
    } else {
      div(
        class = "mb-2 text-muted small",
        base_text
      )
    }
  })
  ```

  Note: `format_large_number()` is defined in mod_search_notebook.R which is source()'d before mod_keyword_filter.R. If not accessible, define it in mod_keyword_filter.R as well, or move it to a shared utils file. The simplest approach: since both files are source()'d into the same R session, the function from mod_search_notebook.R will be available globally. Verify by checking source() order in app.R or global.R.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 10 && curl -s http://localhost:3838 > /dev/null && echo "App starts OK" || echo "FAIL"; kill %1 2>/dev/null</automated>
  </verify>
  <done>
  - mod_keyword_filter_server accepts optional remaining_count parameter
  - Keywords panel summary shows "X papers | Y keywords | **Z remaining**" when remaining count is available
  - Remaining count is bold and formatted with K/M suffixes (e.g., "1.6M remaining", "234K remaining")
  - When no remaining count (NULL or 0), summary shows original "X papers | Y keywords" without remaining
  - Existing keyword filter functionality unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restructured search notebook toolbar with 3x2 grid layout, semantic colors, icon+text labels, relocated remaining count to keywords panel, and widened paper list panel</what-built>
  <how-to-verify>
    1. Start the app and navigate to a search notebook with results
    2. Verify toolbar layout:
       - Row 1: Import | Edit Search | Citation Network (equal width)
       - Row 2: Export | Refresh | Load More (equal width)
       - Rows have tight vertical gap (~4px)
    3. Verify button labels: all 6 buttons show icon + text
    4. Verify colors:
       - Lavender outline: Import, Citation Network, Export, Load More
       - Gray outline: Edit Search, Refresh
       - If lavender + gray looks odd, note for potential all-lavender fallback
    5. Verify "Papers" label is gone from card header
    6. Verify keywords panel shows remaining count: "X papers | Y keywords | **Z remaining**"
    7. Verify paper list panel is wider (5/12 instead of 4/12)
    8. Verify abstract pane at 7/12 is not too cramped
    9. Verify sort radio buttons are evenly spaced
    10. Verify Export dropdown still works (BibTeX/CSV options)
    11. Test in both light and dark modes
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- App starts without errors (smoke test)
- All 6 toolbar buttons visible with icon+text labels
- 3x2 grid layout renders correctly
- Export dropdown functional
- Keywords panel shows remaining count when available
- Load More button still shows/hides based on pagination state
- Both light and dark modes render properly
</verification>

<success_criteria>
- TOOL-01: All 6 buttons have icon+text labels (no icon-only buttons)
- TOOL-02: Buttons ordered Import, Edit Search, Citation Network, Export, Refresh, Load More
- TOOL-03: Primary actions use lavender (btn-outline-primary), support actions use gray (btn-outline-secondary)
- TOOL-04: 3x2 grid provides visual grouping (row 1 = input/discovery, row 2 = output/data)
- TOOL-06: "Papers" label removed, result count relocated to keywords panel
- Panel widths changed to c(5, 7) for better paper title display
</success_criteria>

<output>
After completion, create `.planning/phases/53-toolbar-restructuring/53-01-SUMMARY.md`
</output>
