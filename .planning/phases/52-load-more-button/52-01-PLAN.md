---
phase: 52-load-more-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/theme_catppuccin.R
  - R/mod_search_notebook.R
  - tests/testthat/test-load-more.R
autonomous: true
requirements: [PAGE-02, PAGE-03, PAGE-04]
must_haves:
  truths:
    - "Load More button visible in toolbar after Refresh button"
    - "Clicking Load More appends next page of results to paper list"
    - "Load More button disabled when pagination_state$has_more is FALSE"
    - "Load More button disabled during fetch (prevents double-click)"
    - "Button icon swaps to spinner during fetch, then restores"
    - "Success toast shows count of newly loaded papers"
    - "Error during fetch shows error toast and re-enables button"
  artifacts:
    - path: "R/theme_catppuccin.R"
      provides: "icon_angles_down() wrapper function"
      contains: "icon_angles_down"
    - path: "R/mod_search_notebook.R"
      provides: "Load More button UI, do_load_more() function, enable/disable observer"
      contains: "load_more"
    - path: "tests/testthat/test-load-more.R"
      provides: "Unit tests for Load More behavior"
      contains: "test_that"
  key_links:
    - from: "R/mod_search_notebook.R (load_more observer)"
      to: "search_papers() in R/api_openalex.R"
      via: "cursor parameter from pagination_state$cursor"
      pattern: "search_papers.*cursor.*pagination_state"
    - from: "R/mod_search_notebook.R (enable/disable observer)"
      to: "pagination_state$has_more"
      via: "observe() watching reactive"
      pattern: "pagination_state\\$has_more"
    - from: "R/mod_search_notebook.R (load_more observer)"
      to: "paper_refresh reactiveVal"
      via: "triggers paper list re-read after append"
      pattern: "paper_refresh\\("
---

<objective>
Add a Load More button to the search notebook toolbar that fetches the next page of OpenAlex results and appends them to the existing paper list.

Purpose: Users can accumulate larger result sets without losing context. Pagination state from Phase 51 already tracks cursor/has_more — this phase adds the UI trigger and append logic.
Output: Working Load More button with loading feedback, deduplication, and proper state management.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-load-more-button/52-CONTEXT.md
@.planning/phases/52-load-more-button/52-RESEARCH.md
@.planning/phases/51-pagination-state-management/51-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/mod_search_notebook.R (line 377-383) — pagination state:
```r
pagination_state <- reactiveValues(
  cursor = NULL,          # NULL = page 1, string = continuation token
  has_more = FALSE,       # TRUE if next_cursor was non-NULL in last response
  total_fetched = 0L,     # Total papers in notebook (from DB count)
  api_total = 0L          # Total matching papers from OpenAlex meta.count
)
```

From R/mod_search_notebook.R (line 367-368) — processing state:
```r
paper_refresh <- reactiveVal(0)
is_processing <- reactiveVal(FALSE)
```

From R/mod_search_notebook.R (line 108-111) — toolbar insertion point:
```r
actionButton(ns("refresh_search"), "Refresh",
             class = "btn-sm btn-outline-secondary",
             icon = icon_rotate()),
span(class = "text-muted small ms-2 align-self-center", textOutput(ns("result_count"), inline = TRUE))
```

From R/mod_search_notebook.R (line 2225) — do_search_refresh signature:
```r
do_search_refresh <- function() {
  # Gets nb_id, nb, cfg internally
  # Calls search_papers(..., cursor = NULL)
  # Filters excluded papers, saves to DB with dedup
  # Updates pagination_state and paper_refresh
}
```

From R/theme_catppuccin.R (line 352, 467) — icon wrapper pattern:
```r
icon_spinner <- function(...) shiny::icon("spinner", ...)
icon_rotate <- function(...) shiny::icon("rotate", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Load More button UI, server logic, and icon wrapper</name>
  <files>R/theme_catppuccin.R, R/mod_search_notebook.R</files>
  <action>
**Step 1: Add icon_angles_down() to R/theme_catppuccin.R**

Add after the existing `icon_rotate()` function (around line 467):

```r
#' Angles down icon (double chevron down — load more)
#' @param ... Additional arguments passed to shiny::icon()
#' @return Icon tag
icon_angles_down <- function(...) shiny::icon("angles-down", ...)
```

**Step 2: Add Load More button to toolbar in R/mod_search_notebook.R**

In the UI function, insert the Load More button AFTER the Refresh button (line 110) and BEFORE the result count span (line 111). The button goes inside the existing `div(class = "d-flex gap-2", ...)` container:

```r
actionButton(ns("load_more"), "Load More",
             class = "btn-sm btn-outline-info",
             icon = icon_angles_down(),
             title = "Fetch next page of search results"),
```

**Step 3: Create do_load_more() function in server**

Add a new local function AFTER do_search_refresh() (after line 2370). This function reuses the same search/filter/save pattern but with append semantics:

```r
do_load_more <- function() {
  nb_id <- notebook_id()
  req(nb_id)
  req(pagination_state$has_more)
  req(!is_processing())

  is_processing(TRUE)

  # Swap icon to spinner
  updateActionButton(session, "load_more", icon = icon_spinner())

  on.exit({
    # Always restore icon and processing state
    updateActionButton(session, "load_more", icon = icon_angles_down())
    is_processing(FALSE)
  })

  nb <- get_notebook(con(), nb_id)
  req(nb$type == "search")

  cfg <- config()
  newly_added <- 0L

  tryCatch({
    withProgress(message = "Loading more papers...", value = 0, {
      email <- get_setting(cfg, "openalex", "email") %||% ""
      api_key <- get_setting(cfg, "openalex", "api_key")

      filters <- if (!is.na(nb$search_filters) && nchar(nb$search_filters) > 0) {
        tryCatch(jsonlite::fromJSON(nb$search_filters), error = function(e) list())
      } else {
        list()
      }

      abstracts_count <- get_setting(cfg, "app", "abstracts_per_search") %||% 25

      incProgress(0.2, detail = "Fetching next page")

      result <- search_papers(
        nb$search_query,
        email,
        api_key,
        from_year = filters$from_year,
        to_year = filters$to_year,
        per_page = abstracts_count,
        search_field = filters$search_field %||% "default",
        is_oa = filters$is_oa %||% FALSE,
        min_citations = filters$min_citations,
        exclude_retracted = if (!is.null(filters$exclude_retracted)) filters$exclude_retracted else TRUE,
        work_types = filters$work_types,
        cursor = pagination_state$cursor
      )

      # Update pagination state
      pagination_state$cursor <- result$next_cursor
      pagination_state$has_more <- !is.null(result$next_cursor)
      pagination_state$api_total <- result$count

      papers <- result$papers

      if (length(papers) == 0) {
        showNotification("No more papers found", type = "message")
        return()
      }

      incProgress(0.4, detail = paste("Found", length(papers), "papers"))

      # Filter out excluded papers
      excluded_ids <- tryCatch({
        if (!is.na(nb$excluded_paper_ids) && nchar(nb$excluded_paper_ids) > 0) {
          jsonlite::fromJSON(nb$excluded_paper_ids)
        } else {
          character()
        }
      }, error = function(e) character())

      if (length(excluded_ids) > 0) {
        papers <- Filter(function(p) !(p$paper_id %in% excluded_ids), papers)
        if (length(papers) == 0) {
          showNotification("All papers were previously excluded", type = "warning")
          return()
        }
      }

      incProgress(0.6, detail = "Saving papers...")

      # Save papers with dedup (same pattern as do_search_refresh)
      for (paper in papers) {
        existing <- dbGetQuery(con(), "
          SELECT id FROM abstracts WHERE notebook_id = ? AND paper_id = ?
        ", list(nb_id, paper$paper_id))

        if (nrow(existing) > 0) next

        create_abstract(
          con(), nb_id, paper$paper_id, paper$title,
          paper$authors, paper$abstract,
          paper$year, paper$venue, paper$pdf_url,
          keywords = paper$keywords,
          work_type = paper$work_type,
          work_type_crossref = paper$work_type_crossref,
          oa_status = paper$oa_status,
          is_oa = paper$is_oa,
          cited_by_count = paper$cited_by_count,
          referenced_works_count = paper$referenced_works_count,
          fwci = paper$fwci,
          doi = paper$doi
        )

        newly_added <- newly_added + 1L
      }

      incProgress(1.0, detail = "Done")
    })

    # Trigger paper list re-read
    paper_refresh(paper_refresh() + 1)

    # Update total_fetched from actual DB count
    total_in_nb <- nrow(list_abstracts(con(), nb_id))
    pagination_state$total_fetched <- total_in_nb

    if (newly_added > 0L) {
      showNotification(
        paste0("Loaded ", newly_added, " more paper", if (newly_added != 1L) "s" else "",
               " (", total_in_nb, " total)"),
        type = "message"
      )
    } else {
      showNotification("No new papers (all duplicates)", type = "message")
    }
  }, error = function(e) {
    if (inherits(e, "api_error")) {
      show_error_toast(e$message, e$details, e$severity)
    } else {
      err <- classify_api_error(e, "OpenAlex")
      show_error_toast(err$message, err$details, err$severity)
    }
    showNotification("Failed to load more — try again", type = "error")
  })
}
```

**Step 4: Add Load More click observer**

Add AFTER the existing refresh_search observer (after line 2375):

```r
# Load More button
observeEvent(input$load_more, {
  do_load_more()
}, ignoreInit = TRUE, ignoreNULL = TRUE)
```

**Step 5: Add enable/disable observer for Load More button**

Add AFTER the result_count output (after line 2411). Use shinyjs::disable/enable:

```r
# Enable/disable Load More based on pagination state
observe({
  has_more <- pagination_state$has_more
  processing <- is_processing()
  if (has_more && !processing) {
    shinyjs::enable("load_more")
  } else {
    shinyjs::disable("load_more")
  }
})
```

**Important notes:**
- The `on.exit()` in do_load_more() ensures icon and processing state are always restored, even on error (Pitfall 2 from research).
- The `req(!is_processing())` guard prevents race conditions from rapid clicks (Pitfall 3).
- Papers go through DB for deduplication (never client-side append per anti-pattern guidance).
- The `newly_added` counter uses `<<-` inside the for loop since it's modifying the enclosing scope variable within withProgress. Use `<-` if the for loop is at the same scope level as the variable declaration — check the actual scope when implementing.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3842, launch.browser=FALSE)" &amp; sleep 15 &amp;&amp; kill %1</automated>
    Smoke test: app starts without errors. Load More button visible in toolbar.
  </verify>
  <done>
    - icon_angles_down() exists in theme_catppuccin.R
    - Load More button in toolbar UI with btn-outline-info, icon, and tooltip
    - do_load_more() function calls search_papers with pagination_state$cursor
    - Click observer wired to do_load_more()
    - Enable/disable observer watches pagination_state$has_more and is_processing
    - Button shows spinner during fetch, restores angles-down after
    - Success/error toasts display appropriate messages
    - App starts without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for Load More logic</name>
  <files>tests/testthat/test-load-more.R</files>
  <behavior>
    - Test 1: icon_angles_down() returns a shiny.tag with "angles-down" icon name
    - Test 2: icon_angles_down() accepts additional arguments (class, style)
    - Test 3: Verify do_load_more scope — newly_added counter increments correctly (if extractable as unit)
  </behavior>
  <action>
Create `tests/testthat/test-load-more.R` with unit tests:

1. **icon_angles_down tests:** Verify the icon wrapper returns correct HTML tag structure. Pattern: test that the returned tag contains "fa-angles-down" class (Font Awesome class name). Match the existing test style in test-pagination-state.R.

2. **Icon accepts arguments:** Verify `icon_angles_down(class = "fa-spin")` includes the extra class.

Note: The core Load More logic (do_load_more) is deeply embedded in Shiny reactive context and cannot be unit tested without shinytest2. Focus tests on the icon helper and any pure functions. The integration behavior (button click -> API call -> append -> state update) is verified by the smoke test in Task 1 and manual UAT.

Run with: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-load-more.R')"`
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-load-more.R')"</automated>
  </verify>
  <done>
    - test-load-more.R exists with passing tests
    - icon_angles_down returns correct shiny.tag
    - No regressions in existing test suite
  </done>
</task>

</tasks>

<verification>
1. App smoke test: starts without errors on port 3842
2. Unit tests: `testthat::test_file('tests/testthat/test-load-more.R')` passes
3. Full test suite: no regressions (315 pass, 13 pre-existing failures)
4. Visual check: Load More button appears in toolbar with sapphire color, angles-down icon
</verification>

<success_criteria>
- Load More button visible in toolbar after Refresh, styled btn-outline-info with angles-down icon
- Button starts disabled (has_more=FALSE before first search)
- After a search, if has_more=TRUE, button becomes enabled
- Clicking Load More fetches next page via cursor and appends papers to notebook
- Button shows spinner during fetch, disables to prevent double-click
- Success toast confirms count, error toast on failure
- Button re-disables when has_more becomes FALSE (last page)
</success_criteria>

<output>
After completion, create `.planning/phases/52-load-more-button/52-01-SUMMARY.md`
</output>
