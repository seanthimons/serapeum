---
phase: 34-openalex-batch-api-support
plan: 02
type: tdd
wave: 2
depends_on: ["34-01"]
files_modified:
  - R/api_openalex.R
  - tests/testthat/test-batch-openalex.R
autonomous: true
requirements:
  - "Foundation for BULK-04"
  - "Foundation for BULK-05"
  - "Foundation for AUDIT-02"
  - "Foundation for AUDIT-03"
  - "Foundation for AUDIT-06"

must_haves:
  truths:
    - "batch_fetch_papers() accepts a vector of bare DOIs and returns papers + errors"
    - "DOIs are queried in batches of up to 50 using pipe-separated filter syntax"
    - "0.1s delay between batches (configurable via delay parameter)"
    - "429 errors trigger exponential backoff (1s, 2s, 4s) with 3 retries"
    - "Missing DOIs categorized as not_found with original DOI string"
    - "Failed batches categorized as api_error after 3 retries"
    - "Results deduplicated by OpenAlex work ID (paper_id)"
    - "progress_callback receives batch_current, batch_total, found_so_far, not_found_so_far"
    - "parse=TRUE returns normalized objects; parse=FALSE returns raw work objects"
    - "Partial results returned even when some batches fail"
  artifacts:
    - path: "R/api_openalex.R"
      provides: "batch_fetch_papers(), fetch_single_batch(), chunk_dois()"
      contains: "batch_fetch_papers"
    - path: "tests/testthat/test-batch-openalex.R"
      provides: "Batch operation tests with mocked HTTP"
      min_lines: 150
  key_links:
    - from: "R/api_openalex.R"
      to: "batch_fetch_papers"
      via: "function definition"
      pattern: "batch_fetch_papers"
    - from: "R/api_openalex.R"
      to: "parse_openalex_work"
      via: "internal call from fetch_single_batch"
      pattern: "parse_openalex_work"
    - from: "R/api_openalex.R"
      to: "build_openalex_request"
      via: "internal call from fetch_single_batch"
      pattern: "build_openalex_request"
---

<objective>
Implement batch_fetch_papers() — the core batch DOI lookup function that queries OpenAlex in chunks of 50, with rate limiting, exponential backoff, error categorization, and progress reporting.

Purpose: This is the transport layer for bulk import (Phase 35) and citation audit (Phase 37). It turns a list of DOIs into structured results with categorized errors.
Output: Working batch_fetch_papers() function with full test coverage.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
@C:/Users/sxthi/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-openalex-batch-api-support/34-01-SUMMARY.md
@R/api_openalex.R
@R/utils_doi.R

<interfaces>
<!-- Existing functions this plan builds on -->
From R/api_openalex.R:
```r
build_openalex_request <- function(endpoint, email = NULL, api_key = NULL)
# Returns httr2 request object with base URL, mailto, api_key, 30s timeout

parse_openalex_work <- function(work)
# Returns list with 21 fields (after Plan 01): paper_id, title, authors, abstract,
# year, venue, publisher, doi, cited_by_count, pdf_url, keywords, work_type,
# work_type_crossref, oa_status, is_oa, referenced_works_count, referenced_works,
# fwci, is_retracted, cited_by_percentile, topics

OPENALEX_BASE_URL <- "https://api.openalex.org"
```

From R/utils_doi.R:
```r
normalize_doi_bare <- function(doi)
# Returns bare DOI "10.xxxx/yyyy" or NA_character_

parse_doi_list <- function(input, delimiter = NULL)
# Returns list(valid = character(), invalid = list(), duplicates_removed = integer())
```

OpenAlex API batch query format:
- URL: GET /works?filter=doi:10.xxxx/a|10.yyyy/b|10.zzzz/c&per_page=50
- Response: { results: [...works], meta: { count, per_page, page } }
- Max 50 DOIs per pipe-separated filter (CONTEXT.md decision)
- Max URL length 4096 chars (use bare DOIs to stay within)
- Rate: 100 req/sec, $1/day budget for free tier
- 429 on over-budget: check Retry-After header
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for batch_fetch_papers</name>
  <files>tests/testthat/test-batch-openalex.R</files>
  <action>
Create a new test file `tests/testthat/test-batch-openalex.R`. Source both `R/api_openalex.R` and `R/utils_doi.R`.

Write tests using mocked HTTP responses. Use httr2's testing support or simply mock at the function level by temporarily replacing functions. The simplest approach for this codebase (which doesn't use httptest or webmockr) is to test the helper functions directly and use local mocking for the HTTP parts.

**Test blocks to create:**

1. **chunk_dois() tests:**
   - 5 DOIs with batch_size=2 → 3 chunks (2,2,1)
   - 3 DOIs with batch_size=50 → 1 chunk of 3
   - 0 DOIs → error or empty list
   - 1 DOI → 1 chunk of 1

2. **build_batch_filter() tests:** (helper that builds the pipe-separated filter string)
   - Vector of 3 bare DOIs → "doi:10.aaa/x|10.bbb/y|10.ccc/z"
   - Single DOI → "doi:10.aaa/x"

3. **match_results_to_dois() tests:** (helper that matches API results back to input DOIs)
   - 3 input DOIs, 2 found in results → matched list with 2 found, 1 not_found
   - DOI case mismatch (input lowercase, result has mixed case) → still matches
   - Duplicate OpenAlex results (same paper_id) → deduplicated

4. **batch_fetch_papers() integration tests (with mocked fetch_single_batch):**
   - 5 DOIs, batch_size=2 → calls fetch_single_batch 3 times, returns combined results
   - progress_callback receives correct batch_current and batch_total
   - One batch fails → other batches still succeed, failed DOIs marked as api_error
   - parse=FALSE → returns raw work objects instead of parsed

5. **Error categorization tests:**
   - DOI not in OpenAlex results → categorized as "not_found"
   - Batch HTTP error → all DOIs in batch categorized as "api_error"

Run: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-batch-openalex.R')"`
Tests MUST FAIL because batch_fetch_papers() and helpers don't exist yet.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-batch-openalex.R')"</automated>
    Tests must FAIL (functions not yet defined). No syntax errors in test file.
  </verify>
  <done>Test file created with comprehensive test coverage for chunking, filtering, matching, batch orchestration, and error categorization</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement batch_fetch_papers and helpers</name>
  <files>R/api_openalex.R</files>
  <action>
Add the following functions to R/api_openalex.R (at the end of the file, after existing functions):

**1. chunk_dois(dois, batch_size = 50)**
```r
chunk_dois <- function(dois, batch_size = 50) {
  if (length(dois) == 0) return(list())
  split(dois, ceiling(seq_along(dois) / batch_size))
}
```

**2. build_batch_filter(dois)**
Build pipe-separated DOI filter string for OpenAlex API.
```r
build_batch_filter <- function(dois) {
  paste0("doi:", paste(dois, collapse = "|"))
}
```
- Input: vector of bare DOIs (10.xxxx/yyyy format)
- Output: "doi:10.xxxx/a|10.yyyy/b"

**3. match_results_to_dois(works, input_dois)**
Match returned works to input DOIs, identify not_found.
```r
match_results_to_dois <- function(works, input_dois) {
  # Extract bare DOIs from returned works (OpenAlex returns https://doi.org/ prefix)
  found_dois <- vapply(works, function(w) {
    tolower(gsub("^https://doi.org/", "", w$doi %||% ""))
  }, character(1))

  input_lower <- tolower(input_dois)
  matched <- input_lower %in% found_dois

  not_found <- lapply(input_dois[!matched], function(d) {
    list(doi = d, reason = "not_found", details = "DOI not found in OpenAlex")
  })

  list(found = works, not_found = not_found)
}
```

**4. fetch_single_batch(dois, email, api_key, parse)**
Fetch one batch of DOIs from OpenAlex with retry logic.
```r
fetch_single_batch <- function(dois, email, api_key = NULL, parse = TRUE) {
  filter_str <- build_batch_filter(dois)

  req <- build_openalex_request("works", email, api_key) |>
    httr2::req_url_query(filter = filter_str, per_page = length(dois)) |>
    httr2::req_retry(
      max_tries = 3,
      is_transient = function(resp) httr2::resp_status(resp) == 429,
      backoff = function(tries) 2^(tries - 1)  # 1s, 2s, 4s
    )

  resp <- httr2::req_perform(req)
  body <- httr2::resp_body_json(resp)

  if (is.null(body$results)) {
    return(match_results_to_dois(list(), dois))
  }

  works <- if (parse) {
    lapply(body$results, parse_openalex_work)
  } else {
    body$results
  }

  match_results_to_dois(works, dois)
}
```

**5. batch_fetch_papers(dois, email, api_key, batch_size, delay, parse, progress_callback, log_file)**
Main orchestrator function.
```r
batch_fetch_papers <- function(dois, email, api_key = NULL,
                                batch_size = 50, delay = 0.1,
                                parse = TRUE, progress_callback = NULL,
                                log_file = NULL) {
  stopifnot(is.character(dois), length(dois) > 0)
  stopifnot(batch_size > 0, batch_size <= 50)

  # Initialize log file if requested
  if (!is.null(log_file)) {
    dir.create(dirname(log_file), recursive = TRUE, showWarnings = FALSE)
    cat(paste0("[", Sys.time(), "] batch_fetch_papers started: ", length(dois), " DOIs in ",
               ceiling(length(dois) / batch_size), " batches\n"),
        file = log_file, append = TRUE)
  }

  # Initialize collectors
  all_papers <- list()
  all_errors <- list()

  # Chunk DOIs
  chunks <- chunk_dois(dois, batch_size)

  for (i in seq_along(chunks)) {
    batch_dois <- chunks[[i]]

    batch_msg <- paste0("[batch ", i, "/", length(chunks), "] Fetching ", length(batch_dois), " DOIs...")
    message(batch_msg)
    if (!is.null(log_file)) {
      cat(paste0("[", Sys.time(), "] ", batch_msg, "\n"), file = log_file, append = TRUE)
    }

    batch_result <- tryCatch({
      fetch_single_batch(batch_dois, email, api_key, parse)
    }, error = function(e) {
      err_msg <- conditionMessage(e)

      # Determine error category
      reason <- if (grepl("429|rate.limit", err_msg, ignore.case = TRUE)) {
        "rate_limited"
      } else {
        "api_error"
      }

      error_entries <- lapply(batch_dois, function(d) {
        list(doi = d, reason = reason, details = err_msg)
      })

      list(found = list(), not_found = error_entries)
    })

    all_papers <- c(all_papers, batch_result$found)
    all_errors <- c(all_errors, batch_result$not_found)

    # Log result
    if (!is.null(log_file)) {
      cat(paste0("[", Sys.time(), "] Batch ", i, ": found=", length(batch_result$found),
                 " not_found=", length(batch_result$not_found), "\n"),
          file = log_file, append = TRUE)
    }

    # Progress callback
    if (!is.null(progress_callback)) {
      found_count <- length(all_papers)
      not_found_count <- sum(vapply(all_errors, function(e) e$reason == "not_found", logical(1)))
      progress_callback(
        batch_current = i,
        batch_total = length(chunks),
        found_so_far = found_count,
        not_found_so_far = not_found_count
      )
    }

    # Inter-batch delay
    if (i < length(chunks)) {
      Sys.sleep(delay)
    }
  }

  # Deduplicate by paper_id
  if (parse && length(all_papers) > 0) {
    seen_ids <- character()
    unique_papers <- list()
    for (paper in all_papers) {
      if (!is.null(paper$paper_id) && !(paper$paper_id %in% seen_ids)) {
        seen_ids <- c(seen_ids, paper$paper_id)
        unique_papers <- c(unique_papers, list(paper))
      }
    }
    all_papers <- unique_papers
  }

  # Final log
  if (!is.null(log_file)) {
    cat(paste0("[", Sys.time(), "] batch_fetch_papers complete: ",
               length(all_papers), " papers, ", length(all_errors), " errors\n"),
        file = log_file, append = TRUE)
  }

  list(papers = all_papers, errors = all_errors)
}
```

Key implementation details:
- `stopifnot()` for input validation (batch_size 1-50, dois must be character vector)
- `tryCatch` around each batch — never fail the whole operation
- Classify 429-containing errors as "rate_limited", others as "api_error"
- Deduplication by paper_id using a seen set
- Dual logging: `message()` for console + `cat()` to log file
- Progress callback called after each batch with running totals
- `parse=FALSE` passes through raw work objects (for callers who need raw data)

Run all tests to confirm GREEN.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-batch-openalex.R')"</automated>
    ALL tests must PASS.
  </verify>
  <done>batch_fetch_papers() and all helpers implemented. All tests pass. Function accepts DOIs, queries OpenAlex in batches, handles errors gracefully, and returns structured results.</done>
</task>

</tasks>

<verification>
- Run batch tests: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-batch-openalex.R')"`
- Run existing OpenAlex tests: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-api-openalex.R')"`
- Verify chunk_dois() splits correctly for various sizes
- Verify match_results_to_dois() handles case differences and missing DOIs
- Verify error categorization distinguishes not_found vs api_error vs rate_limited
- Verify deduplication by paper_id works
- Verify progress_callback receives correct values
</verification>

<success_criteria>
- batch_fetch_papers() accepts a DOI vector and returns list(papers, errors)
- Batches of up to 50 DOIs using pipe-separated filter syntax
- 0.1s configurable inter-batch delay
- Exponential backoff on 429 (via httr2 req_retry)
- Three error categories: not_found, api_error, rate_limited
- Partial results on batch failures
- Deduplication by paper_id
- Progress callback works
- parse=TRUE/FALSE toggle works
- Dual logging to message() and optional log file
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-openalex-batch-api-support/34-02-SUMMARY.md`
</output>
