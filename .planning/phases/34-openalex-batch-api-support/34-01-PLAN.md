---
phase: 34-openalex-batch-api-support
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - R/api_openalex.R
  - tests/testthat/test-api-openalex.R
autonomous: true
requirements:
  - "Foundation for AUDIT-02"
  - "Foundation for AUDIT-03"

must_haves:
  truths:
    - "parse_openalex_work() returns is_retracted boolean for any work object"
    - "parse_openalex_work() returns cited_by_percentile numeric from cited_by_percentile_year"
    - "parse_openalex_work() returns topics list with id, name, and score fields"
    - "All existing fields remain unchanged (backward compatible)"
  artifacts:
    - path: "R/api_openalex.R"
      provides: "Extended parse_openalex_work() with is_retracted, cited_by_percentile, topics"
      contains: "is_retracted"
    - path: "tests/testthat/test-api-openalex.R"
      provides: "Tests for new fields"
      min_lines: 100
  key_links:
    - from: "R/api_openalex.R"
      to: "parse_openalex_work"
      via: "function extension"
      pattern: "is_retracted|cited_by_percentile|topics"
---

<objective>
Extend parse_openalex_work() to extract three new fields: is_retracted, cited_by_percentile, and topics.

Purpose: These fields are needed by citation audit (retraction detection, citation impact ranking) and future analytics. Adding them to the parser means batch_fetch_papers() (Plan 02) automatically includes them.
Output: Updated parse_openalex_work() with 3 new fields, backward-compatible, fully tested.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
@C:/Users/sxthi/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@R/api_openalex.R
@tests/testthat/test-api-openalex.R

<interfaces>
<!-- Key function signature being extended -->
From R/api_openalex.R:
```r
parse_openalex_work <- function(work) {
  # Currently returns list with: paper_id, title, authors, abstract, year, venue,
  # publisher, doi, cited_by_count, pdf_url, keywords, work_type, work_type_crossref,
  # oa_status, is_oa, referenced_works_count, referenced_works, fwci
  # ADD: is_retracted, cited_by_percentile, topics
}
```

OpenAlex work object fields to extract:
- `work$is_retracted` → boolean (TRUE/FALSE)
- `work$cited_by_percentile_year$min` → numeric (0-100 percentile)
- `work$topics` → list of objects with $id, $display_name, $score
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for new parse_openalex_work fields</name>
  <files>tests/testthat/test-api-openalex.R</files>
  <action>
Add test blocks to the existing test file for the three new fields:

1. **is_retracted tests:**
   - Mock work with `is_retracted = TRUE` → expect `result$is_retracted` is TRUE
   - Mock work with `is_retracted = FALSE` → expect `result$is_retracted` is FALSE
   - Mock work with `is_retracted = NULL` (missing) → expect `result$is_retracted` is FALSE (safe default)

2. **cited_by_percentile tests:**
   - Mock work with `cited_by_percentile_year = list(min = 85.5, max = 90.0)` → expect `result$cited_by_percentile` equals 85.5
   - Mock work with `cited_by_percentile_year = NULL` → expect `result$cited_by_percentile` is NA_real_
   - Mock work with `cited_by_percentile_year = list(min = NULL)` → expect `result$cited_by_percentile` is NA_real_

3. **topics tests:**
   - Mock work with topics list containing 2 topics with id, display_name, score → expect `result$topics` is a list of length 2, each with id (stripped of URL prefix), name, score
   - Mock work with `topics = NULL` → expect `result$topics` is empty list
   - Mock work with `topics = list()` → expect `result$topics` is empty list

4. **Backward compatibility test:**
   - Use the existing mock_work from the first test block → verify all 18 existing fields are still present and unchanged, plus the 3 new fields exist

Use the same mock object construction pattern as existing tests. Each mock needs at minimum: id, title, authorships, abstract_inverted_index, publication_year, primary_location, open_access.

Run tests: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-api-openalex.R')"`

Tests MUST FAIL (RED phase) because parse_openalex_work() doesn't return these fields yet.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-api-openalex.R')"</automated>
    Tests for new fields must FAIL. Existing tests must still PASS.
  </verify>
  <done>New test blocks written and confirmed failing for is_retracted, cited_by_percentile, and topics fields</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement new fields in parse_openalex_work</name>
  <files>R/api_openalex.R</files>
  <action>
Extend parse_openalex_work() in R/api_openalex.R to extract 3 new fields. Add the extraction logic BEFORE the final `list(...)` return, and add the fields to the return list.

**is_retracted extraction:**
```r
is_retracted <- isTRUE(work$is_retracted)
```
- `isTRUE()` handles NULL, NA, FALSE all safely → returns FALSE

**cited_by_percentile extraction:**
```r
cited_by_percentile <- NA_real_
if (!is.null(work$cited_by_percentile_year) && !is.null(work$cited_by_percentile_year$min)) {
  cited_by_percentile <- as.numeric(work$cited_by_percentile_year$min)
}
```
- Use `$min` (the lower bound of the percentile range)
- Default NA_real_ when missing

**topics extraction:**
```r
topics <- list()
if (!is.null(work$topics) && length(work$topics) > 0) {
  topics <- lapply(work$topics, function(t) {
    list(
      id = gsub("https://openalex.org/", "", t$id %||% ""),
      name = t$display_name %||% NA_character_,
      score = t$score %||% NA_real_
    )
  })
}
```
- Strip OpenAlex URL prefix from topic IDs (consistent with paper_id pattern)
- Use `%||%` for null-safe defaults (already used throughout the file)

Add all three to the return list:
```r
list(
  # ... existing 18 fields ...
  is_retracted = is_retracted,
  cited_by_percentile = cited_by_percentile,
  topics = topics
)
```

Run all tests to confirm GREEN.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-api-openalex.R')"</automated>
    ALL tests must PASS including new field tests.
  </verify>
  <done>parse_openalex_work() returns all 21 fields. All tests pass. Backward compatible — existing callers unaffected.</done>
</task>

</tasks>

<verification>
- Run full test suite: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-api-openalex.R')"`
- Verify all tests pass (both existing and new)
- Verify return list has exactly 21 named elements
</verification>

<success_criteria>
- parse_openalex_work() returns is_retracted (boolean), cited_by_percentile (numeric or NA), topics (list)
- All existing tests unchanged and passing
- New tests cover: present values, NULL/missing values, empty arrays
- No breaking changes to existing callers
</success_criteria>

<output>
After completion, create `.planning/phases/34-openalex-batch-api-support/34-01-SUMMARY.md`
</output>
