---
phase: 56-year-slider-alignment-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_search_notebook.R
autonomous: false
requirements:
  - YEAR-01
must_haves:
  truths:
    - "Year histogram bars align edge-to-edge with the slider track (no horizontal offset)"
    - "Histogram bars auto-switch color between light and dark mode without R-side logic"
    - "Year filter panel (slider + histogram + unknown year checkbox) hidden when no papers exist"
    - "Slider min/max is fully data-driven from actual paper years (no hardcoded floor)"
  artifacts:
    - path: "R/mod_search_notebook.R"
      provides: "HTML div histogram replacing ggplot2 plotOutput, conditionalPanel for empty state"
      contains: "year-histogram-bars"
  key_links:
    - from: "R/mod_search_notebook.R (renderUI)"
      to: "R/db.R (get_year_distribution)"
      via: "DB query for year counts fed into HTML div bars"
      pattern: "get_year_distribution"
    - from: "R/mod_search_notebook.R (histogram bars)"
      to: "Bootstrap CSS variables"
      via: "var(--bs-primary) for automatic dark mode color switching"
      pattern: "var\\(--bs-primary\\)"
---

<objective>
Replace ggplot2 histogram with HTML div bars for pixel-perfect alignment with the year range slider, add conditional visibility for empty state, and ensure dark mode compatibility via Bootstrap CSS variables.

Purpose: Fix visual misalignment between histogram and slider (issue #143) caused by ggplot2 internal padding differing from Shiny sliderInput layout.
Output: Modified R/mod_search_notebook.R with HTML histogram and conditional panel.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-year-slider-alignment-fix/56-CONTEXT.md
@.planning/phases/56-year-slider-alignment-fix/56-RESEARCH.md

@R/mod_search_notebook.R
@R/db.R
</context>

<interfaces>
<!-- Key functions from R/db.R the executor needs -->

From R/db.R:
```r
get_year_distribution <- function(con, notebook_id)
# Returns data.frame with columns: year (integer), count (integer)
# Returns empty data.frame if no papers with years

get_year_bounds <- function(con, notebook_id)
# Returns list(min_year, max_year) with COALESCE defaults (2000, 2026)

get_unknown_year_count <- function(con, notebook_id)
# Returns integer count of NULL-year papers
```

From R/mod_search_notebook.R current UI (lines 230-254):
```r
# Year range filter panel
div(
  class = "mb-2",
  sliderInput(ns("year_range"), "Publication Year", min = 1900, max = 2026, ...),
  plotOutput(ns("year_histogram"), height = "60px"),
  div(
    class = "d-flex justify-content-between align-items-center",
    checkboxInput(ns("include_unknown_year"), ...),
    textOutput(ns("unknown_year_count"), inline = TRUE) |> tagAppendAttributes(...)
  )
)
```

From R/mod_search_notebook.R current server (lines 1158-1179):
```r
output$year_histogram <- renderPlot({
  # ... uses ggplot2::geom_col with LATTE$lavender
}, bg = "transparent")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace ggplot2 histogram with HTML div bars and add conditional visibility</name>
  <files>R/mod_search_notebook.R</files>
  <action>
Two changes in R/mod_search_notebook.R:

**UI section (lines ~230-254):** Replace the year range filter panel div:

1. Wrap the entire year filter panel in `conditionalPanel(condition = "output.has_papers", ns = ns, ...)` so slider + histogram + unknown year checkbox are all hidden when no papers exist.

2. Inside the panel, replace `plotOutput(ns("year_histogram"), height = "60px")` with `uiOutput(ns("year_histogram"))`. Keep the sliderInput and unknown year checkbox/count as-is.

**Server section (lines ~1137-1179):** Two changes:

1. Replace the `renderPlot` block (lines 1158-1179) with a `renderUI` block:
   - Call `get_year_distribution(con(), nb_id)` (same as current)
   - If `nrow(year_counts) == 0`, return `div(style = "height: 60px; width: 100%;")`
   - Otherwise: normalize heights as `(count / max(count)) * 100` for percentage
   - Build a flexbox container: `div(class = "year-histogram-bars", style = "display: flex; align-items: flex-end; height: 60px; width: 100%; gap: 1px;", ...)`
   - Each bar: `div(style = paste0("flex: 1; height: ", height_pct, "%; background-color: var(--bs-primary); border-radius: 2px 2px 0 0;"))`
   - No title attributes on bars (locked decision: visual only, no tooltips/interactivity)
   - Remove the `, bg = "transparent"` argument (not applicable to renderUI)

2. Add a reactive output flag for the conditionalPanel:
   ```r
   output$has_papers <- reactive({
     nb_id <- notebook_id()
     req(nb_id)
     paper_refresh()
     year_counts <- get_year_distribution(con(), nb_id)
     nrow(year_counts) > 0
   })
   outputOptions(output, "has_papers", suspendWhenHidden = FALSE)
   ```

**Important:** Do NOT modify the slider bounds observer (lines 1137-1151) — it already uses data-driven bounds from `get_year_bounds()`. Do NOT touch citation network year filter. Do NOT add hover tooltips to histogram bars (per locked decision: visual only).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" &amp; sleep 8 &amp;&amp; curl -s http://localhost:3838 > /dev/null &amp;&amp; echo "App starts OK" || echo "FAIL"; kill %1 2>/dev/null</automated>
  </verify>
  <done>
    - plotOutput replaced with uiOutput for year_histogram
    - renderPlot replaced with renderUI emitting flexbox div bars with var(--bs-primary)
    - Year filter panel wrapped in conditionalPanel tied to has_papers output
    - App starts without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify histogram-slider alignment in light and dark modes</name>
  <what-built>HTML div histogram replacing ggplot2 plot, with conditional visibility and dark mode support</what-built>
  <how-to-verify>
    1. Open the app and navigate to Search Notebook
    2. Open a notebook with papers — verify:
       a. Histogram bars appear directly above the slider track
       b. Bars and slider are edge-to-edge aligned (no horizontal offset)
       c. Bar heights reflect year distribution (taller = more papers)
    3. Toggle dark mode — verify:
       a. Histogram bar color switches automatically (lavender shade changes)
       b. No visual glitches or missing bars
    4. Create a new empty notebook — verify:
       a. Year filter panel (slider + histogram + unknown year) is completely hidden
       b. No empty plot placeholder or layout gap
    5. Add papers to the empty notebook — verify:
       a. Year filter panel appears with correct slider bounds and histogram
  </how-to-verify>
  <resume-signal>Type "approved" or describe alignment issues</resume-signal>
</task>

</tasks>

<verification>
- App starts without errors (Shiny smoke test)
- No ggplot2 references remain in year histogram code path
- Histogram bars use var(--bs-primary) for automatic dark mode switching
- Year filter panel hidden when notebook has no papers
- Slider bounds remain data-driven (no hardcoded floor)
</verification>

<success_criteria>
- Year histogram bars visually align with slider track (no horizontal offset)
- Dark mode color switching works via CSS variables (no R-side theme detection)
- Empty notebooks show no year filter panel
- Existing year filter functionality (debounced slider, unknown year checkbox) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/56-year-slider-alignment-fix/56-01-SUMMARY.md`
</output>
