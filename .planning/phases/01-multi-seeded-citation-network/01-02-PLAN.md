---
phase: 01-multi-seeded-citation-network
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - R/mod_citation_network.R
  - R/mod_search_notebook.R
  - R/mod_bulk_import.R
  - app.R
autonomous: true
requirements:
  - MSEED-03
  - MSEED-04
  - MSEED-06

must_haves:
  truths:
    - "Citation network module accepts multiple seed IDs and builds network using multi-seed BFS"
    - "Search notebook toolbar has Seed Network button that sends all papers to citation network tab"
    - "BibTeX import Seed Citation Network button sends imported papers to citation network tab"
    - "Clicking either seed button auto-switches to Citation Network view without auto-building"
    - "Legend shows star/diamond/dot shapes"
  artifacts:
    - path: "R/mod_citation_network.R"
      provides: "Refactored module with multi-seed reactive state and ExtendedTask"
    - path: "R/mod_search_notebook.R"
      provides: "Seed Network toolbar button"
    - path: "R/mod_bulk_import.R"
      provides: "Wired Seed Citation Network button (replaces stub)"
    - path: "app.R"
      provides: "Communication bridge between notebook/import modules and citation network"
  key_links:
    - from: "R/mod_search_notebook.R"
      to: "app.R seed callback"
      via: "Return value from module server with seed_request reactive"
      pattern: "seed_request|seed_network_callback"
    - from: "R/mod_bulk_import.R"
      to: "app.R seed callback"
      via: "Return value or callback from module server"
      pattern: "seed_network|seed_callback"
    - from: "app.R"
      to: "R/mod_citation_network.R"
      via: "current_network reactiveVal + set_seeds return value"
      pattern: "set_seeds|current_seed_ids"
    - from: "R/mod_citation_network.R:network_task"
      to: "R/citation_network.R:fetch_multi_seed_citation_network"
      via: "mirai call in ExtendedTask"
      pattern: "fetch_multi_seed_citation_network"
---

<objective>
Refactor the citation network module for multi-seed support and wire entry points (search notebook + BibTeX import).

Purpose: Connect the multi-seed BFS engine (Plan 01) to the UI. Users can now seed the citation network from their search notebook papers or BibTeX import results, and the network module builds using all seeds.

Output: Working multi-seed network builds from two entry points, with updated legend showing star/diamond/dot shapes.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-seeded-citation-network/01-CONTEXT.md
@.planning/phases/01-multi-seeded-citation-network/01-RESEARCH.md
@.planning/phases/01-multi-seeded-citation-network/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From R/citation_network.R (new multi-seed function):
```r
fetch_multi_seed_citation_network <- function(
  seed_paper_ids,        # character vector of OpenAlex Work IDs
  email, api_key = NULL,
  direction = "both", depth = 2,
  node_limit_per_seed = 100,
  interrupt_flag = NULL,
  progress_file = NULL
)
# Returns: list(nodes = data.frame, edges = data.frame, partial = logical)
# nodes has: paper_id, title, authors, year, venue, doi, cited_by_count, is_seed, is_overlap
```

From R/citation_network.R (updated build):
```r
build_network_data <- function(nodes_df, edges_df, palette = "viridis", seed_paper_id = NULL)
# seed_paper_id can be single ID or vector of IDs
# Returns: list(nodes, edges) with shape = star/diamond/dot
```

From R/db.R (updated save/load):
```r
save_network <- function(con, id = NULL, name, seed_paper_id, seed_paper_title,
                          direction, depth, node_limit, palette, nodes_df, edges_df,
                          seed_paper_ids = NULL, source_notebook_id = NULL)
load_network <- function(con, network_id)
# metadata now includes: seed_paper_ids (character vector), source_notebook_id
```

From app.R (existing communication pattern):
```r
current_network <- reactiveVal(NULL)  # Set to paper_id or network UUID
current_view <- reactiveVal("welcome")  # Set to "network" to switch tabs
```

From R/mod_citation_network.R (current module return):
```r
list(
  set_seed = function(paper_id) {
    current_seed_id(paper_id)
  }
)
```

From R/mod_search_notebook.R (toolbar location, line ~70-97):
```r
# Papers card_header has: Import DOIs, Export dropdown, Edit Search, Refresh buttons
# New Seed Network button goes in this button group
```

From R/mod_bulk_import.R (stub at line 810-815):
```r
observeEvent(input$seed_network, {
  showNotification("Citation network seeding will be available...", ...)
  removeModal()
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor mod_citation_network.R for multi-seed support</name>
  <files>R/mod_citation_network.R</files>
  <action>
  1. **Reactive state changes:**
     - Replace `current_seed_id <- reactiveVal(NULL)` with `current_seed_ids <- reactiveVal(character())` (character vector)
     - Add `source_notebook_id <- reactiveVal(NULL)` to track originating notebook
     - Keep backward compat: when a single paper_id comes in via `network_id_r()` (starts with "W"), set `current_seed_ids(paper_id)` (vector of length 1) and `source_notebook_id(NULL)`

  2. **Update ExtendedTask definition (around line 193):**
     - Change parameter from `seed_id` to `seed_ids`
     - In mirai block: call `fetch_multi_seed_citation_network(seed_ids, email, ...)` instead of `fetch_citation_network(seed_id, ...)`
     - Source `citation_network.R` already happens — `fetch_multi_seed_citation_network` will be available
     - Update mirai parameter passing: `seed_ids = seed_ids`

  3. **Update build button handler (around line 360):**
     - Change `req(current_seed_id())` to `req(length(current_seed_ids()) > 0)`
     - Update `network_task$invoke()` call:
       - `seed_ids = current_seed_ids()` (vector)
       - `node_limit` param rename to `node_limit_per_seed = input$node_limit`
     - Progress modal title: if multiple seeds, show "Building Multi-Seed Citation Network" else keep "Building Citation Network"

  4. **Update result handler (around line 460-505):**
     - Change `seed_id <- current_seed_id()` to `seed_ids <- current_seed_ids()`
     - Update `build_network_data()` call: pass `seed_paper_id = seed_ids` (vector)
     - Update metadata storage:
       ```r
       metadata = list(
         seed_paper_id = seed_ids[1],  # backward compat
         seed_paper_ids = seed_ids,
         seed_paper_title = viz_data$nodes$paper_title[viz_data$nodes$is_seed][1],
         source_notebook_id = source_notebook_id(),
         direction = input$direction,
         depth = input$depth,
         node_limit = input$node_limit,
         palette = palette,
         partial = isTRUE(result$partial)
       )
       ```
     - Update notification: show seed count in message ("Network built: X papers from Y seeds")

  5. **Update explore-from-node handler (around line 877-888):**
     - `current_seed_ids(node_id)` (single-element vector) — exploring from a node resets to single-seed mode
     - `source_notebook_id(NULL)` — no longer associated with a notebook

  6. **Update save handler (around line 910-937):**
     - Pass `seed_paper_ids = net_data$metadata$seed_paper_ids` and `source_notebook_id = net_data$metadata$source_notebook_id` to `save_network()`

  7. **Update load handler (around line 952-1001):**
     - When loading, use `loaded$metadata$seed_paper_ids` if available, else fall back to `c(loaded$metadata$seed_paper_id)`
     - Set `current_seed_ids(loaded_seed_ids)` and `source_notebook_id(loaded$metadata$source_notebook_id)`
     - Pass vector to `build_network_data()`

  8. **Update module return value (around line 1010-1015):**
     - Change from `set_seed` to `set_seeds`:
       ```r
       list(
         set_seeds = function(seed_ids, notebook_id = NULL) {
           current_seed_ids(seed_ids)
           source_notebook_id(notebook_id)
         }
       )
       ```
     - Keep `set_seed` as alias for backward compat: `set_seed = function(paper_id) { current_seed_ids(paper_id); source_notebook_id(NULL) }`

  9. **Update legend (around line 149-157):**
     - Add diamond entry between star and existing items:
       ```r
       div(
         class = "mt-2",
         icon("star", class = "text-warning"), " = Seed Paper", br(),
         icon("diamond", class = "text-info"), " = Multi-Seed Overlap", br(),
         icon("circle", class = "text-muted"), " = Regular Paper"
       )
       ```
     - Note: Font Awesome `diamond` icon exists. Use it. If not available, use the Unicode diamond character.

  AVOID: Don't change the `network_id_r` observe handler for single paper_id loads (line 958-960) — that's the existing sidebar seed flow. Just ensure it calls `current_seed_ids(c(network_id))` instead of `current_seed_id(network_id)`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "tryCatch({ source('R/mod_citation_network.R'); cat('Module loaded OK\n') }, error = function(e) { cat('ERROR:', e$message, '\n'); quit(status=1) })"</automated>
  </verify>
  <done>
  - `current_seed_ids` replaces `current_seed_id` throughout the module
  - `source_notebook_id` reactive tracks originating notebook
  - ExtendedTask calls `fetch_multi_seed_citation_network` with vector of seed IDs
  - Build/result handlers use multi-seed metadata
  - Legend shows star/diamond/dot
  - Module returns `set_seeds(seed_ids, notebook_id)` function
  - Single-seed usage still works (vector of length 1)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire entry points — search notebook button, BibTeX import button, app.R bridge</name>
  <files>R/mod_search_notebook.R, R/mod_bulk_import.R, app.R</files>
  <action>
  1. **Search notebook — add Seed Network button (R/mod_search_notebook.R, around line 70-97):**
     - Add a new button in the toolbar `div(class = "d-flex gap-2", ...)` section, after the Export dropdown and before Edit:
       ```r
       actionButton(ns("seed_citation_network"), NULL,
                    class = "btn-sm btn-outline-info",
                    icon = icon("share-nodes"),
                    title = "Seed Citation Network")
       ```
     - Add server handler:
       ```r
       seed_request <- reactiveVal(NULL)

       observeEvent(input$seed_citation_network, {
         nb_id <- notebook_id()
         req(nb_id)
         con <- con_r()
         papers <- dbGetQuery(con, "SELECT paper_id FROM abstracts WHERE notebook_id = ?", list(nb_id))
         if (nrow(papers) == 0) {
           showNotification("No papers in notebook to seed network", type = "warning")
           return()
         }
         seed_request(list(
           seed_ids = papers$paper_id,
           source_notebook_id = nb_id,
           timestamp = Sys.time()
         ))
         showNotification(paste("Seeding network with", nrow(papers), "papers"), type = "message")
       })
       ```
     - Add `seed_request` to the module return value (find existing return list and add it):
       ```r
       seed_request = seed_request
       ```

  2. **BibTeX import — wire stub button (R/mod_bulk_import.R, line 810-817):**
     - Replace the stub `observeEvent(input$seed_network, ...)` with real logic:
       ```r
       observeEvent(input$seed_network, {
         run_id <- current_run_id()
         req(run_id)
         con <- con_r()
         run_meta <- dbGetQuery(con, "SELECT notebook_id FROM import_runs WHERE id = ?", list(run_id))
         imported <- dbGetQuery(con, "SELECT work_id FROM import_items WHERE import_run_id = ? AND status = 'imported'", list(run_id))
         if (nrow(imported) == 0) {
           showNotification("No papers imported yet", type = "warning")
           return()
         }
         seed_request(list(
           seed_ids = imported$work_id,
           source_notebook_id = run_meta$notebook_id[1],
           timestamp = Sys.time()
         ))
         removeModal()
         showNotification(paste("Seeding network with", nrow(imported), "imported papers"), type = "message")
       })
       ```
     - Add `seed_request <- reactiveVal(NULL)` to module server
     - Add `seed_request` to the module return value. Check what `mod_bulk_import_server` currently returns and add to it.
     - Note: Check `current_run_id` — it may be named differently. Look for the reactive that tracks the current import run ID in mod_bulk_import.R.

  3. **app.R — bridge modules to citation network:**
     - Capture the return value of `mod_citation_network_server`:
       ```r
       network_api <- mod_citation_network_server("citation_network", con_r, effective_config, current_network, network_refresh)
       ```
     - Capture search notebook seed request. The search notebook module currently returns `search_seed_request` (line 1036). Check if that's the right return or if we need to also get the new seed_request:
       ```r
       search_nb_result <- mod_search_notebook_server(...)
       ```
     - Add observer for search notebook seed request:
       ```r
       observeEvent(search_nb_result$seed_request(), {
         req_data <- search_nb_result$seed_request()
         req(req_data)
         network_api$set_seeds(req_data$seed_ids, req_data$source_notebook_id)
         current_view("network")
       }, ignoreInit = TRUE)
       ```
     - Capture bulk import seed request. Find where `mod_bulk_import_server` is called (around line 1061) and get its return:
       ```r
       sidebar_import_api <- mod_bulk_import_server(...)
       ```
       It may already be captured. Add observer:
       ```r
       observeEvent(sidebar_import_api$seed_request(), {
         req_data <- sidebar_import_api$seed_request()
         req(req_data)
         network_api$set_seeds(req_data$seed_ids, req_data$source_notebook_id)
         current_view("network")
       }, ignoreInit = TRUE)
       ```

  IMPORTANT: Before implementing, read the actual module server return values to understand what's already returned. Don't break existing return values — only add new fields.
  IMPORTANT: The existing `current_network()` / `network_id_r` pattern in app.R is for single paper_id loads from the sidebar. The new multi-seed path bypasses this and goes directly via `network_api$set_seeds()`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "tryCatch({ source('R/mod_search_notebook.R'); source('R/mod_bulk_import.R'); cat('Modules loaded OK\n') }, error = function(e) { cat('ERROR:', e$message, '\n'); quit(status=1) })"</automated>
  </verify>
  <done>
  - Search notebook has "Seed Citation Network" button in toolbar
  - Clicking it queries all papers from notebook and emits seed_request
  - BibTeX import stub replaced with real logic that emits seed_request
  - app.R observes both seed_request reactives and calls network_api$set_seeds()
  - Both buttons trigger current_view("network") to auto-switch to Citation Network tab
  - Network does NOT auto-build — user sees seeds loaded and clicks "Build Network"
  </done>
</task>

</tasks>

<verification>
- Search notebook toolbar shows Seed Network button (icon: share-nodes)
- BibTeX import Seed Citation Network button no longer shows stub notification
- Clicking either button switches to Citation Network view
- Citation network module accepts multiple seeds and builds using fetch_multi_seed_citation_network
- Legend shows star/diamond/dot entries
- Single-seed flow (sidebar paper selection) still works
</verification>

<success_criteria>
- Two entry points for multi-seed networks are functional: search notebook and BibTeX import
- Citation network module handles multi-seed state correctly
- Auto-switch to network tab works from both entry points
- No regressions in single-seed workflow
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-seeded-citation-network/01-02-SUMMARY.md`
</output>
