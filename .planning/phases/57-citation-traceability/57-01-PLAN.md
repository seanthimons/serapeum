---
phase: 57-citation-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/rag.R
  - R/slides.R
autonomous: true
requirements:
  - CITE-01
  - CITE-02
must_haves:
  truths:
    - "Every prose preset system prompt instructs the LLM to cite with page numbers in (Author, Year, p.X) format"
    - "Table presets instruct the LLM to add a numbered footnote section below the table with page citations"
    - "Research Questions and Gap Analysis prompts instruct the LLM to include page numbers in rationales"
    - "RAG chat prompt uses (Author, Year, p.X) format instead of [Document Name, p.X]"
    - "Slide generation prompt instructs LLM to include page numbers in ^[text] footnotes"
    - "All prompts specify fallback formats for abstracts and missing page numbers"
  artifacts:
    - path: "R/rag.R"
      provides: "Citation instructions in all AI preset prompts"
      contains: "Author, Year, p.X"
    - path: "R/slides.R"
      provides: "Page-number citation instruction in slide prompt"
      contains: "page number"
  key_links:
    - from: "R/rag.R"
      to: "build_context() source labels"
      via: "Source labels already contain [DocName, p.X] — prompts now instruct LLM to use that page data"
      pattern: "Author.*Year.*p\\."
    - from: "R/slides.R"
      to: "build_slides_prompt context_parts"
      via: "Context already passes [DocName, p.X] per chunk — prompt instructs LLM to use page numbers in footnotes"
      pattern: "page.*number"
---

<objective>
Add page-level citation instructions to all AI preset prompts in R/rag.R and slide generation prompts in R/slides.R.

Purpose: Users can trace every AI-generated claim back to a specific page in the source document, improving research credibility and verifiability.
Output: Updated prompt strings in R/rag.R (all presets) and R/slides.R (slide generation + healing prompts).
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-citation-traceability/57-CONTEXT.md
@R/rag.R
@R/slides.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add citation instructions to all R/rag.R preset prompts</name>
  <files>R/rag.R</files>
  <read_first>
    - R/rag.R (full file — understand every prompt's current wording, format, and source label patterns)
    - .planning/phases/57-citation-traceability/57-CONTEXT.md (locked decisions on format, fallback, density)
  </read_first>
  <action>
Update the following prompts in R/rag.R with citation instructions. The source context already includes page metadata via `build_context()` labels like `[DocName, p.X]` and `build_context_by_paper()` labels like `[p.X, section_hint]`. The prompts must instruct the LLM to USE that page data in its output.

**A. rag_query() system_prompt (line ~108):**
Change from: `"Always cite your sources using the format [Document Name, p.X] or [Paper Title]."`
Change to instruct: cite using `(Author, Year, p.X)` format. Add fallback rules:
- When source is a document with page numbers: `(Author, Year, p.X)`
- When source is an abstract (no pages): `(Author, Year, abstract)`
- When page_number is missing/NA in document: `(Author, Year, chunk N)`
- When multiple sources support a claim, cite all: `(Smith, 2023, p.5; Jones, 2022, p.12)`
Add a correct/wrong example pair (proven effective per v7.0):
- Correct: `"Studies show increased resistance rates (WHO, 2024, p.12; Smith, 2023, p.45)."`
- Wrong: `"Studies show increased resistance rates [WHO Report, p.12]."`

**B. generate_preset() system_prompt (line ~208):**
Current: `"You are a helpful research assistant. Generate the requested content based on the provided sources. Be thorough and well-organized."`
Add citation instruction block after the existing text:

```
CITATION RULES:
- Cite every substantive claim using (Author, Year, p.X) format
- When page metadata is available: (Author, Year, p.X)
- When source is an abstract only: (Author, Year, abstract)
- When page number is missing: (Author, Year, chunk N)
- When multiple sources support a claim, cite all: (Smith, 2023, p.5; Jones, 2022, p.12)
- Extract author name and year from the source labels provided (e.g., [DocName, p.X] or [Paper Title])

Correct: "Machine learning improves diagnostic accuracy (Chen, 2023, p.15; WHO, 2024, p.8)."
Wrong: "Machine learning improves diagnostic accuracy."
```

This applies to all 4 basic presets (summarize, keypoints, studyguide, outline) since they share this system_prompt.

**C. generate_conclusions_preset() system_prompt (line ~362):**
Current format instruction: `"[Synthesized conclusions with citations using [Source Name] format]"`
Update the OUTPUT FORMAT section to use `(Author, Year, p.X)` format. Add the same fallback rules and density instruction ("cite every substantive claim"). Add correct/wrong example.

**D. call_overview_quick() system_prompt (line ~517):**
Add citation instruction block after `"IMPORTANT: Base all content ONLY on the provided sources. Do not invent findings."`:

```
CITATION RULES:
- Cite every substantive claim using (Author, Year, p.X) format
- For abstracts: (Author, Year, abstract)
- For missing page numbers: (Author, Year, chunk N)
- When multiple sources support a claim, cite all
- Extract author/year from the source labels in the provided data
```

**E. call_overview_summary() system_prompt (line ~558):**
Add citation instruction: `"Cite every substantive claim using (Author, Year, p.X) format. For abstracts: (Author, Year, abstract). For missing page numbers: (Author, Year, chunk N)."`

**F. call_overview_keypoints() system_prompt (line ~587):**
Add citation instruction: `"Cite every substantive claim using (Author, Year, p.X) format. For abstracts: (Author, Year, abstract). For missing page numbers: (Author, Year, chunk N)."`

**G. generate_research_questions() system_prompt (line ~771):**
Current: `"Each rationale MUST name specific papers by 'Author et al. (Year)' format"`
Update to: `"Each rationale MUST name specific papers with page numbers: 'Smith et al. (2023, p.14) found that...'"`
Also update the OUTPUT FORMAT line: `"Each rationale MUST include page numbers where available: Author et al. (Year, p.X)"`

**H. generate_lit_review_table() system_prompt (line ~996):**
This is a structured table preset. Do NOT add inline citations to table cells (keep cells clean with brief phrases). Instead, add instruction to append a numbered footnote section below the table:

```
FOOTNOTES:
After the table, add a "### Sources" section with numbered footnotes linking key findings to specific page numbers.
Format: [1] Author (Year), p.X — brief finding description
Only include footnotes for Key Findings column entries.
```

**I. generate_methodology_extractor() system_prompt (line ~1196):**
Same treatment as lit review table — keep table cells clean, add footnote instruction:

```
FOOTNOTES:
After the table, add a "### Sources" section with numbered footnotes linking methodology details to specific page numbers.
Format: [1] Author (Year), p.X — methodology detail
Include footnotes for Study Design and Statistical Methods columns.
```

**J. generate_gap_analysis() system_prompt (line ~1421):**
Current: `"Weave inline citations naturally: 'Smith et al. (2020) found...', 'contradicting Johnson (2018)'"`
Update to include page numbers: `"Weave inline citations with page numbers: 'Smith et al. (2020, p.14) found...', 'contradicting Johnson (2018, p.8)'"`
Add fallback: `"When citing abstracts without page numbers, use: 'Smith et al. (2020, abstract)'"`
  </action>
  <verify>
    <automated>grep -c "Author, Year, p\\.X\|Author.*Year.*p\\." R/rag.R</automated>
  </verify>
  <acceptance_criteria>
    - R/rag.R rag_query() system_prompt contains "(Author, Year, p.X)" and does NOT contain "[Document Name, p.X]"
    - R/rag.R generate_preset() system_prompt contains "CITATION RULES" and "(Author, Year, p.X)"
    - R/rag.R generate_conclusions_preset() system_prompt contains "(Author, Year, p.X)" and does NOT contain "[Source Name] format"
    - R/rag.R call_overview_quick() system_prompt contains "(Author, Year, p.X)"
    - R/rag.R call_overview_summary() system_prompt contains "(Author, Year, p.X)"
    - R/rag.R call_overview_keypoints() system_prompt contains "(Author, Year, p.X)"
    - R/rag.R generate_research_questions() system_prompt contains "p.14" or "p.X" in rationale format instruction
    - R/rag.R generate_lit_review_table() system_prompt contains "### Sources" footnote instruction
    - R/rag.R generate_methodology_extractor() system_prompt contains "### Sources" footnote instruction
    - R/rag.R generate_gap_analysis() system_prompt contains page number in citation example (e.g., "2020, p.14")
    - All prompts include fallback for abstract-only sources: "(Author, Year, abstract)"
    - No changes to build_context(), build_context_by_paper(), or any non-prompt code
  </acceptance_criteria>
  <done>All 10 prompt functions in R/rag.R include page-level citation instructions with correct format, fallback rules, and density requirements per CONTEXT.md decisions</done>
</task>

<task type="auto">
  <name>Task 2: Add page-number citation instruction to R/slides.R prompts</name>
  <files>R/slides.R</files>
  <read_first>
    - R/slides.R (full file — understand slide prompt structure, existing footnote syntax examples)
    - .planning/phases/57-citation-traceability/57-CONTEXT.md (locked decision: do NOT modify ^[text] Quarto footnote format)
  </read_first>
  <action>
Update slide generation prompts in R/slides.R to instruct LLM to include page numbers in footnote citations. The existing `^[Author et al., 2023, p.5]` example already shows page numbers — but the instruction needs to be explicit that page numbers are REQUIRED, not optional.

**A. build_slides_prompt() system_prompt (line ~64):**
The existing footnote example already shows: `'Machine learning improves accuracy.^[WHO AMR Report, 2024, p.12]'`
This is good. Add an explicit instruction line after the footnote syntax reference block:

```
IMPORTANT: Always include the page number in footnotes when the source data includes page numbers.
If no page number is available, use the chunk identifier from the source label.
Every substantive claim on a slide MUST have a footnote citation.
```

Do NOT change the `^[text]` syntax — leave it exactly as-is per user decision.

**B. build_slides_prompt() citation_instructions (line ~101, the switch statement):**
Update the "footnotes" case from:
`"Use Quarto inline footnotes: add ^[source info] after key points (e.g., 'key finding.^[Author et al., 2023, p.5]'). Do NOT use [^1] reference-style or bare ^1. Quarto renders these as numbered footnotes automatically."`
To:
`"Use Quarto inline footnotes: add ^[source info] after ALL substantive claims (e.g., 'key finding.^[Author et al., 2023, p.5]'). Always include the page number from the source data. Do NOT use [^1] reference-style or bare ^1. Quarto renders these as numbered footnotes automatically."`

Update the "inline" case to also require page numbers:
`"Use inline parenthetical citations like (Author, Year, p.X) after ALL substantive claims. Always include the page number from the source data."`

**C. build_healing_prompt() system_prompt (line ~372):**
The existing footnote example already shows `'Key finding.^[Author et al., 2023, p.5]'` — this is correct.
Add after the footnote syntax reference block:
`"When fixing citations, ensure page numbers from the source data are preserved in footnotes.\n"`
  </action>
  <verify>
    <automated>grep -c "page number" R/slides.R</automated>
  </verify>
  <acceptance_criteria>
    - R/slides.R build_slides_prompt() system_prompt contains "Always include the page number" instruction
    - R/slides.R build_slides_prompt() footnotes citation_instructions contains "ALL substantive claims" (changed from "key points")
    - R/slides.R build_slides_prompt() inline citation_instructions contains "(Author, Year, p.X)"
    - R/slides.R build_healing_prompt() contains "page numbers" preservation instruction
    - R/slides.R does NOT contain any changes to `^[text]` Quarto syntax format (format preserved exactly)
    - R/slides.R does NOT contain `[^1]` style footnotes being introduced
    - No changes to non-prompt code (context_parts formatting, slide_counts, option handling, etc.)
  </acceptance_criteria>
  <done>Slide generation and healing prompts explicitly require page-number citations in every footnote, with existing ^[text] Quarto syntax preserved unchanged</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "(Author, Year, p.X)" R/rag.R` returns 5+ matches (across all preset prompts)
2. `grep -c "abstract)" R/rag.R` returns 5+ matches (fallback format present in all presets)
3. `grep "(Author, Year, p.X)\|page number" R/slides.R` returns 2+ matches
4. `grep "\[Document Name, p.X\]" R/rag.R` returns 0 matches (old format removed from rag_query)
5. `grep "\[Source Name\] format" R/rag.R` returns 0 matches (old format removed from conclusions)
6. No changes to any function signatures, return values, or non-prompt code
7. Existing tests still pass: `Rscript -e "testthat::test_file('tests/testthat/test-slides.R')"`
</verification>

<success_criteria>
- All 10 prompt functions in R/rag.R instruct the LLM to use (Author, Year, p.X) citation format
- Table presets (lit review, methodology) use footnote sections instead of inline citations
- Slide prompts require page numbers in ^[text] footnotes
- Fallback formats specified for abstracts and missing page numbers
- No infrastructure changes — purely prompt text modifications
- Existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/57-citation-traceability/57-01-SUMMARY.md`
</output>
