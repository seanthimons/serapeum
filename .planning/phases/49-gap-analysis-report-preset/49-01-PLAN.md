---
phase: 49-gap-analysis-report-preset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/rag.R
autonomous: true
requirements:
  - GAPS-02
  - GAPS-03
  - GAPS-04
  - GAPS-06

must_haves:
  truths:
    - "generate_gap_analysis() produces narrative prose with 5 gap category headings"
    - "Contradictions formatted with bold prefix within relevant gap category"
    - "Section-targeted RAG filters discussion/limitations/future_work with 3-level fallback"
    - "Papers with < 3 documents return early with threshold message"
    - "20 chunks retrieved per paper for broader cross-paper context"
  artifacts:
    - path: "R/rag.R"
      provides: "generate_gap_analysis() function"
      contains: "generate_gap_analysis <- function"
  key_links:
    - from: "R/rag.R"
      to: "search_chunks_hybrid"
      via: "section_filter parameter"
      pattern: "section_filter.*discussion.*limitations.*future_work"
---

<objective>
Create the generate_gap_analysis() backend function in R/rag.R that produces narrative gap analysis reports from cross-paper synthesis.

Purpose: Provides the core AI synthesis engine for the Gap Analysis Report preset — the final v10.0 AI synthesis feature.
Output: generate_gap_analysis() function appended to R/rag.R
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-gap-analysis-report-preset/49-CONTEXT.md
@.planning/phases/49-gap-analysis-report-preset/49-RESEARCH.md
@.planning/phases/48-methodology-extractor-preset/48-01-SUMMARY.md

<interfaces>
<!-- Key function signature and patterns from R/rag.R that executor must follow -->

From R/rag.R line 1077:
```r
generate_methodology_extractor <- function(con, config, notebook_id, session_id = NULL) {
  # Clone this structure. Key differences for gap_analysis:
  # - section_filter: c("discussion", "limitations", "future_work") instead of c("methods", "methodology")
  # - chunks_per_paper: 7L starting (same dynamic budget), but 20 total chunks target
  # - System prompt: narrative prose with 5 gap categories, not GFM table
  # - Minimum 3 papers enforced (methodology_extractor has no minimum)
  # - Cost logging as "gap_analysis"
}
```

From R/rag.R (shared utilities already available):
```r
get_setting(config, "openrouter", "api_key")
format_chat_messages(system_prompt, user_prompt)
chat_completion(api_key, model, messages)
estimate_cost(model, prompt_tokens, completion_tokens)
log_cost(con, type, model, prompt_tokens, completion_tokens, total_tokens, cost, session_id)
list_documents(con, notebook_id)  # Returns data.frame with id, filename, title, authors, year, doi
```

From R/rag.R (chunk retrieval pattern, lines 1148-1174):
```r
# Section-filtered retrieval from DuckDB chunks table
section_chunks <- dbGetQuery(con, "
  SELECT chunk_index, content, page_number, section_hint
  FROM chunks WHERE source_id = ? AND section_hint IN ('discussion', 'limitations', 'future_work')
  ORDER BY chunk_index LIMIT ?
", list(doc$id, chunks_per_paper))
# Fallback to distributed sampling if < 2 section chunks found
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate_gap_analysis() function in R/rag.R</name>
  <files>R/rag.R</files>
  <action>
Clone generate_methodology_extractor() (lines 1077-1273) and adapt for gap analysis. Append the new function after line 1273.

Key modifications from methodology_extractor:

1. **Function signature:** `generate_gap_analysis <- function(con, config, notebook_id, session_id = NULL)`

2. **Minimum paper threshold (GAPS-06):** After fetching docs, check `nrow(docs) < 3` and return early: "Gap analysis requires at least 3 papers to identify meaningful patterns."

3. **Section filter (GAPS-04):** Change section filter from `c("methods", "methodology")` to `c("discussion", "limitations", "future_work")` — all three section types from detect_section_hint().

4. **Chunk limit:** Keep chunks_per_paper at 7L starting (same dynamic token budget logic), but the broader section filter naturally captures more content per paper.

5. **System prompt (GAPS-02, GAPS-03):** Replace the methodology table prompt with narrative gap analysis prompt:
   - Opening: "You are a research gap analyst..."
   - OUTPUT FORMAT section with: ## Summary, ## Methodological Gaps, ## Geographic Gaps, ## Population Gaps, ## Measurement Gaps, ## Theoretical Gaps
   - RULES section mandating:
     - All 5 gap categories always shown as headings
     - "No significant [type] gaps identified across the reviewed papers." when none found
     - Inline Author (Year) citations woven into narrative
     - Active contradiction detection with "**Contradictory finding:**" bold prefix
     - Contradictions integrated within their relevant gap category
     - Analysis based only on provided sources

6. **User prompt:** "Generate the gap analysis report." (replacing methodology extractor instruction)

7. **Retrieval query:** Use gap-specific query: "limitations future work research gaps contradictions methodological challenges" instead of methodology-specific terms.

8. **Coverage transparency note:** After generating response, if any papers used fallback retrieval (no section-filtered chunks found), append: "\n\n---\n*Note: Some papers lacked structured Discussion/Limitations sections; analysis drew from available content.*"

9. **Cost logging:** Use "gap_analysis" as the type label (not "methodology_extractor").

10. **Error message:** "Error generating gap analysis: %s"

Do NOT change the existing pipe-count validation or GFM table logic — that belongs to methodology_extractor. The gap_analysis function returns plain narrative markdown (no table validation needed).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/rag.R'); cat('generate_gap_analysis exists:', exists('generate_gap_analysis'), '\n')"</automated>
  </verify>
  <done>generate_gap_analysis() function exists in R/rag.R with: 3-paper minimum check, section filter for discussion/limitations/future_work, narrative prose system prompt with 5 gap categories, contradiction detection instruction, coverage transparency note, and gap_analysis cost logging.</done>
</task>

</tasks>

<verification>
- generate_gap_analysis() loads without parse errors
- Function accepts (con, config, notebook_id, session_id = NULL) parameters
- Section filter targets discussion, limitations, future_work
- System prompt includes all 5 gap category headings
- Contradiction detection instruction present with bold prefix format
- Minimum 3 papers check returns early message
- Cost logging uses "gap_analysis" label
</verification>

<success_criteria>
generate_gap_analysis() function is appended to R/rag.R, loads without error, and implements section-targeted RAG with narrative gap analysis output format including all 5 gap categories and contradiction detection.
</success_criteria>

<output>
After completion, create `.planning/phases/49-gap-analysis-report-preset/49-01-SUMMARY.md`
</output>
