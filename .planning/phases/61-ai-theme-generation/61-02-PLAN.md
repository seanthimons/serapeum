---
phase: 61-ai-theme-generation
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - R/mod_slides.R
autonomous: false
requirements:
  - THME-05
  - THME-06
  - THME-07

must_haves:
  truths:
    - "User sees an AI Generate link next to the Upload link in the slide generation modal"
    - "Clicking AI Generate reveals a collapse form with textarea and Generate button"
    - "Submitting a description calls LLM and populates color pickers and font selector with AI values"
    - "Invalid hex colors show error toast naming the bad fields"
    - "Invalid font falls back to Source Sans Pro with warning toast"
    - "JSON extraction failure retries once silently then shows error toast"
    - "Regenerate button appears in customize panel after AI generation"
    - "Customize panel auto-expands after successful AI generation"
    - "Spinner and disable state shown on Generate/Regenerate buttons during LLM call"
  artifacts:
    - path: "R/mod_slides.R"
      provides: "AI Generate UI trigger, collapse form, server observers, Regenerate button"
      contains: "ai_generate_btn"
  key_links:
    - from: "R/mod_slides.R"
      to: "R/themes.R"
      via: "Server calls extract_theme_json, validate_theme_colors, validate_and_fix_font, generate_theme_from_description"
      pattern: "generate_theme_from_description"
    - from: "R/mod_slides.R"
      to: "R/cost_tracking.R"
      via: "log_cost with operation = 'theme_generation'"
      pattern: "theme_generation"
    - from: "R/mod_slides.R"
      to: "R/mod_slides.R (color pickers)"
      via: "updateTextInput for bg_hex/text_hex/accent_hex/link_hex, updateSelectInput for font, sendCustomMessage for swatches"
      pattern: "update_color_swatch"
---

<objective>
Wire AI theme generation UI into the slide generation modal. Add the AI Generate trigger link, collapse form with textarea + Generate button, server observers for LLM call with retry logic, validation, color picker population, panel expansion, Regenerate button, spinner states, and cost tracking.

Purpose: This is the user-facing integration that connects the Plan 01 helper functions to the existing Phase 60 color picker UI, completing the THME-05/06/07 requirements.
Output: Fully working AI theme generation flow in the slide generation modal.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-ai-theme-generation/61-CONTEXT.md
@.planning/phases/61-ai-theme-generation/61-RESEARCH.md
@.planning/phases/61-ai-theme-generation/61-UI-SPEC.md
@.planning/phases/61-ai-theme-generation/61-01-SUMMARY.md

<interfaces>
<!-- From R/themes.R (Plan 01 outputs) -->
```r
extract_theme_json <- function(llm_response)
# Returns: parsed list with backgroundColor, mainColor, accentColor, linkColor, mainFont
# Returns: NULL if no fence block or malformed JSON

validate_theme_colors <- function(theme_list)
# Returns: character(0) if all valid; character vector of bad field names otherwise

validate_and_fix_font <- function(font_name)
# Returns: list(font = "validated_name", warning = NULL_or_message_string)

generate_theme_from_description <- function(api_key, model, description)
# Returns: list(content = "LLM text", usage = list(prompt_tokens, completion_tokens))
```

<!-- From R/api_openrouter.R -->
```r
chat_completion <- function(api_key, model, messages)
# Returns: list(content, usage, model, id)
# Throws error on API failure
```

<!-- From R/cost_tracking.R -->
```r
log_cost <- function(con, operation, model, prompt_tokens, completion_tokens = 0,
                     total_tokens = NULL, estimated_cost, session_id)
estimate_cost <- function(model, prompt_tokens, completion_tokens)
```

<!-- From R/mod_slides.R — existing patterns (lines 646-656) -->
```r
# Color picker population pattern:
updateTextInput(session, "bg_hex",     value = bg)
updateTextInput(session, "text_hex",   value = fg)
updateTextInput(session, "accent_hex", value = acc)
updateTextInput(session, "link_hex",   value = lnk)
updateSelectInput(session, "font",     selected = fnt)
session$sendCustomMessage("update_color_swatch", list(
  ids    = list(ns("bg_swatch"), ns("text_swatch"), ns("accent_swatch"), ns("link_swatch")),
  values = list(tolower(bg), tolower(fg), tolower(acc), tolower(lnk))
))
```

<!-- Existing custom message handlers in customize panel (lines 236-252) -->
```r
# Already registered: update_color_swatch, focus_element, collapse_panel
# Need to ADD: expand_panel, set_button_loading
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI Generate UI and server wiring to mod_slides.R</name>
  <files>R/mod_slides.R</files>
  <read_first>
    - R/mod_slides.R (full file — understand modal UI structure lines 1-291, server wiring lines 500-720)
    - R/themes.R (Plan 01 functions: extract_theme_json, validate_theme_colors, validate_and_fix_font, generate_theme_from_description)
    - R/cost_tracking.R (log_cost, estimate_cost signatures)
    - R/api_openrouter.R (chat_completion signature)
    - .planning/phases/61-ai-theme-generation/61-UI-SPEC.md (component inventory, interaction states, copywriting contract)
    - .planning/phases/61-ai-theme-generation/61-CONTEXT.md (locked decisions)
  </read_first>
  <action>
    **UI changes in mod_slides_modal_ui function:**

    1. **Add 2 new custom message handlers** inside the existing `tags$script(HTML(...))` block in the customize panel (around line 235-252). Add after the `collapse_panel` handler, before the closing `"` of the HTML string:

    ```javascript
    Shiny.addCustomMessageHandler('expand_panel', function(id) {
      var el = document.getElementById(id);
      if (el && !el.classList.contains('show')) {
        bootstrap.Collapse.getOrCreateInstance(el).show();
      }
    });
    Shiny.addCustomMessageHandler('set_button_loading', function(msg) {
      var btn = document.getElementById(msg.id);
      if (!btn) return;
      if (msg.loading) {
        btn.disabled = true;
        btn.dataset.originalHtml = btn.innerHTML;
        btn.innerHTML = '<span class="spinner-border spinner-border-sm" role="status"><span class="visually-hidden">Loading</span></span> ' + (msg.text || 'Generating...');
      } else {
        btn.disabled = false;
        btn.innerHTML = btn.dataset.originalHtml || msg.label || 'Generate';
      }
    });
    ```

    2. **Add AI Generate trigger link** next to the Upload label. Find the `tags$label(for = ns("theme_file"), ...)` block (around line 188-194). Wrap the Upload label and a new AI Generate link in a flex container. The result should be:

    ```r
    # Replace the standalone Upload label with a flex row containing both links
    div(
      class = "d-flex gap-3 align-items-center",
      # Existing Upload link (keep as-is)
      tags$label(
        `for` = ns("theme_file"),
        class = "small text-muted d-inline-flex align-items-center gap-1",
        style = "cursor:pointer; margin-bottom:0;",
        icon("upload"),
        " Upload custom theme (.scss)"
      ),
      # NEW: AI Generate trigger
      tags$a(
        class = "small text-muted d-inline-flex align-items-center gap-1",
        style = "cursor:pointer; margin-bottom:0;",
        `data-bs-toggle` = "collapse",
        href = paste0("#", ns("ai_generate_form")),
        role = "button",
        `aria-expanded` = "false",
        `aria-controls` = ns("ai_generate_form"),
        `aria-label` = "Generate theme with AI",
        icon("wand-magic-sparkles"),
        " AI Generate"
      )
    ),
    ```

    3. **Add AI Generate collapse form** immediately after the Upload file input div (after line 201, before the `uiOutput(ns("upload_error"))` line). Insert:

    ```r
    # AI Generate collapse form
    div(
      id = ns("ai_generate_form"),
      class = "collapse mt-2 p-3 border rounded bg-body-secondary",
      tags$textarea(
        id = ns("ai_theme_description"),
        class = "form-control form-control-sm",
        rows = "2",
        placeholder = "e.g., ocean blues, dark background, modern sans-serif font"
      ),
      tags$button(
        id = ns("ai_generate_btn"),
        type = "button",
        class = "btn btn-primary btn-sm mt-2 w-100",
        onclick = sprintf(
          "Shiny.setInputValue('%s', Date.now(), {priority: 'event'});",
          ns("ai_generate_btn")
        ),
        "Generate theme"
      )
    ),
    ```

    4. **Add Regenerate button placeholder** inside the customize panel, after the font selector (line 263) and before the save row div (line 265). Insert:

    ```r
    # Regenerate button (rendered conditionally by server)
    uiOutput(ns("regenerate_btn_area")),
    ```

    **Server changes in mod_slides_server function:**

    5. **Add reactive state tracking** near the top of the server function (after `refresh_theme_dropdown` helper or equivalent). Add:

    ```r
    # Track whether AI generation has occurred (controls Regenerate button visibility)
    ai_generated <- reactiveVal(FALSE)
    # Store the last AI description for Regenerate
    last_ai_description <- reactiveVal(NULL)
    ```

    6. **Add AI Generate button observer.** After the `save_custom_theme` observer (around line 709), add:

    ```r
    # AI Theme Generation
    observeEvent(input$ai_generate_btn, {
      description <- input$ai_theme_description
      if (is.null(description) || !nzchar(trimws(description))) {
        showNotification("Please enter a theme description.", type = "warning")
        return()
      }

      cfg <- config()
      api_key <- cfg$api_key
      model <- cfg$chat_model

      if (is.null(api_key) || !nzchar(api_key)) {
        showNotification("Please set your API key in Settings first.", type = "error")
        return()
      }

      # Save description for Regenerate
      last_ai_description(trimws(description))

      # Spinner on
      session$sendCustomMessage("set_button_loading",
        list(id = ns("ai_generate_btn"), loading = TRUE, text = "Generating..."))

      # Attempt generation with 1 retry on JSON failure
      attempt_generate <- function(desc, attempt_num = 1) {
        result <- tryCatch(
          generate_theme_from_description(api_key, model, desc),
          error = function(e) list(content = NULL, usage = NULL)
        )

        # Log cost regardless of parse success
        if (!is.null(result$usage)) {
          cost <- estimate_cost(model,
                                prompt_tokens = result$usage$prompt_tokens,
                                completion_tokens = result$usage$completion_tokens)
          log_cost(con(),
                   operation = "theme_generation",
                   model = model,
                   prompt_tokens = result$usage$prompt_tokens,
                   completion_tokens = result$usage$completion_tokens,
                   estimated_cost = cost,
                   session_id = session$token)
        }

        if (is.null(result$content)) {
          if (attempt_num < 2) return(attempt_generate(desc, 2))
          return(list(theme = NULL, error = "Couldn't generate theme. Try a more specific description."))
        }

        json <- extract_theme_json(result$content)
        if (is.null(json)) {
          if (attempt_num < 2) return(attempt_generate(desc, 2))
          return(list(theme = NULL, error = "Couldn't generate theme. Try a more specific description."))
        }

        list(theme = json, usage = result$usage, error = NULL)
      }

      gen_result <- attempt_generate(description)

      # Spinner off
      session$sendCustomMessage("set_button_loading",
        list(id = ns("ai_generate_btn"), loading = FALSE, label = "Generate theme"))

      if (!is.null(gen_result$error)) {
        showNotification(gen_result$error, type = "error")
        return()
      }

      theme <- gen_result$theme

      # Validate hex colors
      bad_colors <- validate_theme_colors(theme)
      if (length(bad_colors) > 0) {
        showNotification(
          paste0("Theme has invalid colors (", paste(bad_colors, collapse = ", "),
                 "). Try a more specific description."),
          type = "error")
        return()
      }

      # Validate font
      font_result <- validate_and_fix_font(theme$mainFont %||% "")
      if (!is.null(font_result$warning)) {
        showNotification(font_result$warning, type = "warning")
      }

      # Populate color pickers and font selector
      bg  <- theme$backgroundColor
      fg  <- theme$mainColor
      acc <- theme$accentColor
      lnk <- theme$linkColor
      fnt <- font_result$font

      updateTextInput(session, "bg_hex",     value = bg)
      updateTextInput(session, "text_hex",   value = fg)
      updateTextInput(session, "accent_hex", value = acc)
      updateTextInput(session, "link_hex",   value = lnk)
      updateSelectInput(session, "font",     selected = fnt)

      session$sendCustomMessage("update_color_swatch", list(
        ids    = list(ns("bg_swatch"), ns("text_swatch"), ns("accent_swatch"), ns("link_swatch")),
        values = list(tolower(bg), tolower(fg), tolower(acc), tolower(lnk))
      ))

      # Expand customize panel
      session$sendCustomMessage("expand_panel", ns("customize_panel"))

      # Mark AI generation as complete (shows Regenerate button)
      ai_generated(TRUE)

      # Collapse the AI generate form
      session$sendCustomMessage("collapse_panel", ns("ai_generate_form"))
    })
    ```

    7. **Add Regenerate button renderer and observer.** After the AI generate observer:

    ```r
    # Regenerate button — only visible after AI generation
    output$regenerate_btn_area <- renderUI({
      if (!ai_generated()) return(NULL)
      div(
        class = "mt-2",
        actionButton(ns("regenerate_theme"), "Regenerate",
                     class = "btn btn-outline-secondary btn-sm",
                     icon = icon("rotate"))
      )
    })

    # Regenerate observer — re-runs AI generation with last description
    observeEvent(input$regenerate_theme, {
      description <- last_ai_description()
      if (is.null(description) || !nzchar(description)) return()

      cfg <- config()
      api_key <- cfg$api_key
      model <- cfg$chat_model

      session$sendCustomMessage("set_button_loading",
        list(id = ns("regenerate_theme"), loading = TRUE, text = "Regenerating..."))

      # Same attempt_generate logic as main observer
      attempt_generate <- function(desc, attempt_num = 1) {
        result <- tryCatch(
          generate_theme_from_description(api_key, model, desc),
          error = function(e) list(content = NULL, usage = NULL)
        )
        if (!is.null(result$usage)) {
          cost <- estimate_cost(model,
                                prompt_tokens = result$usage$prompt_tokens,
                                completion_tokens = result$usage$completion_tokens)
          log_cost(con(),
                   operation = "theme_generation",
                   model = model,
                   prompt_tokens = result$usage$prompt_tokens,
                   completion_tokens = result$usage$completion_tokens,
                   estimated_cost = cost,
                   session_id = session$token)
        }
        if (is.null(result$content)) {
          if (attempt_num < 2) return(attempt_generate(desc, 2))
          return(list(theme = NULL, error = "Couldn't generate theme. Try a more specific description."))
        }
        json <- extract_theme_json(result$content)
        if (is.null(json)) {
          if (attempt_num < 2) return(attempt_generate(desc, 2))
          return(list(theme = NULL, error = "Couldn't generate theme. Try a more specific description."))
        }
        list(theme = json, usage = result$usage, error = NULL)
      }

      gen_result <- attempt_generate(description)

      session$sendCustomMessage("set_button_loading",
        list(id = ns("regenerate_theme"), loading = FALSE, label = "Regenerate"))

      if (!is.null(gen_result$error)) {
        showNotification(gen_result$error, type = "error")
        return()
      }

      theme <- gen_result$theme
      bad_colors <- validate_theme_colors(theme)
      if (length(bad_colors) > 0) {
        showNotification(
          paste0("Theme has invalid colors (", paste(bad_colors, collapse = ", "),
                 "). Try a more specific description."),
          type = "error")
        return()
      }

      font_result <- validate_and_fix_font(theme$mainFont %||% "")
      if (!is.null(font_result$warning)) {
        showNotification(font_result$warning, type = "warning")
      }

      bg  <- theme$backgroundColor
      fg  <- theme$mainColor
      acc <- theme$accentColor
      lnk <- theme$linkColor
      fnt <- font_result$font

      updateTextInput(session, "bg_hex",     value = bg)
      updateTextInput(session, "text_hex",   value = fg)
      updateTextInput(session, "accent_hex", value = acc)
      updateTextInput(session, "link_hex",   value = lnk)
      updateSelectInput(session, "font",     selected = fnt)

      session$sendCustomMessage("update_color_swatch", list(
        ids    = list(ns("bg_swatch"), ns("text_swatch"), ns("accent_swatch"), ns("link_swatch")),
        values = list(tolower(bg), tolower(fg), tolower(acc), tolower(lnk))
      ))
    })
    ```

    8. **Reset ai_generated state on save.** In the existing `observeEvent(input$save_custom_theme, ...)` handler (around line 684-709), add after the `showNotification` success line:

    ```r
    ai_generated(FALSE)
    last_ai_description(NULL)
    ```

    **Important implementation notes:**
    - The AI Generate form uses a `tags$textarea` (raw HTML) + `tags$button` with `Shiny.setInputValue()` onclick because the collapse block's textarea is a raw HTML element not a Shiny textAreaInput. Read the textarea value via JS: add `var desc = document.getElementById('` + ns("ai_theme_description") + `').value;` in the onclick, passing it as the value instead of `Date.now()`. **OR** use a standard Shiny `textAreaInput()` which will register the value automatically (preferred — simpler). If using `textAreaInput()`, the server reads `input$ai_theme_description` directly.
    - For the `tags$button` onclick approach with `Shiny.setInputValue()`: the value should be `Date.now()` (a unique number to ensure the event fires each time, since Shiny deduplicates identical values). The actual description text is read from `input$ai_theme_description` in the server observer.
    - Ensure `%||%` is available — it is part of rlang (loaded via shiny). If not, use `if (is.null(x)) "" else x` pattern instead.
    - The `con()` function for database access should already be available in the server scope from the parent module.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 8 && curl -s http://localhost:3838 > /dev/null && echo "App starts successfully" || echo "App failed to start"; kill %1 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_slides.R contains `ns("ai_generate_form")` (collapse form ID)
    - R/mod_slides.R contains `ns("ai_generate_btn")` (generate button ID)
    - R/mod_slides.R contains `ns("regenerate_theme")` (regenerate button ID)
    - R/mod_slides.R contains `ns("ai_theme_description")` (textarea ID)
    - R/mod_slides.R contains `generate_theme_from_description(` call in server observer
    - R/mod_slides.R contains `extract_theme_json(` call in server observer
    - R/mod_slides.R contains `validate_theme_colors(` call in server observer
    - R/mod_slides.R contains `validate_and_fix_font(` call in server observer
    - R/mod_slides.R contains `log_cost(` with `operation = "theme_generation"`
    - R/mod_slides.R contains `expand_panel` custom message handler
    - R/mod_slides.R contains `set_button_loading` custom message handler
    - R/mod_slides.R contains `ai_generated <- reactiveVal(FALSE)`
    - R/mod_slides.R contains `ai_generated(FALSE)` in the save_custom_theme observer
    - R/mod_slides.R contains `icon("wand-magic-sparkles")` for the AI Generate trigger
    - App starts without error (Shiny smoke test)
  </acceptance_criteria>
  <done>AI Generate link, collapse form, Generate/Regenerate buttons, server observers with retry/validation/population/cost-tracking all wired into mod_slides.R; app starts without crash</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AI theme generation flow</name>
  <what-built>Complete AI theme generation flow: AI Generate link in slide modal, collapse form with textarea, Generate button with spinner, LLM call with retry, JSON extraction, hex/font validation, color picker population, panel auto-expansion, Regenerate button, cost tracking</what-built>
  <how-to-verify>
    1. Open the app, navigate to a notebook with documents
    2. Click "Generate Slides" to open the slide generation modal
    3. Verify "AI Generate" link appears next to "Upload custom theme (.scss)" with a wand icon
    4. Click "AI Generate" — verify a collapse form appears with a textarea and "Generate theme" button
    5. Enter "ocean blues, dark background, modern sans-serif font" and click Generate
    6. Verify: Generate button shows spinner + "Generating..." text and is disabled during LLM call
    7. Verify: After success, the customize panel auto-expands with AI-populated color pickers
    8. Verify: A "Regenerate" button with rotate icon appears in the customize panel
    9. Verify: Color swatch dots in the theme dropdown update to reflect AI-chosen colors
    10. Manually tweak one color picker — verify the change sticks
    11. Click "Regenerate" — verify new colors populate (may differ from original)
    12. Enter a theme name and click "Save as custom theme" — verify Regenerate button disappears
    13. Check session cost panel — verify "Theme Generation" entries appear
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- App starts without error (Shiny smoke test)
- AI Generate trigger visible in slide modal next to Upload link
- Full generation flow works: description -> LLM -> JSON -> validation -> picker population
- Error states work: empty description warning, invalid hex error toast, font fallback warning
- Regenerate button visible after generation, hidden after save
- Cost tracked in session cost panel
</verification>

<success_criteria>
- User can type a theme description, generate via AI, and see the color pickers populated with AI values
- Invalid hex colors produce error toast naming bad fields
- Invalid fonts fall back to Source Sans Pro with warning
- Failed JSON extraction retries once then shows error toast
- Regenerate button appears after generation, disappears after save
- Generation cost tracked via existing cost infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/61-ai-theme-generation/61-02-SUMMARY.md`
</output>
