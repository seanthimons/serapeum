---
phase: 60-color-picker-and-font-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/themes.R
  - tests/testthat/test-themes.R
autonomous: true
requirements:
  - THME-08
  - THME-11
must_haves:
  truths:
    - "generate_custom_scss() writes a valid .scss file with 5 variables and both section markers"
    - "parse_scss_colors_full() extracts all 4 colors + font from an .scss file"
    - "CURATED_FONTS contains at least 10 fonts across at least 2 categories"
    - "Generated .scss wraps multi-word font names in double quotes"
    - "Filename sanitization removes special characters"
  artifacts:
    - path: "R/themes.R"
      provides: "generate_custom_scss(), parse_scss_colors_full(), CURATED_FONTS"
      contains: "generate_custom_scss"
    - path: "tests/testthat/test-themes.R"
      provides: "Tests for new helper functions"
      contains: "generate_custom_scss"
  key_links:
    - from: "tests/testthat/test-themes.R"
      to: "R/themes.R"
      via: "source() and test assertions"
      pattern: "generate_custom_scss|parse_scss_colors_full|CURATED_FONTS"
---

<objective>
Add theme helper functions to R/themes.R: generate_custom_scss() for writing minimal 5-variable .scss files, parse_scss_colors_full() for extracting all 4 colors + font from .scss, and CURATED_FONTS constant for the font selector. TDD approach: write failing tests first, then implement.

Purpose: These pure-R functions are the foundation for the color picker UI (Plan 02) and future AI theme generation (Phase 61). Testing them in isolation ensures correctness before wiring into reactive UI.
Output: Extended R/themes.R with 3 new exports, extended test-themes.R with passing tests.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-color-picker-and-font-selector/60-CONTEXT.md
@.planning/phases/60-color-picker-and-font-selector/60-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from existing code. -->

From R/themes.R (existing functions — do NOT modify):
```r
BUILTIN_THEME_SWATCHES  # list of 11 themes, each list(bg, fg, accent)
parse_scss_swatches(scss_text)  # returns list(bg, fg, accent)
validate_scss_file(scss_text)   # returns TRUE/FALSE
list_custom_themes(themes_dir)  # returns list of list(filename, label, bg, fg, accent)
build_theme_choices_df(custom_themes)  # returns data.frame(value, label, bg, fg, accent, group)
```

From data/themes/epa-owm.scss (reference for variable naming):
```scss
/*-- scss:defaults --*/
$body-bg: $epa-white;
$body-color: $epa-text;
$link-color: $epa-blue;
$presentation-heading-font: "Source Sans Pro", "Segoe UI", Helvetica, Arial, sans-serif;
/*-- scss:rules --*/
```

Generated .scss contract (from 60-CONTEXT.md):
```scss
/*-- scss:defaults --*/

$backgroundColor: #FFFFFF;
$mainColor: #000000;
$linkColor: #157efb;
$accentColor: #157efb;
$mainFont: "Source Sans Pro", sans-serif;

/*-- scss:rules --*/

/* Generated by Serapeum theme customizer */
.reveal h1, .reveal h2, .reveal h3 {
  color: $accentColor;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — theme helper functions (generate_custom_scss, parse_scss_colors_full, CURATED_FONTS)</name>
  <files>R/themes.R, tests/testthat/test-themes.R</files>
  <behavior>
    - generate_custom_scss("My Theme", "#FFFFFF", "#000000", "#157efb", "#157efb", "Source Sans Pro") writes a file to data/themes/My-Theme.scss containing all 5 SCSS variables and both section markers
    - generate_custom_scss sanitizes filenames: "Hello World!" becomes "Hello-World-"
    - generate_custom_scss wraps font name in double quotes: $mainFont: "Source Sans Pro", sans-serif;
    - generate_custom_scss includes scss:rules block wiring $accentColor to heading colors
    - generate_custom_scss returns the file path on success, NULL on failure
    - generate_custom_scss overwrites existing file silently (no error on duplicate name)
    - parse_scss_colors_full reads an .scss file and returns list(bg, fg, accent, link, font) with fallbacks
    - parse_scss_colors_full handles both $backgroundColor/$mainColor style AND $body-bg/$body-color style variable names
    - parse_scss_colors_full extracts $mainFont value (unquoted), or returns "Source Sans Pro" as fallback
    - parse_scss_colors_full returns fallback values for missing variables: bg="#FFFFFF", fg="#000000", accent="#157efb", link="#157efb", font="Source Sans Pro"
    - CURATED_FONTS is a named list with groups "Sans-serif", "Serif", "Monospace" containing at least 10 total font names
    - CURATED_FONTS includes at minimum: Source Sans Pro, Lato, Fira Sans, Roboto, Open Sans (sans-serif); Merriweather, PT Serif, Roboto Slab, Playfair Display (serif); IBM Plex Mono, Fira Code (monospace)
  </behavior>
  <action>
    RED phase: Write failing tests in tests/testthat/test-themes.R covering all behaviors above. Use tempdir() for file operations. Run tests — must fail.

    GREEN phase: Add three items to R/themes.R:

    1. CURATED_FONTS — named list constant at module top level, grouped by category:
       ```r
       CURATED_FONTS <- list(
         "Sans-serif" = c("Source Sans Pro", "Lato", "Fira Sans", "Roboto", "Open Sans"),
         "Serif" = c("Merriweather", "PT Serif", "Roboto Slab", "Playfair Display"),
         "Monospace" = c("IBM Plex Mono", "Fira Code")
       )
       ```

    2. parse_scss_colors_full(scss_text) — extends parse_scss_swatches pattern to also extract $linkColor/$link-color and $mainFont/$presentation-heading-font. Returns list(bg, fg, accent, link, font). Reuses the same resolve_value + extract_color helper approach from parse_scss_swatches. For font extraction: regex pattern `\\$(?:mainFont|presentation-heading-font):\\s*"?([^";,]+)"?` to capture the first font name (unquoted).

    3. generate_custom_scss(name, bg_color, text_color, accent_color, link_color, font_name, themes_dir = "data/themes") — sanitizes name via gsub("[^a-zA-Z0-9_-]", "-", name), builds .scss string with paste0(), writes with writeLines(). Returns file path on success, NULL on error (tryCatch wrapping writeLines).

    Run tests — must pass.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-themes.R')" && grep -q "generate_custom_scss" R/themes.R && grep -q "parse_scss_colors_full" R/themes.R && grep -q "CURATED_FONTS" R/themes.R</automated>
  </verify>
  <done>All tests in test-themes.R pass (existing tests unbroken + new tests green). R/themes.R contains generate_custom_scss(), parse_scss_colors_full(), and CURATED_FONTS.</done>
</task>

</tasks>

<verification>
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-themes.R')"
```
All existing + new tests pass with 0 failures.
</verification>

<success_criteria>
- generate_custom_scss() writes valid .scss with $backgroundColor, $mainColor, $linkColor, $accentColor, $mainFont variables
- parse_scss_colors_full() returns 5-field list (bg, fg, accent, link, font) from .scss text
- CURATED_FONTS has >= 10 fonts across >= 2 groups
- All tests in test-themes.R pass (existing tests unbroken + new tests green)
- Generated .scss passes validate_scss_file() (both section markers present)
</success_criteria>

<output>
After completion, create `.planning/phases/60-color-picker-and-font-selector/60-01-SUMMARY.md`
</output>
