---
phase: 60-color-picker-and-font-selector
plan: 02
type: execute
wave: 2
depends_on:
  - "60-01"
files_modified:
  - R/mod_slides.R
autonomous: false
requirements:
  - THME-08
  - THME-10
  - THME-11
must_haves:
  truths:
    - "User sees a collapsible 'Customize colors & font' panel below the theme dropdown, collapsed by default"
    - "User can pick 4 colors (BG, Text, Accent, Link) via native color swatch + hex text field pairs in a 2x2 grid"
    - "User can select a font from a curated grouped dropdown with at least 10 fonts"
    - "Changing the theme dropdown pre-fills all 4 color pickers and the font selector with the selected theme's values"
    - "User can save customized colors/font as a named .scss theme to data/themes/"
    - "After save, the new theme is auto-selected in the dropdown, panel collapses, toast notification shown"
    - "Color pickers are reactive targets that Phase 61 can populate via updateTextInput/updateSelectInput"
  artifacts:
    - path: "R/mod_slides.R"
      provides: "Collapsible color/font customization panel UI + server wiring"
      contains: "customize_panel"
  key_links:
    - from: "R/mod_slides.R"
      to: "R/themes.R"
      via: "generate_custom_scss(), parse_scss_colors_full(), CURATED_FONTS"
      pattern: "generate_custom_scss|parse_scss_colors_full|CURATED_FONTS"
    - from: "R/mod_slides.R (color picker JS)"
      to: "R/mod_slides.R (Shiny textInput)"
      via: "oninput JS event -> dispatchEvent('change') -> Shiny input binding"
      pattern: "dispatchEvent.*new Event.*change"
    - from: "R/mod_slides.R (save handler)"
      to: "R/mod_slides.R (refresh_theme_dropdown)"
      via: "generate_custom_scss() then refresh_theme_dropdown(selected = basename(path))"
      pattern: "refresh_theme_dropdown"
---

<objective>
Wire the color picker / font selector UI panel into the slides generation modal in mod_slides.R. Add the collapsible panel UI with 4 native color input + hex text field pairs in a 2x2 grid, a grouped font selectInput, and a save-as-custom-theme row. Wire server-side reactives for theme pre-fill, hex validation, swatch dot live update, and .scss save.

Purpose: This is the user-facing deliverable for Phase 60 — manual color/font customization. The picker inputs also serve as the target for Phase 61's AI-populated values (THME-10 contract: updateTextInput/updateSelectInput calls).
Output: Working collapsible panel in mod_slides.R with full save-to-scss flow.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-color-picker-and-font-selector/60-CONTEXT.md
@.planning/phases/60-color-picker-and-font-selector/60-RESEARCH.md
@.planning/phases/60-color-picker-and-font-selector/60-UI-SPEC.md
@.planning/phases/60-color-picker-and-font-selector/60-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (R/themes.R) — the executor will read the actual file but these are the contracts: -->

```r
# New in Plan 01:
CURATED_FONTS  # list("Sans-serif" = c(...), "Serif" = c(...), "Monospace" = c(...))
parse_scss_colors_full(scss_text)  # returns list(bg, fg, accent, link, font)
generate_custom_scss(name, bg_color, text_color, accent_color, link_color, font_name, themes_dir = "data/themes")  # returns file path or NULL
```

<!-- Existing in R/mod_slides.R — key insertion points and patterns: -->

```r
# Line 9: ns_prefix <- ns("")  — used for JS Shiny.setInputValue namespacing
# Lines 86-145: Theme div block (selectizeInput + upload link + fileInput) — NEW PANEL GOES AFTER THIS BLOCK (after line 145), BEFORE the speaker notes checkbox (line 149)
# Line 149: checkboxInput(ns("include_notes"), ...) — panel must be inserted above this
# Lines 359-368: refresh_theme_dropdown() helper — reuse for save flow
# Lines 505-516: Theme vs custom_scss resolution — must also handle picker-generated custom themes
```

<!-- Existing selectizeInput render JS (swatch dots): -->
```js
// item.bg, item.fg, item.accent are read from the choices df
// To update dots: call updateSelectizeInput with modified choices df
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add collapsible panel UI and color/font inputs to mod_slides_modal_ui</name>
  <read_first>
    - R/mod_slides.R (full file — understand UI structure lines 1-160, server structure lines 340-490)
    - R/themes.R (CURATED_FONTS constant from Plan 01)
    - .planning/phases/60-color-picker-and-font-selector/60-UI-SPEC.md (component inventory, spacing, typography, copy)
    - .planning/phases/60-color-picker-and-font-selector/60-RESEARCH.md (Pattern 1: native color input + hex sync JS)
  </read_first>
  <files>R/mod_slides.R</files>
  <action>
    Insert the collapsible customization panel in mod_slides_modal_ui() between the theme div block (ends line 145) and the speaker notes checkbox (line 149).

    **Panel structure (in order):**

    1. **Toggle button** — an `actionLink` or `tags$a` with Bootstrap collapse attributes:
       ```r
       tags$a(
         class = "small text-muted d-flex align-items-center gap-1 mt-2",
         style = "cursor:pointer; text-decoration:none;",
         `data-bs-toggle` = "collapse",
         href = paste0("#", ns("customize_panel")),
         role = "button",
         `aria-expanded` = "false",
         icon("chevron-right", class = "customize-chevron"),
         "Customize colors & font"
       )
       ```
       Plus CSS for chevron rotation:
       ```r
       tags$style(HTML(sprintf(
         "#%s.show ~ .customize-toggle .customize-chevron,
          [aria-expanded='true'] .customize-chevron {
            transform: rotate(90deg);
          }
          .customize-chevron { transition: transform 0.2s; }",
         ns("customize_panel")
       )))
       ```

    2. **Collapsible div** wrapping all picker content:
       ```r
       div(
         id = ns("customize_panel"),
         class = "collapse mt-2 p-3 border rounded bg-body-secondary",
         ...
       )
       ```

    3. **2x2 color grid** inside the collapsible div, using `layout_columns(col_widths = c(6, 6))`:
       - Top row: BG (left), Text (right)
       - Bottom row: Accent (left), Link (right)
       - Each cell is a color_picker_pair(ns, id, label) helper producing:
         - `tags$label(label, class = "form-label small fw-semibold")`
         - `div(class = "d-flex align-items-center gap-2", ...)` containing:
           - `tags$input(type = "color", id = ns("{id}_swatch"), value = "#FFFFFF", style = "width:40px;height:38px;padding:2px;border:1px solid #ced4da;border-radius:4px;cursor:pointer;")` — NOTE: not a Shiny input, DOM-only
           - `textInput(ns("{id}_hex"), NULL, value = "#FFFFFF", width = "100px", placeholder = "#RRGGBB")`
         - JS sync script (swatch -> hex):
           ```js
           document.getElementById('{ns(id_swatch)}').addEventListener('input', function(e) {
             var el = document.getElementById('{ns(id_hex)}-shiny-input-container').querySelector('input') || document.getElementById('{ns(id_hex)}');
             if (el) { el.value = e.target.value.toUpperCase(); el.dispatchEvent(new Event('change')); }
           });
           ```
         - JS sync script (hex -> swatch):
           ```js
           document.getElementById('{ns(id_hex)}').addEventListener('input', function(e) {
             var v = e.target.value.trim();
             if (/^#[0-9A-Fa-f]{6}$/.test(v)) {
               document.getElementById('{ns(id_swatch)}').value = v.toLowerCase();
             }
           });
           ```
         - JS validation script (hex field blur):
           ```js
           document.getElementById('{ns(id_hex)}').addEventListener('blur', function(e) {
             var v = e.target.value.trim();
             if (/^#[0-9A-Fa-f]{6}$/.test(v)) {
               e.target.classList.remove('is-invalid');
             } else {
               e.target.classList.add('is-invalid');
             }
           });
           ```

       The 4 input IDs (namespaced): bg_hex, text_hex, accent_hex, link_hex (Shiny textInputs), bg_swatch, text_swatch, accent_swatch, link_swatch (DOM-only).

    4. **Font selector** below the color grid:
       ```r
       selectInput(
         ns("font"),
         "Font",
         choices = CURATED_FONTS,  # from R/themes.R
         selected = "Source Sans Pro"
       )
       ```

    5. **Save row** below the font selector:
       ```r
       div(
         class = "d-flex align-items-center gap-2 mt-3",
         textInput(ns("custom_theme_name"), NULL, placeholder = "Theme name...", width = "200px"),
         actionButton(ns("save_custom_theme"), "Save as custom theme", class = "btn-primary btn-sm", icon = icon_save())
       )
       ```

    **Define color_picker_pair as a local helper function** inside mod_slides_modal_ui() (before the modalDialog call) or as a standalone function at module top level. It takes (ns, id, label) and returns the div structure above.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" — wait for "Listening on" (startup smoke test; kill after 10s)</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_slides.R contains `id = ns("customize_panel")` — the collapsible div
    - R/mod_slides.R contains `tags$input(type = "color"` — native color inputs
    - R/mod_slides.R contains `textInput(ns("bg_hex")` and `textInput(ns("text_hex")` and `textInput(ns("accent_hex")` and `textInput(ns("link_hex")` — all 4 hex fields
    - R/mod_slides.R contains `selectInput(ns("font")` with `choices = CURATED_FONTS`
    - R/mod_slides.R contains `actionButton(ns("save_custom_theme")`
    - R/mod_slides.R contains `textInput(ns("custom_theme_name")`
    - R/mod_slides.R contains `data-bs-toggle` = "collapse" — Bootstrap collapse toggle
    - R/mod_slides.R contains `layout_columns(col_widths = c(6, 6)` — 2x2 grid
    - R/mod_slides.R contains `addEventListener('input'` — JS sync between swatch and hex
    - R/mod_slides.R contains `is-invalid` — hex validation on blur
    - The panel UI is positioned between the theme div block (upload link) and `checkboxInput(ns("include_notes")`
    - App starts without error (smoke test passes)
  </acceptance_criteria>
  <done>Collapsible panel with 4 color picker pairs, font selector, and save row renders in the slides modal. All JS sync and validation wired. App starts without crash.</done>
</task>

<task type="auto">
  <name>Task 2: Wire server-side reactives for pre-fill, swatch update, and save</name>
  <read_first>
    - R/mod_slides.R (server function — lines 340-490+, especially refresh_theme_dropdown, theme_file handler, theme_delete handler, generation handler lines 505-527)
    - R/themes.R (generate_custom_scss, parse_scss_colors_full, CURATED_FONTS, BUILTIN_THEME_SWATCHES, list_custom_themes, build_theme_choices_df)
    - .planning/phases/60-color-picker-and-font-selector/60-RESEARCH.md (Pattern 2: panel pre-fill, Pattern 3: swatch dot update, Pattern 4: save flow)
  </read_first>
  <files>R/mod_slides.R</files>
  <action>
    Add 3 server-side observeEvent blocks to mod_slides_server():

    **1. Theme pre-fill observer** — when the theme dropdown changes, update all picker fields:
    ```r
    observeEvent(input$theme, {
      sel <- input$theme
      req(sel)

      if (sel %in% names(BUILTIN_THEME_SWATCHES)) {
        sw <- BUILTIN_THEME_SWATCHES[[sel]]
        bg <- sw$bg; fg <- sw$fg; acc <- sw$accent
        lnk <- sw$accent  # fallback: link = accent for built-ins
        fnt <- "Source Sans Pro"  # safe default for built-ins
      } else if (nzchar(sel)) {
        path <- file.path("data/themes", sel)
        if (file.exists(path)) {
          scss_text <- paste(readLines(path, warn = FALSE), collapse = "\n")
          full <- parse_scss_colors_full(scss_text)
          bg <- full$bg; fg <- full$fg; acc <- full$accent
          lnk <- full$link; fnt <- full$font
        } else {
          return()
        }
      } else {
        return()
      }

      updateTextInput(session, "bg_hex",     value = bg)
      updateTextInput(session, "text_hex",   value = fg)
      updateTextInput(session, "accent_hex", value = acc)
      updateTextInput(session, "link_hex",   value = lnk)
      updateSelectInput(session, "font",     selected = fnt)

      # Update native color swatches via JS (Shiny doesn't bind raw <input type="color">)
      session$sendCustomMessage("update_color_swatch", list(
        ids = list(
          paste0(ns("bg_swatch")),
          paste0(ns("text_swatch")),
          paste0(ns("accent_swatch")),
          paste0(ns("link_swatch"))
        ),
        values = list(tolower(bg), tolower(fg), tolower(acc), tolower(lnk))
      ))
    }, ignoreInit = TRUE)
    ```

    Also add a `tags$script` in the UI (inside the collapsible panel or at modal level) that registers the custom message handler:
    ```js
    Shiny.addCustomMessageHandler('update_color_swatch', function(msg) {
      for (var i = 0; i < msg.ids.length; i++) {
        var el = document.getElementById(msg.ids[i]);
        if (el) el.value = msg.values[i];
      }
    });
    ```

    **2. Swatch dot live update observer** — when any picker hex field changes (on blur, via Shiny's built-in debounce on textInput change event), rebuild the theme dropdown choices df with overridden colors for the currently selected theme row:
    ```r
    observe({
      # Read all 3 dot-relevant colors
      bg_val  <- input$bg_hex
      fg_val  <- input$text_hex
      acc_val <- input$accent_hex
      req(bg_val, fg_val, acc_val)

      # Only update if values are valid hex
      is_hex <- function(v) grepl("^#[0-9A-Fa-f]{6}$", v)
      if (!all(sapply(c(bg_val, fg_val, acc_val), is_hex))) return()

      sel <- isolate(input$theme)
      req(sel)

      # Rebuild choices df with overridden swatch colors for the active theme
      custom <- list_custom_themes()
      df <- build_theme_choices_df(custom)
      row_idx <- which(df$value == sel)
      if (length(row_idx) == 1) {
        df$bg[row_idx]     <- toupper(bg_val)
        df$fg[row_idx]     <- toupper(fg_val)
        df$accent[row_idx] <- toupper(acc_val)
      }
      updateSelectizeInput(session, "theme", choices = df, selected = sel, server = TRUE)
    })
    ```

    **3. Save custom theme handler:**
    ```r
    observeEvent(input$save_custom_theme, {
      name <- trimws(input$custom_theme_name)
      if (!nzchar(name)) {
        # Focus the name field via JS
        shinyjs::runjs(sprintf("document.getElementById('%s').focus();", ns("custom_theme_name")))
        return()
      }

      path <- generate_custom_scss(
        name       = name,
        bg_color   = input$bg_hex,
        text_color = input$text_hex,
        accent_color = input$accent_hex,
        link_color = input$link_hex,
        font_name  = input$font
      )

      if (!is.null(path)) {
        refresh_theme_dropdown(selected = basename(path))
        showNotification(paste0("Theme '", name, "' saved"), type = "message")
        # Collapse the panel via JS
        shinyjs::runjs(sprintf(
          "var el = document.getElementById('%s'); if (el && el.classList.contains('show')) { new bootstrap.Collapse(el).hide(); }",
          ns("customize_panel")
        ))
      } else {
        showNotification("Could not save theme. Check file permissions.", type = "error")
      }
    })
    ```

    **4. Also update the theme resolution logic** in the generation handler (around lines 505-516). When the user has customized colors via the picker, the generated theme should be used. The current logic already handles custom themes from data/themes/ — no change needed IF the save handler wrote the file and auto-selected it. But add a note comment in the code documenting this flow:
    ```r
    # Theme resolution: if a picker-generated custom theme was saved and auto-selected,
    # it appears as a custom .scss filename in input$theme (same path as uploaded themes).
    # No special handling needed — the existing custom_scss logic covers it.
    ```

    **5. Ensure shinyjs is available** — check that `shinyjs::useShinyjs()` is called somewhere in the app UI (it likely already is from prior phases). If not, add it.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" — wait for "Listening on" (startup smoke test; kill after 10s)</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_slides.R contains `observeEvent(input$theme,` with `parse_scss_colors_full` call — pre-fill observer
    - R/mod_slides.R contains `updateTextInput(session, "bg_hex"` — pre-fill updates hex fields
    - R/mod_slides.R contains `sendCustomMessage("update_color_swatch"` — pushes values to native color inputs
    - R/mod_slides.R contains `Shiny.addCustomMessageHandler('update_color_swatch'` — JS handler registration
    - R/mod_slides.R contains `observeEvent(input$save_custom_theme,` — save handler
    - R/mod_slides.R contains `generate_custom_scss(` — calls the helper from themes.R
    - R/mod_slides.R contains `refresh_theme_dropdown(selected = basename(path))` — auto-selects saved theme
    - R/mod_slides.R contains `showNotification(paste0("Theme '", name, "' saved")` — toast
    - R/mod_slides.R contains `bootstrap.Collapse` — collapses panel after save
    - R/mod_slides.R contains `build_theme_choices_df(custom)` in an observe block — swatch dot update
    - App starts without error (smoke test passes)
  </acceptance_criteria>
  <done>Theme pre-fill works on dropdown change. Swatch dots update when picker values change. Save writes .scss, auto-selects, collapses panel, shows toast. App starts cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete color picker and font selector panel in the slides generation modal. Four native color picker + hex field pairs in a 2x2 grid, a grouped font dropdown, and save-as-custom-theme flow. Collapsible panel below the theme dropdown.</what-built>
  <how-to-verify>
    1. Start the app and open the slide generation modal (click "Generate Slides" on a notebook with documents)
    2. Verify the "Customize colors & font" toggle appears below the theme dropdown, collapsed by default
    3. Click to expand — verify 4 color picker pairs (BG, Text, Accent, Link) in a 2x2 grid, font dropdown, and save row appear
    4. Click a color swatch — OS color picker opens; pick a color and verify the hex text field updates
    5. Type a hex value in a text field (e.g., #FF0000) — verify the paired color swatch updates
    6. Type an invalid hex (e.g., "abc") and tab away — verify red border appears on the field
    7. Change the theme dropdown to a different built-in theme — verify all 4 pickers + font update to that theme's values
    8. If a custom uploaded theme exists (e.g., epa-owm.scss), select it — verify pickers show its parsed colors
    9. Customize some colors, type a theme name (e.g., "Test Theme"), click "Save as custom theme"
    10. Verify: toast notification appears, panel collapses, new theme "test-theme.scss" is auto-selected in dropdown
    11. Verify the swatch dots in the dropdown reflect the saved colors
    12. Open the Generate Slides modal again — select the saved custom theme — verify the picker pre-fills match
    13. Check `data/themes/` directory — verify the new .scss file exists with correct content
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- All tests pass: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-themes.R')"`
- App starts without error (smoke test)
- Human verification of full picker flow
</verification>

<success_criteria>
- Collapsible panel with 4 color pickers + font selector visible in slides modal
- Theme pre-fill works for both built-in and custom themes
- Native color input syncs bidirectionally with hex text field
- Hex validation shows red border on invalid values
- Save produces valid .scss, auto-selects theme, collapses panel, shows toast
- Swatch dots in dropdown update to reflect customized colors
- Phase 61 can populate pickers via updateTextInput/updateSelectInput (THME-10 contract satisfied by design)
</success_criteria>

<output>
After completion, create `.planning/phases/60-color-picker-and-font-selector/60-02-SUMMARY.md`
</output>
