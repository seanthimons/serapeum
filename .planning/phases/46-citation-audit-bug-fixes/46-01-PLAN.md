---
phase: 46-citation-audit-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_citation_audit.R
  - R/citation_audit.R
  - app.R
  - tests/testthat/test-citation-audit.R
autonomous: true
requirements: [BUGF-01, BUGF-02]
must_haves:
  truths:
    - "User can import multiple papers via citation audit batch import without errors"
    - "User can import single papers via citation audit without per-paper error modals"
    - "Papers imported via citation audit immediately appear in abstract notebook without manual page refresh"
    - "Duplicate papers are skipped silently with count in summary toast"
    - "Failed imports are reported in summary toast, not individual error modals"
  artifacts:
    - path: "R/mod_citation_audit.R"
      provides: "Fixed single-paper observer pattern, batch import with progress toast, notebook_refresh trigger"
      contains: "notebook_refresh"
    - path: "R/citation_audit.R"
      provides: "import_audit_papers with skipped_count return value"
      contains: "skipped_count"
    - path: "app.R"
      provides: "navigate_to_notebook callback triggers notebook_refresh"
      contains: "notebook_refresh"
    - path: "tests/testthat/test-citation-audit.R"
      provides: "Tests for duplicate skip counting and multi-paper import"
      contains: "skipped_count"
  key_links:
    - from: "R/mod_citation_audit.R"
      to: "app.R notebook_refresh reactiveVal"
      via: "notebook_refresh callback parameter"
      pattern: "notebook_refresh\\(notebook_refresh\\(\\) \\+ 1\\)"
    - from: "R/mod_citation_audit.R"
      to: "R/citation_audit.R import_audit_papers"
      via: "function call returning import/skip/fail counts"
      pattern: "import_audit_papers"
    - from: "app.R navigate_to_notebook"
      to: "notebook_refresh reactiveVal"
      via: "callback increments reactiveVal"
      pattern: "notebook_refresh"
---

<objective>
Fix two critical citation audit bugs: multi-paper import failures (BUGF-01) and abstract notebook sync after import (BUGF-02).

Purpose: Citation audit is broken for adding papers — users get per-paper errors and imported papers don't appear in abstract notebook until manual refresh. These are the last bug fixes before AI synthesis phases.

Output: Working citation audit import (single and batch) with progress toast, duplicate skip counting, error summary, and reactive notebook refresh.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-citation-audit-bug-fixes/46-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs from the existing codebase -->

From R/citation_audit.R (import_audit_papers, lines 530-595):
```r
import_audit_papers <- function(work_ids, notebook_id, email, api_key, con,
                                 interrupt_flag = NULL, progress_file = NULL)
# Returns: list(imported_count = integer, failed_count = integer)
# Already handles: duplicate filtering via SELECT, per-paper tryCatch, metadata fetch
```

From R/mod_citation_audit.R (module server, lines 61-63):
```r
mod_citation_audit_server <- function(id, con, config_r, db_path,
                                       navigate_to_notebook = NULL,
                                       notebook_refresh = NULL)
```

From app.R (citation audit wiring, lines 1058-1065):
```r
mod_citation_audit_server("citation_audit", con, config_r = effective_config,
  db_path = db_path,
  navigate_to_notebook = function(notebook_id) {
    current_notebook(notebook_id)
    current_view("notebook")
  },
  notebook_refresh = notebook_refresh  # <-- passed but not triggered after import
)
```

Key issue #1 — Single-paper import observer (lines 560-595):
- observe() recreates observeEvent(once=TRUE) each time audit_results() changes
- This causes stale/duplicate observers and potential error cascading

Key issue #2 — Neither single nor batch import triggers notebook_refresh():
- Batch handler calls navigate_to_notebook() which does NOT increment notebook_refresh
- Single handler has no refresh trigger at all

Key issue #3 — import_audit_papers() returns no skipped_count:
- Already filters duplicates but doesn't report how many were skipped
- User decision requires summary like "Added 5 papers, 2 already existed (skipped)"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix import_audit_papers to return skipped_count and add progress callback</name>
  <files>R/citation_audit.R, tests/testthat/test-citation-audit.R</files>
  <behavior>
    - Test: import_audit_papers with mix of new and existing papers returns correct imported_count, failed_count, AND skipped_count
    - Test: import_audit_papers with ALL duplicates returns skipped_count == length(work_ids), imported_count == 0
    - Test: import_audit_papers with empty work_ids returns all counts as 0 (including skipped_count)
    - Test: import_audit_papers with progress_callback parameter calls callback with (current_index, total_count) on each paper
  </behavior>
  <action>
    1. In R/citation_audit.R `import_audit_papers()` (line 530):
       - Add `skipped_count` tracking: set `skipped <- length(work_ids) - length(new_ids)` after the duplicate filter (line 543)
       - Add optional `progress_callback = NULL` parameter to function signature
       - Inside the for loop (line 555), after each paper (success or fail), call `if (!is.null(progress_callback)) progress_callback(i, length(metadata))` where `i` is the loop index
       - Update return value to include `skipped_count`: `list(imported_count = imported, failed_count = failed, skipped_count = skipped)`
       - Also update the early return on line 544 to include `skipped_count = length(work_ids)` (all were duplicates)
       - Also update the outer tryCatch error return (line 593) to include `skipped_count = 0L`

    2. In tests/testthat/test-citation-audit.R:
       - Add test for skipped_count when importing duplicates
       - Add test for progress_callback invocation count
       - Verify existing tests still pass (they should since we only added a new return field)
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-citation-audit.R')"</automated>
  </verify>
  <done>import_audit_papers returns skipped_count in all code paths, progress_callback is invoked per-paper, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Fix module import handlers and wire notebook_refresh</name>
  <files>R/mod_citation_audit.R, app.R</files>
  <action>
    **Fix 1: Single-paper import observer pattern (BUGF-01 root cause)**

    The current observe() at line 560 creates new observeEvent(once=TRUE) observers each time audit_results() changes. This causes duplicate/stale observers. Replace with a pattern that tracks which observers have been created to avoid duplicates.

    In R/mod_citation_audit.R, replace lines 560-595 (the observe + for loop creating per-button observers). Keep the per-button `input[[btn_id]]` pattern but track which observers have been created in a character vector reactive. Only create an observer for a button ID if it hasn't been created yet:

    ```r
    created_observers <- reactiveVal(character(0))

    observe({
      results <- audit_results()
      if (is.null(results) || nrow(results) == 0) return()

      already <- created_observers()

      for (wid in results$work_id) {
        local({
          my_wid <- wid
          btn_id <- paste0("imp_", my_wid)
          if (btn_id %in% already) return()  # Skip if already created

          observeEvent(input[[btn_id]], {
            cfg <- config_r()
            email <- get_setting(cfg, "openalex", "email")
            api_key <- get_setting(cfg, "openalex", "api_key")

            # Show progress notification
            notif_id <- showNotification("Importing paper...", duration = NULL, closeButton = FALSE, type = "message")

            result <- import_audit_papers(
              work_ids = my_wid,
              notebook_id = input$notebook_id,
              email = email,
              api_key = api_key,
              con = con
            )

            removeNotification(notif_id)

            if (result$imported_count > 0) {
              showNotification("Paper imported successfully", type = "message", duration = 5)
              # Trigger notebook refresh so abstract notebook updates
              if (!is.null(notebook_refresh)) {
                notebook_refresh(notebook_refresh() + 1)
              }
            } else if (result$skipped_count > 0) {
              showNotification("Paper already exists in notebook", type = "warning", duration = 5)
            } else {
              showNotification("Import failed", type = "error", duration = 5)
            }

            # Refresh audit results to update imported status
            latest <- get_latest_audit_run(con, input$notebook_id)
            if (!is.null(latest)) {
              check_audit_imports(con, latest$id, input$notebook_id)
              audit_results(get_audit_results(con, latest$id))
            }
          }, ignoreInit = TRUE)
        })
      }

      # Track all created observers
      created_observers(unique(c(already, paste0("imp_", results$work_id))))
    })
    ```

    Note: Remove `once = TRUE` from observeEvent — the observer should persist so users can re-import to a different notebook if needed. The duplicate detection in import_audit_papers handles repeated clicks.

    **Fix 2: Batch import handler — add progress toast and notebook_refresh (BUGF-01 + BUGF-02)**

    In R/mod_citation_audit.R, update the `confirm_batch` handler (lines 613-651):

    1. Replace `withProgress()` with Shiny `showNotification()` progress pattern (per research Pattern 4)
    2. Pass a `progress_callback` to `import_audit_papers` that updates the notification
    3. After import, show summary toast with imported/skipped/failed counts per user decision: "Added 5 papers, 2 already existed (skipped)" or "Added 5 papers, 2 already existed, 1 failed"
    4. After successful import, trigger `notebook_refresh()`:
       ```r
       if (!is.null(notebook_refresh) && result$imported_count > 0) {
         notebook_refresh(notebook_refresh() + 1)
       }
       ```
    5. Keep the `navigate_to_notebook()` call for navigation, but the refresh is now handled directly

    **Fix 3: Wire notebook_refresh in navigate_to_notebook callback (BUGF-02)**

    In app.R, update the `navigate_to_notebook` callback (lines 1060-1063) to also trigger notebook_refresh:
    ```r
    navigate_to_notebook = function(notebook_id) {
      current_notebook(notebook_id)
      current_view("notebook")
      notebook_refresh(notebook_refresh() + 1)
    }
    ```

    This ensures abstract notebook refreshes regardless of which code path triggers navigation.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" &amp; sleep 8 &amp;&amp; curl -s http://localhost:3838 | head -c 200 ; kill %1 2>/dev/null</automated>
  </verify>
  <done>
    - Single-paper import buttons work without creating duplicate observers or per-paper error modals
    - Batch import shows updating progress toast ("Importing papers... 3/7") instead of blocking withProgress
    - Summary toast shows imported/skipped/failed counts ("Added 5 papers, 2 already existed")
    - notebook_refresh() triggered after both single and batch import
    - App starts without errors (smoke test)
  </done>
</task>

</tasks>

<verification>
1. Unit tests pass: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-citation-audit.R')"`
2. App smoke test: App starts and loads citation audit view without errors
3. Manual verification checklist (for UAT):
   - Run citation audit on a notebook with papers
   - Import a single paper via "Import" button - paper appears in abstract notebook
   - Import multiple papers via batch - progress toast updates, summary shows counts
   - Re-import same papers - shows "already existed (skipped)" message
   - No modal error dialogs during any import operation
</verification>

<success_criteria>
- BUGF-01: Multi-paper citation audit import completes without errors, shows progress and summary toast
- BUGF-02: Papers imported via citation audit appear in abstract notebook without manual refresh
- No regression in existing citation audit functionality (test suite passes)
- App starts cleanly (smoke test)
</success_criteria>

<output>
After completion, create `.planning/phases/46-citation-audit-bug-fixes/46-01-SUMMARY.md`
</output>
