---
phase: 39-slide-healing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - R/slides.R
  - tests/testthat/test-slides.R
autonomous: true
gap_closure: true
requirements: SLIDE-01

must_haves:
  truths:
    - "System prompt for slide generation includes concrete Quarto/RevealJS syntax examples"
    - "Citation instructions show actual ^1 superscript syntax and reference list format"
    - "Healing prompt includes same format reference for self-correction"
    - "LLM can produce correct footnotes and speaker notes without user teaching it syntax"
  artifacts:
    - path: "R/slides.R"
      provides: "Enhanced build_slides_prompt() with format reference section"
      contains: "Quarto/RevealJS Format Reference"
    - path: "R/slides.R"
      provides: "Enhanced build_healing_prompt() with format reference section"
      contains: "Quarto/RevealJS Format Reference"
    - path: "tests/testthat/test-slides.R"
      provides: "Tests verifying format reference content"
      contains: "format reference"
  key_links:
    - from: "R/slides.R"
      to: "build_slides_prompt() system prompt"
      via: "appends format reference section"
      pattern: "Format Reference.*footnote.*speaker notes"
    - from: "R/slides.R"
      to: "build_healing_prompt() system prompt"
      via: "appends format reference section"
      pattern: "Format Reference.*footnote.*speaker notes"
---

<objective>
Add concrete Quarto/RevealJS syntax examples to slide generation and healing system prompts so LLMs can produce correct output (footnotes, speaker notes, tables) without user intervention.

Purpose: Close the gap identified in UAT test 9 where the model doesn't improve responses from chip prompts + errors alone because prompts lack distilled formatting references.

Output: Updated R/slides.R with enhanced prompts containing working syntax examples, and updated tests verifying the new content.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-slide-healing/39-UAT.md

# Gap diagnosis
Gap identified in UAT test 9:
- **Truth failed**: "Slide generation prompt includes sufficient formatting reference for RevealJS/Quarto constructs"
- **Root cause**: System prompts provide abstract instructions without concrete syntax examples
- **Evidence**: User reported "Model doesn't improve response from chip prompts + errors alone. It revised footnotes only when given proper format explicitly."
- **Artifacts affected**: R/slides.R lines 64-90 (build_slides_prompt), lines 382-394 (build_healing_prompt)

# Current prompt structure
From R/slides.R:
- build_slides_prompt() lines 64-90: Has YAML template, content rules, citation_instructions
- Citation instructions (lines 84-90): Says "add superscript numbers" but doesn't show ^1 syntax
- Speaker notes mention: Says "Include speaker notes using ::: {.notes} blocks" but no complete example
- build_healing_prompt() lines 382-394: Has YAML template but no format reference at all

# What's missing
- Footnote syntax examples (^1 notation, reference list format)
- Speaker notes complete structure example
- Table syntax example
- These examples needed in BOTH build_slides_prompt() and build_healing_prompt()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quarto/RevealJS format reference to build_slides_prompt()</name>
  <files>R/slides.R</files>
  <action>
Enhance the system_prompt in build_slides_prompt() (currently lines 64-81) by adding a "Quarto/RevealJS Format Reference" section immediately after the YAML template and before the content rules.

Add this new section between the YAML template block and the "Content rules:" line:

```
"\n\nQuarto/RevealJS Format Reference:\n",
"Footnotes: Add superscript citation numbers with ^1 syntax, then list references at end\n",
"  Example: 'Machine learning improves accuracy^1'\n",
"  Reference list: '## References\\n\\n1. Author et al., Journal, 2023'\n\n",
"Speaker notes: Use fenced div with .notes class\n",
"  Example: '## Slide Title\\n\\nContent here\\n\\n::: {.notes}\\nPresenter note text\\n:::'\n\n",
"Tables: Use markdown pipe syntax with alignment\n",
"  Example: '| Method | Accuracy |\\n|:-------|:--------:|\\n| CNN | 95% |'\n\n",
```

Update citation_instructions for the "footnotes" case to show actual syntax:
- Current: "Use footnote-style citations: add superscript numbers after key points and list references at the end."
- Updated: "Use footnote-style citations: add ^1 superscript numbers after key points (e.g., 'key finding^1'), then add '## References' slide with numbered list at the end."

Preserve all other functionality (length mapping, audience, include_notes conditional, custom_instructions).
  </action>
  <verify>
```r
testthat::test_file("tests/testthat/test-slides.R", filter = "format reference in slides prompt")
```
Passes and shows format reference content in system prompt.
  </verify>
  <done>
build_slides_prompt() system prompt contains a "Quarto/RevealJS Format Reference" section with working examples for footnotes (^1 syntax), speaker notes (::: {.notes} structure), and tables (markdown pipe syntax). Citation instructions for footnotes style show actual ^1 syntax.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Quarto/RevealJS format reference to build_healing_prompt()</name>
  <files>R/slides.R</files>
  <action>
Enhance the system_prompt in build_healing_prompt() (currently lines 382-394) by adding the same "Quarto/RevealJS Format Reference" section.

Add the reference section after the YAML template block and before the "Output ONLY the complete fixed .qmd content" line:

```
"\n\nQuarto/RevealJS Format Reference:\n",
"Footnotes: Add superscript citation numbers with ^1 syntax, then list references at end\n",
"  Example: 'Machine learning improves accuracy^1'\n",
"  Reference list: '## References\\n\\n1. Author et al., Journal, 2023'\n\n",
"Speaker notes: Use fenced div with .notes class\n",
"  Example: '## Slide Title\\n\\nContent here\\n\\n::: {.notes}\\nPresenter note text\\n:::'\n\n",
"Tables: Use markdown pipe syntax with alignment\n",
"  Example: '| Method | Accuracy |\\n|:-------|:--------:|\\n| CNN | 95% |'\n\n",
```

This gives the healing LLM the same concrete syntax examples so it can self-correct formatting issues without user teaching.
  </action>
  <verify>
```r
testthat::test_file("tests/testthat/test-slides.R", filter = "format reference in healing prompt")
```
Passes and shows format reference content in healing system prompt.
  </verify>
  <done>
build_healing_prompt() system prompt contains the same "Quarto/RevealJS Format Reference" section with working syntax examples, enabling the healing LLM to self-correct formatting issues.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests verifying format reference content</name>
  <files>tests/testthat/test-slides.R</files>
  <action>
Add two new test cases after the existing "build_slides_prompt includes YAML template" test (around line 148):

```r
test_that("build_slides_prompt includes format reference in slides prompt", {
  chunks <- data.frame(
    content = "Test content",
    doc_name = "test.pdf",
    page_number = 1,
    stringsAsFactors = FALSE
  )

  prompt <- build_slides_prompt(chunks, list(citation_style = "footnotes"))

  # Check for format reference section
  expect_true(grepl("Format Reference", prompt$system))
  expect_true(grepl("\\^1", prompt$system))  # Footnote syntax example
  expect_true(grepl("::: \\{.notes\\}", prompt$system))  # Speaker notes syntax
  expect_true(grepl("\\| Method \\|", prompt$system))  # Table syntax

  # Check updated citation instructions
  expect_true(grepl("\\^1 superscript numbers", prompt$user))
})

test_that("build_healing_prompt includes format reference in healing prompt", {
  previous_qmd <- "---\ntitle: Test\n---\n\n## Slide"
  errors <- character(0)
  instructions <- "Fix footnotes"

  prompt <- build_healing_prompt(previous_qmd, errors, instructions)

  # Check for format reference section
  expect_true(grepl("Format Reference", prompt$system))
  expect_true(grepl("\\^1", prompt$system))  # Footnote syntax example
  expect_true(grepl("::: \\{.notes\\}", prompt$system))  # Speaker notes syntax
  expect_true(grepl("\\| Method \\|", prompt$system))  # Table syntax
})
```

These tests verify that both prompts contain the format reference with concrete syntax examples.
  </action>
  <verify>
```r
testthat::test_file("tests/testthat/test-slides.R")
```
All tests pass, including the two new format reference tests.
  </verify>
  <done>
tests/testthat/test-slides.R contains two new tests verifying that both build_slides_prompt() and build_healing_prompt() include the Quarto/RevealJS format reference section with footnote, speaker notes, and table syntax examples.
  </done>
</task>

</tasks>

<verification>
**Prompt content verification:**
1. Run `Rscript -e "source('R/slides.R'); p <- build_slides_prompt(data.frame(content='test', doc_name='test.pdf', page_number=1), list(citation_style='footnotes')); cat(p$system)"` and confirm "Format Reference" section appears with ^1, ::: {.notes}, and table examples
2. Run `Rscript -e "source('R/slides.R'); p <- build_healing_prompt('test', character(0), 'fix'); cat(p$system)"` and confirm same format reference section

**Test verification:**
3. Run `testthat::test_file("tests/testthat/test-slides.R")` and confirm all tests pass, including new format reference tests

**Manual validation:**
4. Generate slides with the updated prompts and observe that footnotes use ^1 syntax and speaker notes use ::: {.notes} structure without user having to specify syntax
</verification>

<success_criteria>
- [ ] build_slides_prompt() system prompt contains "Quarto/RevealJS Format Reference" section with footnote (^1), speaker notes (::: {.notes}), and table syntax examples
- [ ] Citation instructions for "footnotes" style explicitly show ^1 syntax in user prompt
- [ ] build_healing_prompt() system prompt contains the same format reference section
- [ ] Two new tests verify format reference content in both prompts
- [ ] All tests in test-slides.R pass
- [ ] Code changes committed with descriptive message
- [ ] Gap closure addresses UAT test 9 failure: LLM can now produce correct Quarto formatting without user teaching it syntax
</success_criteria>

<output>
After completion, create `.planning/phases/39-slide-healing/39-03-SUMMARY.md` documenting:
- Specific format reference content added
- Changes to citation_instructions
- Test coverage added
- How this addresses the UAT gap (user no longer needs to explicitly teach syntax)
</output>
