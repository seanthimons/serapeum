---
phase: 38-select-all-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_search_notebook.R
  - www/js/select-all.js
autonomous: true
requirements:
  - SLCT-01
must_haves:
  truths:
    - "User can check a select-all checkbox to select all filtered papers"
    - "Select-all checkbox shows indeterminate state when some papers are individually deselected"
    - "Clicking select-all when partial selections exist adds remaining papers (additive)"
    - "Changing any filter resets select-all state and clears all selections"
    - "Import button shows count: Import Selected (N)"
  artifacts:
    - path: "www/js/select-all.js"
      provides: "Tri-state checkbox JS handler"
      min_lines: 10
    - path: "R/mod_search_notebook.R"
      provides: "Select-all reactive state, checkbox UI, button count"
  key_links:
    - from: "R/mod_search_notebook.R"
      to: "www/js/select-all.js"
      via: "session$sendCustomMessage('setCheckboxState')"
      pattern: "setCheckboxState"
---

<objective>
Add select-all checkbox with tri-state behavior to the search notebook paper list, including state management that correctly merges with individual paper selections and resets on filter changes.

Purpose: Enable users to quickly select all filtered papers (SLCT-01) before batch importing them.
Output: Select-all checkbox in paper list header, dynamic "Import Selected (N)" button text, tri-state JS handler.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-select-all-import/38-RESEARCH.md
@R/mod_search_notebook.R
@www/js/import-progress.js

<interfaces>
<!-- Key interfaces the executor needs from mod_search_notebook.R -->

Current paper list UI (lines 148-157):
```r
div(
  id = ns("paper_list_container"),
  style = "max-height: 400px; overflow-y: auto;",
  uiOutput(ns("paper_list"))
),
hr(),
uiOutput(ns("selection_info")),
actionButton(ns("import_selected"), "Import Selected to Notebook",
             class = "btn-primary w-100",
             icon = icon("download"))
```

Current selection tracking (lines 1270-1283):
```r
observe({
  papers <- filtered_papers()
  if (nrow(papers) == 0) return()
  selected <- character()
  for (i in seq_len(nrow(papers))) {
    paper <- papers[i, ]
    checkbox_id <- paste0("select_", paper$id)
    if (isTRUE(input[[checkbox_id]])) {
      selected <- c(selected, paper$id)
    }
  }
  selected_papers(selected)
})
```

Current selection_info output (lines 1286-1295):
```r
output$selection_info <- renderUI({
  n <- length(selected_papers())
  if (n == 0) {
    span(class = "text-muted small", "Select papers to import")
  } else {
    span(class = "text-primary small fw-semibold",
         paste(n, "paper(s) selected"))
  }
})
```

Paper list rendering per-paper checkbox (line 1165, 1197):
```r
checkbox_id <- paste0("select_", paper$id)
checkboxInput(ns(checkbox_id), label = NULL, width = "25px")
```

filtered_papers() reactive (line 889):
```r
filtered_papers <- reactive({
  papers <- journal_filtered_papers()
  # ... has_abstract filter, year range filter
  papers
})
```

selected_papers reactiveVal (line 331):
```r
selected_papers <- reactiveVal(character())
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tri-state checkbox JS handler and select-all UI</name>
  <files>www/js/select-all.js, R/mod_search_notebook.R</files>
  <action>
1. Create `www/js/select-all.js` with a Shiny custom message handler:
   ```javascript
   // Guard against double-registration (same pattern as import-progress.js)
   if (!window._selectAllRegistered) {
     window._selectAllRegistered = true;
     Shiny.addCustomMessageHandler('setCheckboxState', function(data) {
       var cb = document.getElementById(data.id);
       if (cb) {
         cb.checked = data.checked;
         cb.indeterminate = data.indeterminate || false;
       }
     });
   }
   ```

2. In `mod_search_notebook_ui`, add:
   - Load the JS file: `tags$script(src = "js/select-all.js")` at the top of the tagList (same pattern as `import-progress.js` in mod_bulk_import_ui).
   - Add a select-all checkbox ABOVE the paper_list_container div (between the sort/filter controls and the paper list). Place it in a small div with flexbox alignment:
     ```r
     div(
       class = "d-flex align-items-center gap-2 mb-2",
       tags$input(
         type = "checkbox",
         id = ns("select_all_cb"),
         class = "form-check-input",
         onclick = sprintf("Shiny.setInputValue('%s', this.checked, {priority: 'event'})", ns("select_all_click"))
       ),
       tags$label(
         `for` = ns("select_all_cb"),
         class = "form-check-label small text-muted",
         "Select all"
       )
     )
     ```
   NOTE: Use raw HTML `tags$input` instead of `checkboxInput()` because we need to set the `id` attribute directly for JS manipulation and need the raw click event (not Shiny's filtered change event).

3. Replace the static "Import Selected to Notebook" button with dynamic text showing count:
   - Change the `actionButton` for `import_selected` to use `uiOutput(ns("import_button"))` so we can dynamically update the label.
   - Add a new `output$import_button` renderUI:
     ```r
     output$import_button <- renderUI({
       n <- length(selected_papers())
       label <- if (n > 0) paste0("Import Selected (", n, ")") else "Import Selected to Notebook"
       actionButton(ns("import_selected"), label,
                    class = "btn-primary w-100",
                    icon = icon("download"),
                    disabled = if (n == 0) "disabled" else NULL)
     })
     ```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/mod_search_notebook.R'); cat('Module source OK\n')" 2>&1 | head -5</automated>
  </verify>
  <done>Select-all checkbox visible in paper list header, JS handler loaded, import button shows dynamic count</done>
</task>

<task type="auto">
  <name>Task 2: Implement select-all state management with flag+exception pattern</name>
  <files>R/mod_search_notebook.R</files>
  <action>
1. Add a new reactiveVal for select-all state in `mod_search_notebook_server` (near line 331 where `selected_papers` is defined):
   ```r
   # Select-all state: flag + exception set (Phase 38)
   # When all_selected=TRUE, effective selection = all filtered IDs minus exceptions
   # When all_selected=FALSE, effective selection = exceptions (individually checked)
   select_all_state <- reactiveVal(list(all_selected = FALSE, exceptions = character()))
   ```

2. Add a reactive to compute effective selected papers from the state:
   ```r
   # Compute effective selection from select-all state (Phase 38)
   effective_selected <- reactive({
     state <- select_all_state()
     all_ids <- filtered_papers()$id
     if (state$all_selected) {
       setdiff(all_ids, state$exceptions)
     } else {
       intersect(state$exceptions, all_ids)
     }
   })
   ```

3. Replace the existing `selected_papers` observer (lines 1270-1283) to read from `effective_selected()`:
   ```r
   # Track selected papers from effective selection (Phase 38)
   observe({
     selected_papers(effective_selected())
   })
   ```

4. Handle select-all checkbox click (the `input$select_all_click` event from the HTML onclick):
   ```r
   observeEvent(input$select_all_click, {
     if (isTRUE(input$select_all_click)) {
       # Select all: set flag, clear exceptions
       select_all_state(list(all_selected = TRUE, exceptions = character()))
     } else {
       # Deselect all: clear flag, clear exceptions
       select_all_state(list(all_selected = FALSE, exceptions = character()))
     }
   }, ignoreInit = TRUE)
   ```

5. Handle individual paper checkbox changes. Modify the paper list rendering (around line 1197) to add an onclick that sends the paper ID to Shiny:
   In the paper list rendering, change the checkbox from:
   ```r
   checkboxInput(ns(checkbox_id), label = NULL, width = "25px")
   ```
   to a raw HTML checkbox that sends events via JS:
   ```r
   tags$input(
     type = "checkbox",
     id = ns(checkbox_id),
     class = "form-check-input",
     checked = if (paper$id %in% effective_selected()) "checked" else NULL,
     onclick = sprintf(
       "Shiny.setInputValue('%s', {id: '%s', checked: this.checked}, {priority: 'event'})",
       ns("paper_checkbox_click"), paper$id
     )
   )
   ```
   This sends `{id, checked}` as input value on each click.

6. Add observer for individual paper checkbox changes:
   ```r
   observeEvent(input$paper_checkbox_click, {
     click <- input$paper_checkbox_click
     req(click$id)
     state <- select_all_state()

     if (state$all_selected) {
       # In select-all mode: toggle exception
       if (isTRUE(click$checked)) {
         # Re-checking a deselected paper: remove from exceptions
         state$exceptions <- setdiff(state$exceptions, click$id)
       } else {
         # Unchecking a paper: add to exceptions
         state$exceptions <- union(state$exceptions, click$id)
       }
     } else {
       # In individual mode: toggle in exceptions set
       if (isTRUE(click$checked)) {
         state$exceptions <- union(state$exceptions, click$id)
       } else {
         state$exceptions <- setdiff(state$exceptions, click$id)
       }
     }
     select_all_state(state)
   }, ignoreInit = TRUE)
   ```

7. Update the select-all checkbox visual state whenever selection changes:
   ```r
   observe({
     state <- select_all_state()
     all_ids <- filtered_papers()$id
     n_selected <- length(effective_selected())
     n_total <- length(all_ids)

     if (n_total == 0) {
       session$sendCustomMessage('setCheckboxState', list(
         id = ns("select_all_cb"), checked = FALSE, indeterminate = FALSE
       ))
     } else if (n_selected == n_total) {
       session$sendCustomMessage('setCheckboxState', list(
         id = ns("select_all_cb"), checked = TRUE, indeterminate = FALSE
       ))
     } else if (n_selected > 0) {
       session$sendCustomMessage('setCheckboxState', list(
         id = ns("select_all_cb"), checked = FALSE, indeterminate = TRUE
       ))
     } else {
       session$sendCustomMessage('setCheckboxState', list(
         id = ns("select_all_cb"), checked = FALSE, indeterminate = FALSE
       ))
     }
   })
   ```

8. Reset select-all state when filters change. Add an observer that watches `filtered_papers()`:
   ```r
   # Reset select-all when filters change (Phase 38)
   # Use observeEvent on the filtered papers IDs to detect filter changes
   observe({
     # Track the set of filtered paper IDs
     ids <- filtered_papers()$id
     # Reset selection state
     select_all_state(list(all_selected = FALSE, exceptions = character()))
     # Reset checkbox visually
     session$sendCustomMessage('setCheckboxState', list(
       id = ns("select_all_cb"), checked = FALSE, indeterminate = FALSE
     ))
   }) |> bindEvent(filtered_papers(), ignoreInit = TRUE)
   ```
   NOTE: Use `bindEvent` with `ignoreInit = TRUE` so it only fires on actual filter changes, not on initial render.

9. Remove the old `output$selection_info` renderUI (lines 1286-1295) since the count is now shown on the button itself. Replace it with a minimal helper or remove the `uiOutput(ns("selection_info"))` from the UI and the corresponding renderUI.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/mod_search_notebook.R'); cat('Module source OK\n')" 2>&1 | head -5</automated>
  </verify>
  <done>
  - Select-all checkbox selects all filtered papers
  - Individual deselection after select-all works (checkbox goes indeterminate)
  - Additive select-all when partial selections exist
  - Filter changes reset all selections
  - Import button shows "Import Selected (N)" count
  - No infinite reactive loops
  </done>
</task>

</tasks>

<verification>
1. Source R/mod_search_notebook.R without errors
2. Verify select-all.js exists with setCheckboxState handler
3. Verify no circular reactive dependencies (select_all_state -> effective_selected -> selected_papers is one-directional)
4. Verify the import button dynamically shows selection count
</verification>

<success_criteria>
- Select-all checkbox appears in paper list header area
- Clicking select-all sets all filtered paper IDs as selected
- Individual paper deselection transitions select-all to indeterminate
- Additive select-all behavior (partial -> all) works
- Filter changes reset selection state to empty
- Import button text shows "Import Selected (N)" with current count
- No reactive loops or UI freezing
</success_criteria>

<output>
After completion, create `.planning/phases/38-select-all-import/38-01-SUMMARY.md`
</output>
