---
phase: 41-physics-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_citation_network.R
autonomous: false
requirements:
  - PHYS-01
  - PHYS-02
must_haves:
  truths:
    - "Toggling physics on/off after navigating away from the network tab does NOT collapse nodes into a singularity"
    - "Small networks (<=20 nodes) exhibit gentle ambient orbital drift after stabilization completes"
    - "Large networks (>20 nodes) freeze after stabilization — no drift"
    - "Rapid physics toggle clicking does not cause intermediate glitches or collapse"
    - "User interaction (drag/click) pauses ambient drift; drift resumes after interaction ends"
    - "New data arriving while physics is off auto-enables physics for layout"
  artifacts:
    - path: "R/mod_citation_network.R"
      provides: "Debounced physics toggle, stabilization event handler, interaction-aware ambient drift"
      contains: "physics_toggle_debounced"
    - path: "R/mod_citation_network.R"
      provides: "stabilizationIterationsDone event wiring"
      contains: "stabilization_done"
  key_links:
    - from: "R/mod_citation_network.R (physics toggle handler)"
      to: "visNetworkProxy visPhysics"
      via: "debounced reactive with position validation"
      pattern: "physics_toggle_debounced.*visPhysics"
    - from: "R/mod_citation_network.R (stabilization event)"
      to: "conditional freeze/drift logic"
      via: "stabilizationIterationsDone -> Shiny input -> observeEvent"
      pattern: "stabilization_done.*n_nodes.*20"
    - from: "R/mod_citation_network.R (interaction events)"
      to: "drift pause/resume"
      via: "dragStart/dragEnd events -> debounced resume"
      pattern: "interaction_active.*drift"
---

<objective>
Fix the singularity collapse bug on physics toggle (PHYS-01) and add ambient orbital drift for small networks after stabilization (PHYS-02).

Purpose: The physics toggle currently re-enables physics without validating node positions, causing all nodes to collapse to (0,0). Small/single-seed networks also feel lifeless after stabilization because physics is uniformly frozen. This plan fixes both issues in a single pass since they share the same code path.

Output: Updated `R/mod_citation_network.R` with debounced physics toggle, position-aware re-enable, stabilization event handler with size-conditional drift, and interaction-aware drift pausing.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-physics-stabilization/41-CONTEXT.md
@.planning/phases/41-physics-stabilization/41-RESEARCH.md
@R/mod_citation_network.R

<interfaces>
<!-- Key code the executor needs. Extracted from R/mod_citation_network.R. -->

Current physics toggle handler (lines 757-762) — will be replaced:
```r
observeEvent(input$physics_enabled, {
  req(current_network_data())
  visNetwork::visNetworkProxy(session$ns("network_graph")) |>
    visNetwork::visPhysics(enabled = input$physics_enabled)
}, ignoreInit = TRUE)
```

Physics config for fresh networks (lines 593-604) — will gain stabilization event:
```r
vn <- vn |>
  visNetwork::visPhysics(
    solver = "forceAtlas2Based",
    forceAtlas2Based = list(
      gravitationalConstant = gravity,
      springLength = spring,
      damping = 0.4
    ),
    stabilization = list(iterations = stab_iters)
  ) |>
  visNetwork::visLayout(randomSeed = 42)
```

Existing visEvents (lines 636-641) — will gain stabilization + interaction events:
```r
visNetwork::visEvents(
  click = sprintf("function(params) {
    if (params.nodes.length > 0) {
      Shiny.setInputValue('%s', params.nodes[0], {priority: 'event'});
    }
  }", session$ns("node_clicked"))
)
```

UI switch (lines 158-162):
```r
bslib::input_switch(
  ns("physics_enabled"),
  "Physics Simulation",
  value = TRUE
)
```

current_network_data reactiveVal (line 183):
```r
current_network_data <- reactiveVal(NULL)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stabilization event, interaction events, and debounced physics toggle</name>
  <files>R/mod_citation_network.R</files>
  <action>
  This task modifies `R/mod_citation_network.R` to fix PHYS-01 (collapse bug) and enable PHYS-02 (ambient drift).

  **Step 1: Add stabilization + interaction events to visEvents (around line 636)**

  Extend the existing `visNetwork::visEvents()` call to include:
  - `stabilizationIterationsDone` event: fires `Shiny.setInputValue('{ns}-stabilization_done', Date.now(), {priority: 'event'})` so we get a unique value each time
  - `dragStart` event: fires `Shiny.setInputValue('{ns}-user_interacting', true, {priority: 'event'})`
  - `dragEnd` event: fires `Shiny.setInputValue('{ns}-user_interacting', false, {priority: 'event'})`

  Keep the existing `click` event handler for node selection unchanged.

  **Step 2: Add a reactive to track whether ambient drift is active**

  Near the other reactiveVals (around line 183), add:
  ```r
  ambient_drift_active <- reactiveVal(FALSE)
  ```

  **Step 3: Add stabilization handler (PHYS-02 — conditional freeze/drift)**

  After the physics toggle handler, add an `observeEvent(input$stabilization_done, ...)` handler:
  1. Get `net_data <- current_network_data(); req(net_data)`
  2. Count nodes: `n_nodes <- nrow(net_data$nodes)`
  3. If `n_nodes <= 20` (with `#NOTE: Ambient drift threshold — tuneable` comment):
     - Set `ambient_drift_active(TRUE)`
     - Use `visNetworkProxy` to call `visPhysics` with:
       - `enabled = TRUE`
       - `solver = "forceAtlas2Based"`
       - `forceAtlas2Based = list(gravitationalConstant = -50, centralGravity = 0.005, damping = 0.25)` with `#NOTE: Drift speed — tuneable. Lower damping = longer orbit (~30-60s). centralGravity keeps nodes loosely centered.`
     - This creates gentle orbital drift: low gravity keeps them close, low damping lets them float, central gravity prevents escape
  4. If `n_nodes > 20`:
     - Set `ambient_drift_active(FALSE)`
     - Use `visNetworkProxy` to call `visPhysics(enabled = FALSE)` to freeze

  Use `ignoreInit = TRUE`.

  **Step 4: Replace the physics toggle handler (PHYS-01 — collapse fix)**

  Replace lines 757-762 with a debounced physics toggle:

  1. Create debounced reactive:
     ```r
     physics_toggle_debounced <- reactive({
       input$physics_enabled
     }) |> debounce(300)  # #NOTE: Debounce delay — tuneable. Prevents rapid toggle glitches
     ```

  2. Replace the `observeEvent(input$physics_enabled, ...)` with `observeEvent(physics_toggle_debounced(), ...)`:
     - Get `enabled <- physics_toggle_debounced()`
     - `req(!is.null(enabled))` to guard
     - If `enabled == TRUE`:
       - Get `net_data <- current_network_data(); req(net_data)`
       - Validate positions exist: check that nodes have `x` or `x_position` columns with non-NA values. If positions are missing or all NA, call `compute_layout_positions()` to generate them first (this prevents the singularity — vis.js needs valid starting positions)
       - Re-enable physics via proxy: `visNetworkProxy |> visPhysics(enabled = TRUE)`
       - Comment: `# PHYS-01: Position validation prevents singularity collapse on re-enable`
     - If `enabled == FALSE`:
       - Instant freeze: `visNetworkProxy |> visPhysics(enabled = FALSE)`
       - Set `ambient_drift_active(FALSE)`
       - Comment: `# Instant freeze — nodes stop immediately where they are`
     - Use `ignoreInit = TRUE`

  **Step 5: Add interaction-aware drift pausing**

  Add a new handler for `input$user_interacting`:
  1. When `input$user_interacting == TRUE` and `ambient_drift_active()`:
     - Temporarily disable physics: `visNetworkProxy |> visPhysics(enabled = FALSE)`

  Add a debounced reactive for interaction end:
  ```r
  interaction_ended_debounced <- reactive({
    input$user_interacting
  }) |> debounce(1000)  # #NOTE: Resume delay after interaction — tuneable
  ```

  2. `observeEvent(interaction_ended_debounced(), ...)`:
     - When value is `FALSE` and `ambient_drift_active()` is TRUE and `physics_toggle_debounced()` is TRUE (user hasn't manually disabled):
       - Resume drift physics with the same gentle parameters from Step 3
       - Comment: `# Resume ambient drift after user interaction ends`
     - Use `ignoreInit = TRUE`

  **Step 6: Reset physics state on tab navigation / data changes**

  Find the location where `current_network_data()` is set (around lines 334 and 519 where data is loaded/built). After the data is set, the `renderVisNetwork` output already handles fresh physics. But we also need to ensure:
  - When `current_network_data()` changes, reset `ambient_drift_active(FALSE)` — the stabilization event will re-set it
  - The physics toggle switch should reset to TRUE on data change (use `updateSwitchInput` or `bslib::update_switch`)

  Add an observer that watches `current_network_data()`:
  ```r
  observeEvent(current_network_data(), {
    ambient_drift_active(FALSE)
    # Reset physics toggle to ON when new data arrives
    bslib::update_switch("physics_enabled", value = TRUE, session = session)
  }, ignoreInit = TRUE, ignoreNULL = TRUE)
  ```

  **Important implementation notes:**
  - Use `session$ns("network_graph")` consistently for proxy calls (existing pattern)
  - Use `{priority: 'event'}` in all Shiny.setInputValue calls to ensure events always fire even with same value
  - All `#NOTE` comments mark tuneable constants per user decision
  - No toasts or additional visual feedback per user decision — button state only
  - Verify `compute_layout_positions()` exists and works (used at lines 218, 496, 1118 already)
  </action>
  <verify>
  Run the app and manually verify:
  1. Build a network with a single seed paper. After stabilization, nodes should exhibit gentle orbital drift.
  2. Toggle physics off — nodes freeze instantly. Toggle on — nodes resume without collapsing to center.
  3. Navigate away from network tab, return, rebuild — physics resets to ON, no collapse.
  4. Rapidly click the physics toggle 5+ times — no crash, no collapse, settles to final state.
  5. Drag a node during drift — drift pauses. Release and wait 1s — drift resumes.
  6. Build a large network (3+ seeds, >20 nodes) — freezes after stabilization, no ambient drift.

  Automated sanity check (syntax only):
  ```bash
  "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "parse('R/mod_citation_network.R'); cat('Syntax OK\n')"
  ```
  </verify>
  <done>
  - PHYS-01: Physics toggle on/off never causes singularity collapse — positions validated before re-enable, toggle debounced at 300ms
  - PHYS-02: Networks with <=20 nodes exhibit gentle ambient orbital drift after stabilization (low damping, low central gravity). Networks >20 nodes freeze after stabilization.
  - Drift pauses during user interaction (drag), resumes 1s after interaction ends
  - Tab navigation resets physics to default ON state
  - All tuneable constants marked with #NOTE comments
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of physics behavior</name>
  <what-built>Debounced physics toggle with position validation (PHYS-01 collapse fix) and size-conditional ambient orbital drift after stabilization (PHYS-02). Drift pauses during interaction and resumes after.</what-built>
  <how-to-verify>
  1. Start the app: run `shiny::runApp()` in R console
  2. **PHYS-01 collapse test:**
     a. Build a citation network (any seed)
     b. Wait for stabilization
     c. Navigate to a different tab (e.g., Abstract Search)
     d. Return to the Citation Network tab
     e. Toggle physics OFF then ON
     f. VERIFY: Nodes remain spread out — no collapse to center point
  3. **PHYS-01 rapid toggle test:**
     a. Rapidly click the physics toggle 5+ times
     b. VERIFY: No glitches, no collapse, network settles to final toggle state
  4. **PHYS-02 small network drift test:**
     a. Build a network with a single seed paper (should have <=20 nodes)
     b. Wait for stabilization to complete
     c. VERIFY: Nodes exhibit very slow, gentle orbital motion — barely noticeable, like breathing
     d. Drag a node — drift should pause
     e. Release — drift should resume after ~1 second
  5. **PHYS-02 large network freeze test:**
     a. Build a network with 3+ seed papers (should have >20 nodes)
     b. Wait for stabilization
     c. VERIFY: Nodes are frozen — no drift
  6. **Data change test:**
     a. While physics is OFF, add a new seed paper
     b. VERIFY: Physics auto-enables and network re-stabilizes with new nodes
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 tests pass, or describe which tests failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
- R syntax check passes: `parse('R/mod_citation_network.R')` returns no errors
- No new packages introduced — all changes use existing visNetwork, htmlwidgets, shiny, bslib
- Physics toggle debounced at 300ms — rapid clicking produces no intermediate states
- Position validation before physics re-enable — singularity collapse prevented proactively
- Size-conditional stabilization handler — <=20 nodes drift, >20 freeze
- Interaction-aware drift — pauses on drag, resumes after 1s delay
- All tuneable constants marked with #NOTE comments per user decision
</verification>

<success_criteria>
1. PHYS-01: Physics toggle never causes singularity collapse — confirmed by human verification
2. PHYS-02: Small networks (<=20 nodes) show gentle ambient drift — confirmed by human verification
3. No regressions: large networks still freeze, saved networks still load correctly, node click selection still works
</success_criteria>

<output>
After completion, create `.planning/phases/41-physics-stabilization/41-01-SUMMARY.md`
</output>
