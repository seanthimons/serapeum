---
phase: 31-component-styling-visual-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/theme_catppuccin.R
  - R/mod_cost_tracker.R
  - R/mod_search_notebook.R
  - R/mod_about.R
  - R/mod_citation_network.R
  - R/mod_query_builder.R
  - R/mod_seed_discovery.R
  - R/mod_settings.R
  - R/mod_topic_explorer.R
  - R/mod_document_notebook.R
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - COMP-04
  - COMP-05

must_haves:
  truths:
    - "No hardcoded #6366f1 (old indigo) remains anywhere in the codebase except plan/docs files"
    - "All bg-light Bootstrap class usages in R modules are replaced with bg-body-secondary or bg-body-tertiary"
    - "All text-dark class usages in badges are replaced with text-body for theme-awareness"
    - "All custom component dark mode overrides are centralized in catppuccin_dark_css()"
    - "btn-outline-dark on about page replaced with theme-aware alternative"
    - "Dissertation badge #6f42c1 replaced with theme-aware approach"
  artifacts:
    - path: "R/theme_catppuccin.R"
      provides: "Extended catppuccin_dark_css() with additional component overrides"
      contains: "bg-body-secondary"
    - path: "R/mod_cost_tracker.R"
      provides: "Cost chart using LATTE$lavender instead of #6366f1"
      contains: "LATTE"
    - path: "R/mod_search_notebook.R"
      provides: "Year histogram using LATTE$lavender, badges using theme-aware classes"
      contains: "LATTE"
  key_links:
    - from: "R/theme_catppuccin.R"
      to: "All R modules"
      via: "LATTE$/MOCHA$ constants from auto-sourced file"
      pattern: "LATTE\\$"
---

<objective>
Migrate all hardcoded colors and non-theme-aware Bootstrap classes across all R modules to use Catppuccin palette constants and Bootstrap 5.3 theme-aware classes.

Purpose: Phase 30 established the Catppuccin foundation, but many modules still use the old #6366f1 indigo primary, hardcoded bg-light classes (which don't adapt to dark mode), and text-dark classes (which break contrast in dark mode). This plan systematically updates every module to be fully theme-aware.

Output: All R modules use LATTE$ constants for R-generated colors, bg-body-secondary/bg-body-tertiary instead of bg-light, text-body instead of text-dark, and extended catppuccin_dark_css() for any remaining overrides.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-component-styling-visual-consistency/31-CONTEXT.md
@.planning/phases/31-component-styling-visual-consistency/31-RESEARCH.md
@.planning/phases/30-core-dark-mode-palette/30-01-SUMMARY.md
@R/theme_catppuccin.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors with Catppuccin constants in R modules</name>
  <files>R/mod_cost_tracker.R, R/mod_search_notebook.R</files>
  <action>
**R/mod_cost_tracker.R:**

Find the barplot call (around line 186):
```r
col = "#6366f1",
```

Replace with:
```r
col = LATTE$lavender,
```

Note: LATTE is available because R/theme_catppuccin.R is auto-sourced (alphabetically before mod_cost_tracker.R). This is a base R plot, so we use the R constant directly (CSS variables don't apply to R graphics).

**R/mod_search_notebook.R:**

1. Find the ggplot year histogram (around line 846):
```r
ggplot2::geom_col(fill = "#6366f1", width = 0.8, alpha = 0.7) +
```

Replace with:
```r
ggplot2::geom_col(fill = LATTE$lavender, width = 0.8, alpha = 0.7) +
```

2. Find the dissertation badge (around line 666):
```r
"dissertation" = list(class = "bg-purple text-white", label = "dissertation", style = "background-color: #6f42c1;"),
```

Replace with a theme-aware approach — use Bootstrap's bg-info class which maps to Catppuccin blue (close enough to purple for a badge):
```r
"dissertation" = list(class = "bg-info-subtle text-info-emphasis", label = "dissertation"),
```

This removes the inline style entirely and uses Bootstrap 5.3 subtle variant classes that auto-adapt to dark mode.

3. Search for any other hardcoded hex colors in the file and replace if found.
  </action>
  <verify>
1. Grep for "#6366f1" in all R/ files — should only appear in theme_catppuccin.R (if at all) and not in any module
2. Grep for "#6f42c1" — should not appear anywhere
3. Verify LATTE$lavender returns "#7287fd" (quick R source check)
  </verify>
  <done>
No hardcoded #6366f1 or #6f42c1 remains in any R module. Cost tracker chart uses LATTE$lavender. Year histogram uses LATTE$lavender. Dissertation badge uses Bootstrap 5.3 subtle variant classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace bg-light/text-dark with theme-aware Bootstrap classes across all modules</name>
  <files>R/mod_about.R, R/mod_citation_network.R, R/mod_query_builder.R, R/mod_seed_discovery.R, R/mod_settings.R, R/mod_topic_explorer.R, R/mod_document_notebook.R, R/mod_search_notebook.R, R/theme_catppuccin.R</files>
  <action>
**Systematic replacement across all modules:**

The Bootstrap `bg-light` class does NOT adapt to dark mode — it stays light-colored. Replace with theme-aware alternatives:

1. **`bg-light` used for section/panel backgrounds** → Replace with `bg-body-secondary`
   - This maps to --bs-secondary-bg which is Mocha surface0 in dark mode

2. **`bg-light` used for subtle badge backgrounds** → Replace with `bg-body-tertiary`
   - Or use `bg-secondary-subtle` for badges specifically

3. **`text-dark` used in badges** → Replace with `text-body`
   - text-body adapts to the current theme's foreground color

4. **`var(--bs-light)` in inline styles** → Replace with `var(--bs-tertiary-bg)`

**Files to update (search and replace carefully):**

**R/mod_citation_network.R** (~line 28):
- `bg-light rounded` in controls → `bg-body-secondary rounded`

**R/mod_query_builder.R** (~line 160):
- `bg-light` in result container → `bg-body-secondary`

**R/mod_seed_discovery.R** (~line 188):
- `bg-light` in result container → `bg-body-secondary`

**R/mod_settings.R** (~line 445):
- `bg-light` in card body → `bg-body-secondary`

**R/mod_topic_explorer.R** (~line 294):
- `bg-light` in result container → `bg-body-secondary`

**R/mod_search_notebook.R** (multiple locations):
- Line ~280: `var(--bs-light)` inline style → `var(--bs-tertiary-bg)`
- Lines ~659, 668-671: badge `bg-light text-dark` → `bg-body-tertiary text-body`
- Line ~1168: conditional `bg-light` for viewed items → `bg-body-tertiary`
- Line ~1370: venue badge `bg-light text-dark` → `bg-body-tertiary text-body`
- Lines ~1714, 1777, 1795: code/preview `bg-light` → `bg-body-secondary`

**R/mod_document_notebook.R** (~line 127):
- `var(--bs-light)` inline style → `var(--bs-tertiary-bg)`

**R/mod_about.R** (~line 186):
- `btn-outline-dark` → `btn-outline-secondary` (adapts to both themes)
- Note: `hover-bg-light` class in about page is fine — already handled by custom.css dark mode rule

**R/theme_catppuccin.R:**
Add a CSS rule in catppuccin_dark_css() for any remaining bg-light elements that can't be class-replaced (edge cases):
```css
[data-bs-theme="dark"] .bg-light {
  background-color: var(--bs-secondary-bg) !important;
  color: var(--bs-body-color) !important;
}
```
This is a safety net — the class replacements above handle the known cases, but this catches any we miss.

Also add a `text-dark` override for dark mode:
```css
[data-bs-theme="dark"] .text-dark {
  color: var(--bs-body-color) !important;
}
```
  </action>
  <verify>
1. Grep for `"bg-light"` across R/ modules — should only appear in edge cases covered by the CSS safety net (or in hover-bg-light which is intentional)
2. Grep for `"text-dark"` across R/ modules — should be minimal (covered by CSS override)
3. Grep for `var(--bs-light)` — should return 0 results
4. Start the app and verify: toggle dark mode, check that panel backgrounds adapt, badge text is readable, about page GitHub button is visible
  </verify>
  <done>
All bg-light classes replaced with bg-body-secondary or bg-body-tertiary. All text-dark in badges replaced with text-body. var(--bs-light) inline styles replaced with var(--bs-tertiary-bg). btn-outline-dark replaced with btn-outline-secondary. CSS safety net added in catppuccin_dark_css() for any remaining bg-light/text-dark elements.
  </done>
</task>

</tasks>

<verification>
1. Toggle dark mode — all panel/card backgrounds use dark Mocha surface colors, not light gray
2. All badges (work type, OA status, venue) are readable in both light and dark mode
3. Year histogram and cost chart use Catppuccin lavender, not old indigo
4. About page GitHub button is visible in both themes
5. No #6366f1 or #6f42c1 hex values remain in R/ module files
6. Settings page cards adapt to dark mode
7. Query builder, seed discovery, topic explorer result panels adapt to dark mode
</verification>

<success_criteria>
- No hardcoded UI hex colors remain in R modules (viridis data palette excluded)
- All bg-light replaced with theme-aware alternatives
- All text-dark replaced with text-body
- CSS safety net in catppuccin_dark_css() catches edge cases
- App starts without errors in both modes
</success_criteria>

<output>
After completion, create `.planning/phases/31-component-styling-visual-consistency/31-01-SUMMARY.md`
</output>
</content>
