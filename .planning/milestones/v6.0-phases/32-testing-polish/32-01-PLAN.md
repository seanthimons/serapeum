---
phase: 32-testing-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "App starts without R errors or warnings related to dark mode"
    - "No remaining hardcoded hex colors in UI code (data viz excluded)"
    - "catppuccin_dark_css() generates valid CSS"
    - "All modules load successfully"
    - "Theme toggle works (data-bs-theme attribute switches)"
  artifacts: []
---

<objective>
Comprehensive validation of dark mode implementation across all modules.

Purpose: Final verification that Phases 30-31 work correctly. Run automated checks, audit for remaining issues, and fix any bugs found.

Output: Clean validation report confirming v6.0 dark mode is complete.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-testing-polish/32-CONTEXT.md
@R/theme_catppuccin.R
@app.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated validation and remaining issue audit</name>
  <files>R/theme_catppuccin.R, app.R, R/mod_*.R, www/custom.css</files>
  <action>
**Step 1: App startup validation**
Run the app briefly to verify it starts without errors:
```r
# Source all R files and verify no errors
for (f in list.files("R", pattern = "\\.R$", full.names = TRUE)) source(f)
```

**Step 2: Remaining hardcoded color audit**
Grep for any remaining hardcoded hex colors in R module UI code that should be theme-aware:
- `#[0-9a-fA-F]{6}` in R/mod_*.R (excluding data viz colors like viridis)
- Any remaining `bg-light` or `text-dark` not caught by safety net

**Step 3: CSS validation**
Verify catppuccin_dark_css() output is valid:
- No duplicate variable declarations
- All color values are valid hex or rgba
- No syntax errors (unclosed brackets, missing semicolons)

**Step 4: Cross-module consistency check**
Verify all modules follow established patterns:
- Panels use bg-body-secondary
- Badges use bg-body-tertiary text-body
- R plot colors use LATTE$ constants
- No inline style colors that should be CSS variables

**Step 5: Fix any issues found**
If any bugs are discovered during the audit, fix them and document.
  </action>
  <verify>
1. App sources all R files without errors
2. No remaining non-theme-aware hardcoded colors in UI code
3. catppuccin_dark_css() generates valid CSS (no duplicates, valid syntax)
4. All modules follow established dark mode patterns
  </verify>
  <done>
All validation checks pass. Dark mode implementation is complete and consistent across all modules.
  </done>
</task>

</tasks>

<verification>
1. App starts without dark-mode-related errors
2. No remaining hardcoded hex colors in UI elements
3. CSS is valid and well-organized
4. All modules use theme-aware patterns
5. Phase 32 validation report created
</verification>

<success_criteria>
- All automated checks pass
- No remaining issues found (or all found issues fixed)
- v6.0 dark mode implementation validated as complete
</success_criteria>

<output>
After completion, create `.planning/phases/32-testing-polish/32-01-SUMMARY.md`
</output>
