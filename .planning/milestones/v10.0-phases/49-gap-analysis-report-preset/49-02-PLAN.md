---
phase: 49-gap-analysis-report-preset
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - R/mod_document_notebook.R
autonomous: false
requirements:
  - GAPS-01
  - GAPS-05

must_haves:
  truths:
    - "User can click Research Gaps button in document notebook Deep presets row"
    - "Button is positioned after Methods and before Slides in Deep row"
    - "Clicking with < 3 papers shows error toast"
    - "Clicking with 15+ papers shows warning toast"
    - "AI disclaimer banner appears on gap analysis output"
  artifacts:
    - path: "R/mod_document_notebook.R"
      provides: "Research Gaps button + handler + is_synthesis update"
      contains: "btn_gaps"
  key_links:
    - from: "R/mod_document_notebook.R"
      to: "R/rag.R"
      via: "generate_gap_analysis() call"
      pattern: "generate_gap_analysis"
    - from: "R/mod_document_notebook.R"
      to: "is_synthesis check"
      via: "gap_analysis in preset_type vector"
      pattern: "gap_analysis"
---

<objective>
Wire the Gap Analysis Report preset into the document notebook UI: add Research Gaps button, create handler with RAG guard and thresholds, and update is_synthesis for AI disclaimer.

Purpose: Completes the user-facing integration so researchers can generate gap analysis reports from their notebooks.
Output: Functional Research Gaps button in document notebook preset bar.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-gap-analysis-report-preset/49-CONTEXT.md
@.planning/phases/49-gap-analysis-report-preset/49-01-SUMMARY.md

<interfaces>
<!-- Current Deep presets row UI in R/mod_document_notebook.R lines 101-118 -->
```r
# Row 2: Deep presets + Export
div(
  class = "d-flex gap-1",
  div(
    class = "btn-group btn-group-sm",
    actionButton(ns("btn_conclusions"), "Conclusions", class = "btn-sm btn-outline-primary", icon = icon_microscope()),
    actionButton(ns("btn_lit_review"), "Lit Review", class = "btn-sm btn-outline-primary", icon = icon_table()),
    actionButton(ns("btn_methods"), "Methods", class = "btn-sm btn-outline-primary", icon = icon_flask()),
    # INSERT btn_gaps HERE (after Methods, before Slides)
    actionButton(ns("btn_slides"), "Slides", class = "btn-sm btn-outline-primary", icon = icon_file_powerpoint())
  ),
  # ... export dropdown ...
)
```

<!-- is_synthesis check at line 717 -->
```r
is_synthesis <- !is.null(msg$preset_type) && msg$preset_type %in% c(
  "overview", "conclusions", "research_questions", "lit_review", "methodology_extractor"
)
# ADD "gap_analysis" to this vector
```

<!-- Methods handler pattern at lines 984-1031 — clone for btn_gaps -->
<!-- Icon wrapper: icon_search() at line 143 in R/theme_catppuccin.R (magnifying glass) -->

<!-- Backend function from Plan 01: -->
```r
generate_gap_analysis(con, config, notebook_id, session_id = NULL)
# Returns narrative markdown string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Research Gaps button, handler, and is_synthesis update</name>
  <files>R/mod_document_notebook.R</files>
  <action>
Three changes to R/mod_document_notebook.R:

1. **Add button in Deep presets row (line ~114, between Methods and Slides):**
```r
actionButton(ns("btn_gaps"), "Research Gaps",
             class = "btn-sm btn-outline-primary",
             icon = icon_search()),
```
Order must be: Conclusions, Lit Review, Methods, Research Gaps, Slides (per user decision).
Use icon_search() (magnifying glass) for "discovering gaps" semantic.

2. **Add handler (after btn_methods handler, before btn_slides handler — around line 1032):**
Clone the btn_methods observeEvent handler with these changes:
- Input: `input$btn_gaps`
- RAG guard: same pattern (showNotification if !rag_available())
- **Minimum 3 papers (GAPS-06):** Check `doc_count < 3L`, show error toast: "Gap analysis requires at least 3 papers. Add more papers to this notebook." and return()
- **Warning threshold at 15 (not 20):** `doc_count >= 15L` triggers warning toast: sprintf("Analyzing %d papers - output quality may degrade with large collections.", doc_count)
- User message content: "Generate: Research Gaps"
- preset_type: "gap_analysis" (both user and assistant messages)
- Call: `generate_gap_analysis(con(), cfg, nb_id, session_id = session$token)`

3. **Update is_synthesis check (line 717):**
Add "gap_analysis" to the preset_type vector:
```r
is_synthesis <- !is.null(msg$preset_type) && msg$preset_type %in% c(
  "overview", "conclusions", "research_questions", "lit_review",
  "methodology_extractor", "gap_analysis"
)
```

Do NOT modify any existing handlers or button definitions. Only add new code and update the is_synthesis line.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 8 && curl -s http://localhost:3838 > /dev/null && echo "App starts successfully" || echo "App failed to start"; kill %1 2>/dev/null</automated>
  </verify>
  <done>Research Gaps button appears in Deep presets row between Methods and Slides. Handler enforces 3-paper minimum with error toast, 15-paper warning toast, RAG guard, and calls generate_gap_analysis(). is_synthesis check includes "gap_analysis" for AI disclaimer banner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Gap Analysis Report end-to-end</name>
  <what-built>Complete Gap Analysis Report preset: Research Gaps button in Deep presets row, handler with RAG guard and paper thresholds, AI disclaimer on output, narrative report with 5 gap categories and contradiction detection.</what-built>
  <how-to-verify>
    1. Open a document notebook with 3+ indexed papers
    2. Verify preset bar shows: Conclusions, Lit Review, Methods, Research Gaps, Slides
    3. Click "Research Gaps" — should generate narrative report with 5 gap category headings
    4. Verify AI disclaimer banner appears above generated output
    5. Test with < 3 papers notebook — should show error toast about minimum requirement
    6. Verify contradictions (if any) appear with bold prefix within gap categories
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Research Gaps button visible in Deep presets row
- Button positioned after Methods, before Slides
- Clicking generates gap analysis report with 5 category headings
- AI disclaimer banner shown on output
- < 3 papers shows error toast
- 15+ papers shows warning toast
- App starts without errors (Shiny smoke test)
</verification>

<success_criteria>
User can generate a Gap Analysis Report from any document notebook with 3+ papers. Report shows narrative prose organized by 5 gap categories with inline citations and contradiction detection. AI disclaimer banner is displayed.
</success_criteria>

<output>
After completion, create `.planning/phases/49-gap-analysis-report-preset/49-02-SUMMARY.md`
</output>
