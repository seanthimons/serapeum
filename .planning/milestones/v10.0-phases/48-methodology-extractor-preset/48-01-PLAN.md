---
phase: 48-methodology-extractor-preset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/rag.R
  - R/theme_catppuccin.R
autonomous: true
requirements:
  - METH-02
  - METH-03
  - METH-04

must_haves:
  truths:
    - "generate_methodology_extractor() returns a GFM table with 6 columns"
    - "Section-targeted RAG prioritizes methods/methodology chunks with 3-level fallback"
    - "Output includes Author (Year) citations with DOI links injected"
  artifacts:
    - path: "R/rag.R"
      provides: "generate_methodology_extractor() function"
      contains: "generate_methodology_extractor"
    - path: "R/theme_catppuccin.R"
      provides: "icon_flask wrapper for Methods button"
      contains: "icon_flask"
  key_links:
    - from: "R/rag.R"
      to: "search_chunks_hybrid"
      via: "section_filter = c('methods', 'methodology')"
      pattern: "section_filter.*methods"
    - from: "R/rag.R"
      to: "build_context_by_paper"
      via: "function call for context assembly"
      pattern: "build_context_by_paper"
    - from: "R/rag.R"
      to: "validate_gfm_table"
      via: "table validation before return"
      pattern: "validate_gfm_table"
---

<objective>
Create the generate_methodology_extractor() backend function and icon wrapper.

Purpose: Implements the core extraction logic that retrieves methodology-related chunks via section-targeted RAG and produces a structured GFM comparison table. This function is consumed by the UI handler in Plan 02.
Output: Working generate_methodology_extractor() in R/rag.R, icon_flask() wrapper in R/theme_catppuccin.R.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-methodology-extractor-preset/48-CONTEXT.md
@.planning/phases/48-methodology-extractor-preset/48-RESEARCH.md

<interfaces>
<!-- Key functions and patterns the executor needs from existing codebase -->

From R/rag.R (lines 835-876):
```r
build_context_by_paper <- function(papers_with_chunks) { ... }
validate_gfm_table <- function(text) { ... }
```

From R/rag.R (lines 878-1069) — generate_lit_review_table():
```r
generate_lit_review_table <- function(con, config, notebook_id, session_id = NULL) {
  # API setup, docs query, paper_labels, section-aware chunk retrieval with
  # dynamic token budget, system prompt, chat_completion, code fence stripping,
  # validate_gfm_table, DOI injection, missing paper count note
}
```

Key reusable internals from lit_review_table:
- Paper labels (lines 901-936): JSON author parsing, "et al." formatting
- Dynamic token budget (lines 938-997): repeat loop reducing chunks_per_paper from 7 to 2
- DOI injection (lines 1046-1052): gsub with fixed=TRUE on paper labels
- Missing paper note (lines 1054-1063): count data rows vs expected

From R/db.R:
```r
search_chunks_hybrid(con, query, notebook_id, limit, section_filter, api_key, embed_model)
```

From R/theme_catppuccin.R — wrapper pattern:
```r
icon_wrench <- function(...) shiny::icon("wrench", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate_methodology_extractor() function in R/rag.R</name>
  <files>R/rag.R</files>
  <action>
Add generate_methodology_extractor(con, config, notebook_id, session_id = NULL) function at the end of R/rag.R (after generate_lit_review_table, around line 1069).

Clone the generate_lit_review_table() pattern with these modifications:

1. **API setup and docs query**: Identical to lit_review (lines 879-896). Also retrieve embed_model for hybrid search:
   `embed_model <- get_setting(config, "defaults", "embedding_model") %||% "openai/text-embedding-3-small"`

2. **Paper labels**: Reuse identical author parsing logic (lines 901-936).

3. **Chunk retrieval — use HYBRID SEARCH with 3-level fallback** (per user decision), NOT the direct DB query pattern from lit_review. This is the key difference:

   For each document, implement 3 levels:
   - Level 1: `search_chunks_hybrid(con, query = "study design methodology data sources sample size statistical methods instruments", notebook_id = notebook_id, limit = 15, section_filter = c("methods", "methodology"), api_key = api_key, embed_model = embed_model)` — filter results to current doc's chunks only by checking source_id
   - Level 2 (if Level 1 returns 0 rows for this doc): `search_chunks_hybrid()` without section_filter, same query, filter to current doc
   - Level 3 (if Level 2 also fails): Direct DB fallback: `SELECT content, page_number, COALESCE(section_hint, 'general') as section_hint FROM chunks WHERE source_id = ? ORDER BY chunk_index LIMIT 15`

   Use dynamic token budget like lit_review: start at `chunks_per_paper <- 7L`, repeat loop reducing to 2 if total exceeds 80k tokens. BUT for the hybrid search levels, use `limit = 15` for the search call and then take the top `chunks_per_paper` results per doc after filtering by source_id.

   IMPORTANT: The hybrid search returns results across ALL documents in the notebook. Filter to current doc: `chunks[chunks$source_id == doc$id, ]` (if source_id column exists in results) or use the Level 3 direct DB fallback per-doc approach like lit_review does. Given the complexity, the SIMPLER approach is to follow lit_review's exact pattern: use direct DB queries per document with section_hint filtering (Level 1: `section_hint IN ('methods', 'methodology')`, fallback to distributed sampling). This IS section-targeted RAG — it uses the section_hint column populated by detect_section_hint() during PDF processing. Reserve hybrid search for a single global query only if per-doc direct queries yield too few chunks.

   Recommended approach (matches lit_review pattern exactly, satisfies METH-03):
   ```r
   # Per-doc section-targeted retrieval
   section_chunks <- dbGetQuery(con, "
     SELECT chunk_index, content, page_number, section_hint
     FROM chunks
     WHERE source_id = ? AND section_hint IN ('methods', 'methodology')
     ORDER BY chunk_index
     LIMIT ?
   ", list(doc$id, chunks_per_paper))

   # Fallback: distributed sampling (same as lit_review)
   if (nrow(section_chunks) < 2) {
     all_chunks <- dbGetQuery(con, "SELECT ... FROM chunks WHERE source_id = ? ...", list(doc$id))
     # distributed sampling logic
   }
   ```

4. **System prompt** — methodology extraction specific:
   ```
   "You are a methodology extraction assistant. Generate a table in GFM (GitHub Flavored Markdown) pipe table format.\n\n"
   "COLUMNS (exactly these, in this order):\n"
   "| Paper | Study Design | Data Sources | Sample Characteristics | Statistical Methods | Tools/Instruments |\n\n"
   "RULES:\n"
   "- One row per paper, ordered by most recent first\n"
   "- Paper: Use the exact label from the paper delimiter (e.g., 'Smith et al. (2023)')\n"
   "- Each cell: brief phrases (2-5 words), NOT full sentences\n"
   "- Study Design: experimental, quasi-experimental, observational, case study, systematic review, meta-analysis, qualitative, mixed methods, etc.\n"
   "- Data Sources: specify databases, surveys, instruments, or datasets used\n"
   "- Sample Characteristics: population type, size (n=X), demographics\n"
   "- Statistical Methods: specific tests or analytical approaches (e.g., regression, ANOVA, thematic analysis)\n"
   "- Tools/Instruments: software, scales, measurement tools\n"
   "- For papers with no clear methodology: use 'Not described' or contextual notes (e.g., 'Theoretical framework')\n"
   "- Output ONLY the markdown table. No introduction, no summary, no notes.\n"
   "- Every line must have exactly 7 pipe characters"
   ```

5. **User prompt, chat_completion, cost logging**: Same pattern as lit_review. Cost logged as "methodology_extractor".

6. **Post-processing**: Strip code fences, validate_gfm_table(), DOI injection, missing paper note. Identical to lit_review but error message says "Methods button" instead of "Lit Review button".

7. **Pipe count**: The table has 6 columns (Paper + 5 fields) = 7 pipe characters per row. Ensure system prompt says "exactly 7 pipe characters".
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/rag.R'); stopifnot(is.function(generate_methodology_extractor)); cat('OK\n')"</automated>
  </verify>
  <done>generate_methodology_extractor() exists in R/rag.R, accepts (con, config, notebook_id, session_id) parameters, uses section-targeted retrieval for methods/methodology chunks, produces GFM table with 6 columns, includes DOI injection and validation.</done>
</task>

<task type="auto">
  <name>Task 2: Add icon_flask wrapper to theme_catppuccin.R</name>
  <files>R/theme_catppuccin.R</files>
  <action>
Add a new icon wrapper function after the last existing icon wrapper (around line 607):

```r
icon_flask <- function(...) shiny::icon("flask", ...)
```

This uses FontAwesome's "flask" icon (lab flask) to represent methodology extraction. It is distinct from icon_microscope (used by Conclusions button).

Verify the icon name "flask" is a valid FontAwesome 6 Free icon (it is — fa-flask is in the solid set).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/theme_catppuccin.R'); stopifnot(is.function(icon_flask)); cat('OK\n')"</automated>
  </verify>
  <done>icon_flask() wrapper exists in theme_catppuccin.R, returns a flask icon via shiny::icon("flask", ...)</done>
</task>

</tasks>

<verification>
- generate_methodology_extractor() function loads without error
- icon_flask() function loads without error
- Function signature matches (con, config, notebook_id, session_id = NULL)
- System prompt references exactly 7 pipe characters for 6 columns
- Section filter uses c("methods", "methodology")
- DOI injection uses fixed = TRUE
- Cost logging uses "methodology_extractor" label
</verification>

<success_criteria>
- R/rag.R contains generate_methodology_extractor() that follows lit_review_table pattern
- R/theme_catppuccin.R contains icon_flask() wrapper
- Both files source without error
- Function uses section-targeted retrieval prioritizing methods/methodology chunks (METH-03)
- Output format is GFM table with Paper, Study Design, Data Sources, Sample Characteristics, Statistical Methods, Tools/Instruments columns (METH-02)
- DOI injection produces clickable Author (Year) links (METH-04)
</success_criteria>

<output>
After completion, create `.planning/phases/48-methodology-extractor-preset/48-01-SUMMARY.md`
</output>
