---
phase: 47-sidebar-button-theming
plan: 03
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - R/mod_document_notebook.R
  - R/mod_search_notebook.R
  - R/mod_seed_discovery.R
  - R/mod_query_builder.R
  - R/mod_topic_explorer.R
  - R/mod_citation_audit.R
  - R/mod_citation_network.R
  - R/mod_bulk_import.R
  - R/mod_slides.R
autonomous: false
requirements: [DSGN-03, THEM-04, THEM-05]

must_haves:
  truths:
    - "Search/execute buttons across all discovery modules use btn-primary (lavender) not btn-success (green)"
    - "Add-to-notebook buttons keep btn-outline-success (green)"
    - "Stop/Cancel buttons keep btn-warning (yellow)"
    - "Delete buttons keep btn-danger or btn-outline-danger"
    - "Document notebook title bar uses flexbox wrap for preset buttons"
    - "Search notebook title bar matches document notebook styling"
    - "Delete button is positioned closer to notebook title in app.R"
  artifacts:
    - path: "R/mod_seed_discovery.R"
      provides: "Search button changed to btn-primary"
      contains: "btn-primary"
    - path: "R/mod_query_builder.R"
      provides: "Search button changed to btn-primary"
      contains: "btn-primary"
    - path: "R/mod_topic_explorer.R"
      provides: "Search button changed to btn-primary"
      contains: "btn-primary"
    - path: "R/mod_document_notebook.R"
      provides: "Responsive title bar with flex-wrap, consistent button styling"
      contains: "flex-wrap"
    - path: "R/mod_search_notebook.R"
      provides: "Responsive title bar matching document notebook, search button = btn-primary"
      contains: "flex-wrap"
  key_links:
    - from: "R/mod_document_notebook.R"
      to: "Bootstrap flexbox utilities"
      via: "d-flex flex-wrap gap-2 classes"
      pattern: "flex-wrap"
    - from: "R/mod_seed_discovery.R"
      to: "semantic color policy"
      via: "btn-primary instead of btn-success for search"
      pattern: "btn-primary"
---

<objective>
Apply the semantic color scheme to all buttons across every module: change search buttons from green to lavender, ensure consistent notebook title bar styling with responsive wrapping, and move the delete button closer to notebook title.

Purpose: Enforce the Phase 45 semantic color policy across the entire app (DSGN-03), make notebook buttons uniform (THEM-04), and improve title bar space usage (THEM-05).
Output: All module buttons follow semantic color scheme, title bars use responsive flexbox layout
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-sidebar-button-theming/47-CONTEXT.md
@.planning/phases/47-sidebar-button-theming/47-RESEARCH.md
@.planning/phases/47-sidebar-button-theming/47-01-SUMMARY.md

@R/mod_document_notebook.R
@R/mod_search_notebook.R
@R/mod_seed_discovery.R
@R/mod_query_builder.R
@R/mod_topic_explorer.R
@app.R
</context>

<interfaces>
<!-- Semantic color policy (from Phase 45 / theme_catppuccin.R): -->
<!-- PRIMARY (lavender) — Main actions: Search, Save, Send -->
<!-- DANGER (red) — Destructive: Delete, Remove, Clear -->
<!-- SUCCESS (green) — Confirmations: Paper Added, Add to Notebook -->
<!-- WARNING (yellow) — Cautions: Stop, Cancel -->
<!-- INFO (sapphire) — Informational: Help, Tooltips -->
<!-- SECONDARY (surface) — Less Important: Cancel, Close, Export -->

<!-- Key button locations to change: -->
<!-- R/mod_seed_discovery.R:242 — Search btn-success -> btn-primary -->
<!-- R/mod_query_builder.R:176 — Search btn-success -> btn-primary -->
<!-- R/mod_topic_explorer.R:56 — Search btn-success -> btn-primary -->
<!-- R/mod_search_notebook.R — "Search" button if btn-success -> btn-primary -->
<!-- R/mod_document_notebook.R:69-116 — title bar layout -->
<!-- R/mod_search_notebook.R:70-99 — title bar layout -->

<!-- User decisions (locked): -->
<!-- Search buttons: btn-success -> btn-primary -->
<!-- Add to Notebook: keep btn-outline-success -->
<!-- Stop/Cancel: keep btn-warning -->
<!-- Delete: keep btn-danger / btn-outline-danger -->
<!-- Preset buttons: keep icon+text styling, btn-outline-primary -->
<!-- Embed Papers: btn-outline-primary (lavender outline) -->
<!-- Send: btn-primary (solid lavender) -->
<!-- Export dropdown: btn-outline-secondary (Claude's discretion -- keep as is) -->
<!-- Delete button: move closer to notebook title -->
<!-- Title bar: flex-wrap for responsive two-row layout -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Recolor search buttons and apply semantic policy across all modules</name>
  <files>R/mod_seed_discovery.R, R/mod_query_builder.R, R/mod_topic_explorer.R, R/mod_search_notebook.R, R/mod_citation_audit.R, R/mod_citation_network.R, R/mod_bulk_import.R, R/mod_slides.R</files>
  <action>
  **Search/Execute buttons — change from btn-success to btn-primary:**
  - R/mod_seed_discovery.R: Find the search/discover button (~line 242), change `btn-success` to `btn-primary`
  - R/mod_query_builder.R: Find the search button (~line 176), change `btn-success` to `btn-primary`
  - R/mod_topic_explorer.R: Find the search button (~line 56), change `btn-success` to `btn-primary`
  - R/mod_search_notebook.R: Find the "Search" button, change `btn-success` to `btn-primary` if present

  **Verify these are KEPT as-is (do NOT change):**
  - "Add to Notebook" buttons in search results: must stay `btn-outline-success` (green = positive/constructive)
  - Stop/Cancel buttons (bulk import, citation network): must stay `btn-warning` (yellow = caution)
  - Delete buttons anywhere: must stay `btn-danger` or `btn-outline-danger` (red = destructive)

  **Audit all modules for semantic correctness:**
  Go through each module file and check every `class = "btn-*"` usage. Fix any that violate the semantic policy:
  - Any remaining btn-success on a search/execute action -> btn-primary
  - Any btn-danger on a non-destructive action -> correct semantic color
  - Any hardcoded colors (style="background:...") -> semantic Bootstrap class

  Do NOT change button text or icon wrappers -- those were handled in Plan 01.

  **Citation audit module (THEM-02 scope note):** The THEM-02 fix is for the SIDEBAR button only (handled in Plan 02). The in-module "Run Audit" button should use btn-primary (it's a primary action -- executing the audit).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "files <- c('R/mod_seed_discovery.R', 'R/mod_query_builder.R', 'R/mod_topic_explorer.R'); for (f in files) { lines <- readLines(f); if (any(grepl('btn-success', lines))) { cat('FAIL:', f, 'still has btn-success\n'); quit(status=1) } }; cat('All search buttons updated to btn-primary\n')"</automated>
  </verify>
  <done>All search/execute buttons use btn-primary (lavender). Add-to-notebook buttons retain btn-outline-success. Stop/Cancel buttons retain btn-warning. Delete buttons retain btn-danger. All modules follow semantic color policy (DSGN-03).</done>
</task>

<task type="auto">
  <name>Task 2: Restructure notebook title bars with responsive layout and delete button repositioning</name>
  <files>R/mod_document_notebook.R, R/mod_search_notebook.R, app.R</files>
  <action>
  **Step 1: Update document notebook title bar (R/mod_document_notebook.R).**

  Find the title bar section (~lines 69-116). Restructure to use flexbox wrap:

  ```r
  div(
    class = "d-flex flex-wrap gap-2 mb-2 align-items-center",
    # Preset buttons -- keep icon+text per user decision
    actionButton(ns("btn_overview"), "Overview",
                 class = "btn-sm btn-outline-primary", icon = icon_layer_group()),
    actionButton(ns("btn_key_points"), "Key Points",
                 class = "btn-sm btn-outline-primary", icon = icon_key_points()),
    actionButton(ns("btn_methodology"), "Methodology",
                 class = "btn-sm btn-outline-primary", icon = icon_...()),
    actionButton(ns("btn_lit_review"), "Lit Review",
                 class = "btn-sm btn-outline-primary", icon = icon_table()),
    actionButton(ns("btn_slides"), "Slides",
                 class = "btn-sm btn-outline-primary", icon = icon_file_powerpoint()),
    # Embed Papers -- btn-outline-primary per user decision
    actionButton(ns("embed_papers"), "Embed Papers",
                 class = "btn-sm btn-outline-primary", icon = icon_...()),
    # Export dropdown -- btn-outline-secondary (secondary action)
    # ... keep existing export dropdown structure ...
    # Send -- btn-primary (solid, main action) per user decision
    actionButton(ns("send_btn"), "Send",
                 class = "btn-sm btn-primary", icon = icon_paper_plane()),
  )
  ```

  Key changes:
  - Wrap container: `d-flex flex-wrap gap-2` -- buttons reflow naturally on tight screens
  - All preset buttons: `btn-sm btn-outline-primary` (lavender outline, consistent)
  - Send button: `btn-sm btn-primary` (solid lavender, main action)
  - Export: `btn-sm btn-outline-secondary` (kept as-is -- export is less important)

  Read the actual file to get exact button IDs and icons before modifying.

  **Step 2: Move delete button closer to notebook title in app.R.**

  In app.R where notebook headers are rendered (~lines 1008-1028), restructure:

  ```r
  # Document notebook header
  div(
    class = "d-flex align-items-center gap-2 mb-3",
    h4(class = "mb-0", tagList(icon_file_pdf(class = "text-danger me-2"), nb$name)),
    actionButton("delete_nb", NULL, class = "btn-outline-danger btn-sm",
                 icon = icon_delete(), title = "Delete notebook")
  )
  ```

  Remove the `justify-content-between` and put delete immediately after the title with `gap-2` spacing. This places delete visually next to the title (spatial proximity per user decision).

  Same pattern for search notebook header.

  **Step 3: Update search notebook title bar (R/mod_search_notebook.R).**

  Apply the SAME styling rules as document notebook per user decision:
  - Same flex-wrap container
  - Send = btn-primary (solid)
  - Preset buttons = btn-outline-primary (outline)
  - Export = btn-outline-secondary
  - Search button (if in title bar) = btn-primary (not btn-success)

  Verify the exact button structure by reading the file first, then apply consistent changes.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "for (f in c('R/mod_document_notebook.R', 'R/mod_search_notebook.R')) { lines <- readLines(f); if (!any(grepl('flex-wrap', lines))) { cat('FAIL:', f, 'missing flex-wrap\n'); quit(status=1) } }; cat('Both title bars have flex-wrap\n')"</automated>
  </verify>
  <done>Both notebook title bars use d-flex flex-wrap gap-2 for responsive wrapping (THEM-05). Preset buttons are uniform btn-outline-primary with icon+text (THEM-04). Delete button positioned next to title. Search notebook matches document notebook styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify button theming and title bar layout across all modules</name>
  <what-built>All buttons across the app follow semantic color policy: search=lavender, add=green, stop=yellow, delete=red. Notebook title bars use responsive flexbox wrap. Delete button next to title. Both notebook types have consistent button styling.</what-built>
  <how-to-verify>
    1. Run the app: `Rscript -e "shiny::runApp('.', port = 8080)"`
    2. Open Discover from Paper -- verify search button is lavender (not green)
    3. Open Build a Query -- verify search button is lavender (not green)
    4. Open Explore Topics -- verify search button is lavender (not green)
    5. Create a document notebook -- verify:
       a. Preset buttons (Overview, Key Points, etc.) are lavender outline
       b. Send button is solid lavender
       c. Delete button is close to notebook title (not far right)
       d. Narrow the browser window -- buttons should wrap to second row naturally
    6. Create a search notebook -- verify same styling as document notebook
    7. In search results, verify "Add to Notebook" buttons are still green
    8. Toggle dark mode -- verify all button colors remain correct
    9. Check Citation Audit module -- "Run Audit" button should be lavender
  </how-to-verify>
  <resume-signal>Type "approved" if button theming looks correct across all modules, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. No btn-success on search/execute buttons (grep confirms)
2. Both notebook title bars have flex-wrap class
3. Delete button adjacent to title (no justify-content-between)
4. Add-to-notebook buttons still green (grep confirms btn-outline-success present)
5. Visual check passes in both light and dark mode
</verification>

<success_criteria>
- All buttons follow semantic color scheme from Phase 45 (DSGN-03)
- Notebook buttons are uniform: all icon+text, consistent hover states (THEM-04)
- Title bar uses available space with flex-wrap responsive layout (THEM-05)
- Both notebook types have identical button styling rules
- User approves visual appearance in checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/47-sidebar-button-theming/47-03-SUMMARY.md`
</output>
