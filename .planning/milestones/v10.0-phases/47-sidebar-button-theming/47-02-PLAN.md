---
phase: 47-sidebar-button-theming
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - app.R
  - www/custom.css
autonomous: false
requirements: [THEM-01, THEM-02, THEM-03]

must_haves:
  truths:
    - "Sidebar buttons are reordered: Search NB, Document NB, divider, Import, Discover, Topics, Query, Network, Audit"
    - "Both notebook creation buttons have solid fill (btn-primary)"
    - "Discovery/utility buttons have distinct rainbow outline colors"
    - "Import Papers button uses a custom Catppuccin color distinct from all semantic colors"
    - "Citation audit button is readable in light mode (not btn-outline-secondary)"
    - "Sidebar title 'Notebooks' is removed"
    - "Colors adapt correctly when toggling between light and dark mode"
  artifacts:
    - path: "app.R"
      provides: "Reordered sidebar with new button classes and divider"
      contains: "btn-peach"
    - path: "www/custom.css"
      provides: "Custom CSS for non-standard Catppuccin button colors (peach, sky) with light+dark mode"
      contains: ".btn-peach"
  key_links:
    - from: "www/custom.css"
      to: "app.R"
      via: "Bootstrap classes on sidebar buttons"
      pattern: "btn-peach|btn-outline-sky"
    - from: "app.R"
      to: "R/theme_catppuccin.R"
      via: "LATTE/MOCHA color constants for CSS values"
      pattern: "LATTE\\$|MOCHA\\$"
---

<objective>
Restructure the sidebar with reordered buttons, rainbow outline colors, custom Catppuccin color classes, and improved light mode readability for citation audit.

Purpose: Apply the user's locked sidebar hierarchy decisions (THEM-01, THEM-02, THEM-03) with correct colors in both themes.
Output: Reordered sidebar in app.R, custom button CSS classes in www/custom.css
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-sidebar-button-theming/47-CONTEXT.md
@.planning/phases/47-sidebar-button-theming/47-RESEARCH.md
@.planning/phases/47-sidebar-button-theming/47-01-SUMMARY.md

@app.R
@www/custom.css
@R/theme_catppuccin.R
</context>

<interfaces>
<!-- Current sidebar in app.R lines 160-190: -->
<!-- sidebar = sidebar(title = "Notebooks", width = 280, ...) -->
<!-- Buttons: new_document_nb, new_search_nb, discover_paper, build_query, -->
<!--          explore_topics, new_network, citation_audit, import_papers -->

<!-- Catppuccin palette colors available for custom classes: -->
<!-- LATTE$peach = "#fe640b", MOCHA$peach = "#fab387" -->
<!-- LATTE$sky = "#04a5e5", MOCHA$sky = "#89dceb" -->
<!-- LATTE$sapphire = "#209fb5", MOCHA$sapphire = "#74c7ec" -->
<!-- LATTE$base = "#eff1f5", MOCHA$base = "#1e1e2e" -->
<!-- LATTE$text = "#4c4f69", MOCHA$text = "#cdd6f4" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create custom CSS button classes and restructure sidebar</name>
  <files>www/custom.css, app.R</files>
  <action>
  **Step 1: Add custom Catppuccin button classes to www/custom.css.**

  Append to the END of www/custom.css (do NOT modify existing citation network styles):

  Create outline button variants for non-standard Catppuccin colors. Each needs BOTH light mode (default) and dark mode ([data-bs-theme="dark"]) rules. Include hover states.

  Colors to create:
  - `.btn-outline-peach` — for Import Papers (distinct from all 6 semantic colors)
  - `.btn-outline-sky` — available for sidebar buttons needing another distinct color

  For each custom outline button:
  ```css
  .btn-outline-peach {
    background-color: transparent;
    border-color: #fe640b;  /* LATTE$peach */
    color: #fe640b;
  }
  .btn-outline-peach:hover {
    background-color: #fe640b;
    border-color: #fe640b;
    color: #eff1f5;  /* LATTE$base - light text on hover */
  }
  [data-bs-theme="dark"] .btn-outline-peach {
    border-color: #fab387;  /* MOCHA$peach */
    color: #fab387;
  }
  [data-bs-theme="dark"] .btn-outline-peach:hover {
    background-color: #fab387;
    border-color: #fab387;
    color: #1e1e2e;  /* MOCHA$base - dark text on hover */
  }
  ```

  Same pattern for `.btn-outline-sky` using LATTE$sky (#04a5e5) and MOCHA$sky (#89dceb).

  **Step 2: Restructure sidebar in app.R.**

  Replace the sidebar section (lines ~160-190) with the new hierarchy per user decisions:

  1. Remove `title = "Notebooks"` from sidebar() — set to NULL or remove the argument
  2. Reorder buttons top to bottom:
     - New Search Notebook — `btn-primary` (solid fill), icon = `icon_search()`
     - New Document Notebook — `btn-primary` (solid fill), icon = `icon_file_pdf()`
     - Subtle divider: `div(class = "border-top my-2")` (thin line between creation and discovery)
     - Import Papers — `btn-outline-peach` (custom class), icon = `icon_file_import()`
     - Discover from Paper — `btn-outline-success` (green), icon = `icon_seedling()`
     - Explore Topics — `btn-outline-warning` (yellow), icon = `icon_compass()`
     - Build a Query — `btn-outline-info` (sapphire), icon = `icon_wand()`
     - Citation Network — `btn-outline-primary` (lavender, NOT danger-red per user decision), icon = `icon_diagram()`
     - Citation Audit — `btn-outline-sky` (custom, readable in light mode — THEM-02 fix), icon = `icon_audit()`

  3. Use wrapper icons (from Plan 01) for all sidebar buttons — no raw icon() calls.

  **Color assignment rationale (per user decisions):**
  - Both notebook buttons: solid primary (lavender) — main sidebar actions
  - Import Papers: peach outline — distinct from all semantic colors per user request
  - Discover: success (green) — kept per existing
  - Topics: warning (yellow) — kept per existing
  - Query: info (sapphire) — kept per existing
  - Citation Network: primary (lavender) outline — moved from danger to avoid semantic conflict
  - Citation Audit: sky (custom) — THEM-02 fix, readable in light mode (sky #04a5e5 has much better contrast than surface1 #bcc0cc)

  Also update the notebook list icons in the renderUI for notebook_list (around line 386, 402, 424) to use wrappers if not already done in Plan 01.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "css <- readLines('www/custom.css'); has_peach <- any(grepl('btn-outline-peach', css)); has_sky <- any(grepl('btn-outline-sky', css)); has_dark_peach <- any(grepl('dark.*btn-outline-peach', css)); stopifnot(has_peach, has_sky, has_dark_peach); cat('Custom CSS classes verified\n')"</automated>
  </verify>
  <done>Sidebar reordered per user hierarchy. Custom .btn-outline-peach and .btn-outline-sky CSS classes exist with both light and dark mode support. Citation audit uses sky (readable in light mode). "Notebooks" title removed. Divider separates creation from discovery buttons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sidebar appearance in both themes</name>
  <what-built>Sidebar restructured with reordered buttons, rainbow outline colors, custom peach/sky classes, divider between notebook creation and discovery buttons, "Notebooks" title removed, citation audit readable in light mode</what-built>
  <how-to-verify>
    1. Run the app: `Rscript -e "shiny::runApp('.', port = 8080)"`
    2. Check sidebar button order matches: Search NB, Document NB, [divider], Import, Discover, Topics, Query, Network, Audit
    3. Verify both notebook buttons are solid lavender (btn-primary)
    4. Verify Import Papers is peach outline
    5. Verify citation audit button is clearly readable (sky color, not gray)
    6. Toggle dark mode — verify all sidebar buttons remain readable with correct colors
    7. Verify "Notebooks" title text is gone from sidebar top
    8. Verify divider (thin line) appears between Document NB and Import Papers
  </how-to-verify>
  <resume-signal>Type "approved" if sidebar looks correct in both themes, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Sidebar has 9 buttons in correct order with divider after second
2. Custom CSS file has .btn-outline-peach and .btn-outline-sky with light+dark mode rules
3. No `title = "Notebooks"` in sidebar() call
4. Citation audit uses btn-outline-sky (not btn-outline-secondary)
5. Visual check in both Latte and Mocha themes passes
</verification>

<success_criteria>
- Sidebar button order matches user's locked decision (THEM-01)
- Citation audit button clearly readable in light mode — sky color instead of gray (THEM-02)
- Import Papers has distinct peach color from all standard semantic buttons (THEM-03)
- All colors adapt correctly when toggling dark mode (THEM-01)
- User approves visual appearance in checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/47-sidebar-button-theming/47-02-SUMMARY.md`
</output>
