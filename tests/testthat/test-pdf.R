library(testthat)

# chunk_text() was removed in Phase 23 (legacy code removal).
# All PDF chunking now goes through chunk_with_ragnar() in _ragnar.R.
# Integration tests for process_pdf() are in test-ragnar.R.
